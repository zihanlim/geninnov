# Build Progress

_Last updated: 2026-07-22 (T26 full-suite verification)_

## Current status

**Phase 1–5 implemented.** Theme detection, trade ranking, position sizing, daily P&L, risk metrics, and the **Q1 reasoning pipeline** (L0–L6) are all wired into `scripts/daily_refresh.py`. The L5 agent (`backend/services/q1_agent.py`) produces a structured $100M long-short book with per-trade thesis, factor tilts, scenario analysis, and citation-verified numeric claims. The Q1 thesis renders on the `/research` page with inline citation footnotes. See "Pending" below for what's left to actually run against the deployed environment.

## Project structure

```
.github/workflows/ci.yml       ← GitHub Actions CI
CLAUDE.md                       ← AI/project onboarding (read this first)
PROGRESS.md                     ← this file
docs/
├── adrs/                      ← Architecture Decision Records (im-Jarvis format)
│   ├── README.md               ← index
│   ├── 0001-supabase-over-postgres.md
│   ├── 0002-vercel-for-frontend.md
│   ├── 0003-daily-cron-over-fastapi.md
│   ├── 0004-no-redis-in-phase1.md
│   ├── 0005-vader-over-paid-sentiment.md
│   ├── 0006-minmax-over-zscore-hypescore.md
│   ├── 0007-two-method-theme-discovery.md
│   └── 0008-tier1-macro-anchors-practitioner.md
└── superpowers/
    ├── specs/
    │   └── 2026-07-21-andromeda-market-theme-platform-design.md
    └── plans/
        └── 2026-07-21-andromeda-implementation-plan.md
```

## Completed

| Date | Entry |
|---|---|
| 2026-07-24 | **Launch-polish pass — `/method` formulas fixed, Mentions populated, mobile layout ([ADR-0034](docs/adrs/0034-scoring-config-anon-read-policy.md)).** Driving the live site with Playwright surfaced three real defects. (1) **`/method` formula panels showed "NO ROWS"** for HypeScore/TradeScore/EdgeScore: `001` gave the seven display tables a `"Public read"` anon RLS policy but skipped `scoring_config`, so the frontend anon key read it as an empty set (backend unaffected — `service_role` bypasses RLS). Migration 027 adds the anon read policy (applied to prod); all 19 live weights now render. (2) **"Mentions —" was blank on every theme card**: `summarise()` fetched `mention_count_1d` but never surfaced it. Fixed to headline `mention_count_7d_avg` ("Mentions/day") since the 1-day count is structurally often 0 (a run frequently collects no same-day-dated article) — the stable windowed figure is the honest attention volume. (3) **Body horizontal scroll on phones**: the app-shell grid's implicit column defaulted to `auto`, sizing to the 640px position table's min-content and blowing past a 375px viewport; fixed with `grid-cols-[minmax(0,1fr)]` + a `min-w-0` wrapper on `{children}`, plus a tighter mobile TopBar. Desktop/tablet/phone verified clean via Playwright. |
| 2026-07-24 | **EdgeScore Stage 5 — sentiment demotion + IC-fit weights ([ADR-0033](docs/adrs/0033-edge-stage5-sentiment-ic-weights.md)).** Completes the 5-stage redesign. (1) Sentiment re-enters as a MINOR CONTRARIAN 5th component: `sentiment_signal = -tanh(avg_sentiment/0.4)` — extreme optimism is crowding to fade, not the coin-flip direction driver it wrongly was; small weight. (2) A multi-component IC backtest (`scripts/backtest_edge.py`) computes each component's rank IC vs forward 1m returns and **the data overturned a prior**: **Carry IC +0.277 (t=2.77, p=0.007, 60.6% hit) — strong & significant**, while **Trend is weak (+0.033, p=0.30)**. Weights shrunk 50% toward priors (thin data → no overfit) → migration 026 rebalances `edge_carry_weight` 0.20→0.34, `edge_trend_weight` 0.35→0.20, adds `edge_sentiment_weight` 0.05; RegimeFit/Sentiment keep priors (not IC-testable yet). Persisted `sentiment_signal` on `theme_signals_history`/positions/candidates (migration 026); frontend `edgeContributions` + `/method` worked example + drawer now reconcile all 5 components. Full backend suite + `next build` pass. |
| 2026-07-24 | **Never surface mock news (data integrity).** A `mock_brave` headline ("Investors eye US Election…") showed on the frontend despite `ANDROMEDA_ALLOW_MOCK=0`: `persist_theme_news` upserted on `(theme_id,run_date,headline)` and never cleared the run's prior rows, so mock from an earlier same-day (pre-flag) run lingered beside the real Brave data and was faithfully surfaced by the new raw-headline drill-down. Fix: idempotent re-run (prune the run_date before writing) + defense-in-depth (never PERSIST a `mock_*` source when mock is off). Live data cleaned (24 stale mock rows removed; `theme_news` = 498 real Brave, 0 mock; `data_source` = real/none). |
| 2026-07-23 | **Frontend caught up to the 4-component EdgeScore (closes the follow-up flagged below) + legacy pages retired.** `/method` §4 now renders the full `w_trend·Trend + w_regime·Regime + w_carry·Carry + w_value·Value` formula with **all four weights read live** from `scoring_config` (`edge_{trend,regime,carry,value}_weight`), the abstention rule (`\|EdgeScore\| < edge_abstain_threshold`), the conviction × inverse-vol sizing note, a worked example that reconciles all four persisted components to `edge_score`, and a new **abstention roster** table (themes with `\|EdgeScore\| < threshold` this run, sorted by gap-to-trade). `TradeDerivationDrawer` replaced its stale 2-component/0.6·0.4 block with the 4-bar decomposition (each component × live weight, null → 0 flagged), a `conviction = \|EdgeScore\|/vol` panel, the abstention line, and conviction-normalised sizing (Σ\|conviction\| denominator from `portfolio_positions.conviction`) in place of `HypeScore/100`; defaults now fall back to `DEFAULT_EDGE_WEIGHTS` from `frontend/lib/themeSignals.ts`, never hardcoded. **Legacy `/trades`, `/portfolio`, `/research`** — which duplicated the triad and (for `/trades`) bypassed the L5 citation guardrail — are now thin server `redirect()` → `/book` (idiomatic Next.js), so bookmarks and inbound links resolve to the consolidated, guardrail-verified surface. Frontend-only; owned files typecheck + lint clean. |
| 2026-07-23 | **EdgeScore Stages 3–4 — carry, value, abstention, conviction sizing ([ADR-0032](docs/adrs/0032-edge-carry-value-abstention-sizing.md)).** EdgeScore is now 4 components: `0.35·Trend + 0.25·RegimeFit + 0.20·Carry + 0.20·Value`. **Carry** (`carry_signal`) = L0 yield/spread income (credit HY OAS, rates real yield, fx USD rate); **Value** (`value_signal`) = z-score of L0 levels vs `macro_daily_history` (wide/cheap = long). **Abstention**: `rank_trade_candidates(abstain_threshold=0.15)` drops themes whose \|EdgeScore\| < 0.15 — a weak/conflicting signal yields no position, not a forced one. **Conviction sizing**: `allocate_portfolio(size_by="conviction")` weights ∝ `\|EdgeScore\|/vol` (conviction × inverse-vol), not HypeScore. Migration 024 rebalances the weights + adds `carry_signal`/`value_signal`/`conviction` columns. Real-data check (regime late/risk-on): 4 themes carry conviction, **4 abstain** — Corporate Credit abstains on a carry(+0.59)-vs-value(−0.75) conflict; US Dollar abstains on a flat signal. +10 tests (367 total). Stage 5 (sentiment demotion + IC-weighted composite) remains. NOTE: `/method` §4 + `TradeDerivationDrawer` now show only trend+regime and the 0.6/0.4 weights — a frontend follow-up should add carry/value and read the 4 rebalanced weights. |
| 2026-07-23 | **EdgeScore direction surfaced (was actively mis-explained).** After ADR-0031 made direction `sign(EdgeScore)` (trend + regime, not sentiment), the frontend still taught the superseded rule: `/method` §3 stated "the sign of TradeScore picks the side" and `TradeDerivationDrawer` showed "TradeScore > 0 → LONG" — a correctness/trust bug, since the backend row's own `entry_thesis` already reads "direction from EdgeScore". Corrected both to reframe TradeScore as intra-side ranking. Added a live `/method` §4 (EdgeScore formula with `scoring_config` weights + a reconciling worked example), a per-position "why this side" EdgeScore breakdown on `/book`, and the EdgeScore basis in the trade drawer. New `fetchThemeEdge` in `frontend/lib/themeSignals.ts` reads `edge_score/trend_signal/regime_bias` (migration 023). Verified against live data: 8 themes carry real EdgeScores that reconcile to `0.6·trend + 0.4·regime`. |
| 2026-07-23 | **EdgeScore — direction from trend + regime, not sentiment ([ADR-0031](docs/adrs/0031-edge-score-direction-signal.md), [spec](docs/superpowers/specs/2026-07-23-edge-score-direction-redesign.md)).** Direction was `sign(TradeScore) ≈ sign(near-zero VADER sentiment)` — a noise-level basis for a $100M book. New `backend/services/edge_signals.py` computes `EdgeScore = 0.6·Trend + 0.4·RegimeFit` (both [-1,1]): **Trend** = theme basket ~6-month price momentum (tanh-squashed); **RegimeFit** = `risk_beta(asset_class)·sentiment_sign + cycle_tilt` from the L3 regime (risk-off ⇒ long rates/USD, short equity/HY; late-cycle defensive). `rank_trade_candidates` now directions/ranks on `score_key="edge_score"`. Migration 023 adds `edge_score`/`trend_signal`/`regime_bias` columns + config weights. **IC-checked** (`scripts/backtest_edge.py`): Trend rank IC **+0.033** over 6y / 15-ETF panel — positive but weak (p=0.30, hit 54.9%), honestly caveated; wired in because it's economically anchored vs sentiment noise, weights to be IC-fit in Stage 5. Full 5-stage design specced; Stages 3–5 (carry, value, abstention, IC-weighting) planned. +17 tests (357 total). |
| 2026-07-23 | **Unified the L5 candidate pool with L1 ([ADR-0030](docs/adrs/0030-unify-l5-candidate-pool-with-l1.md)).** Found while tracing "how does the system decide long/short": the L5 agent had its *own* copy of the direction rule. `run_q1_agent` received the L1 pool but `screen_candidates` overwrote it, rebuilding from `theme_scores` with its own `hype>=threshold ∩ sign(TradeScore)`, no backfill — so ADR-0029's two-sided fix reached the `trade_candidates`/`portfolio_positions` tables but **not** the L5 `research_recommendations` book. A two-sided L5 book only happened by luck (US Election drifting 49.9→52.0 over the gate). `screen_candidates` now *filters* the L1 pool (inheriting the hype gate, direction, and two-sided backfill) and applies only the L5-specific lens + R² + dedupe + cap. Also unifies the asset universe onto the DB `theme_assets` mapping (was a hardcoded `_theme_default_assets`). The deterministic fallback now ranks the same screened pool instead of rebuilding from `theme_scores` (an adversarial review caught that the old rebuild could fabricate a one-sided book on LLM failure). +10 regression tests (341 total). |
| 2026-07-23 | **Two-sided book — decouple direction from the hype gate + revive momentum ([ADR-0029](docs/adrs/0029-two-sided-book-decouple-direction-revive-momentum.md)).** A run produced a 100% net-short, zero-long book. Cause: `rank_trade_candidates` built `long_pool = {hype≥50} ∩ {trade_score>0}`, and that day both hype-eligible themes (China Growth 90, US Dollar 71.8) were mildly bearish while every positive-signal theme sat below the gate → empty intersection. Compounding it, `hype_momentum` was 0 for every theme: `compute_trade_scores` sought a prior row dated *exactly* D-1, so irregular cadence + NULL historical `hype_score` left momentum dead and direction decided by fourth-decimal sentiment noise. Fix: (1) eligible-first ranking with `min_side` backfill from the strongest sub-threshold themes — two-sided whenever opposite-sign signal exists, never fabricated, backfills sized small by hype-weighting; (2) momentum reads the most-recent prior `hype_score` (self-heals once the daily job persists one). +6 backend tests (331 total). |
| 2026-07-23 | **L5 crash fix — `finalise_book_analytics` None-format.** The daily GitHub Action's L5 stage died with `unsupported format string passed to NoneType.__format__`: `worst = min(estimated_book_return for each scenario)` returned `None` when a sized pick had no factor beta *and* no direct scenario shock (unestimable scenario), and `f"{worst:+.2%}"` crashed the whole stage — before persist, so no book was written. Did not reproduce locally (local book had full betas). Filter `None` estimates before `min()` (default 0.0) + regression test. A full L0→L5 re-run confirms `pipeline_runs` L5 flips `partial → success`. |
| 2026-07-23 | **UI/UX review + remediation.** Full review written to `docs/superpowers/specs/2026-07-23-andromeda-uiux-review-and-redesign.md`. Closed three schema/query mismatches that had silently disabled live surfaces: `research_recommendations.book_metrics_summary`/`.scenario_table` and `regime_classifications.narrative` were selected but never existed (400/42703, so `/research` rendered its empty state unconditionally even with a valid row), and `portfolio_factor_exposure` was queried but never existed (404, so both factor panels showed "Awaiting factor run" while 36 real betas sat unused). |
| 2026-07-23 | **Migration 022 + L5 9th node.** `finalise_book_analytics` recomputes book metrics, 4-scenario stress and the 252d correlation matrix on the FINAL SIZED book and persists them as structured JSONB (`book_metrics`, `scenario_results`, `correlation_pairs`, `cap_utilisation`, `screening_funnel`, `lens`); previously computed every run, formatted into a prompt string, and discarded. `size_positions` now delegates to `allocate_portfolio`, so the documented 20/30/35 caps are actually enforced and shorts carry signed weight. `screen_candidates` records per-filter attrition. Migration 022 also adds the `portfolio_factor_exposure` view. See ADR-0024. |
| 2026-07-23 | **L2 wired into the pipeline.** `refresh_factor_exposures` added to `daily_refresh.main()` as an instrumented stage recorded in `pipeline_runs`. L2 had never been orchestrated — `daily_refresh` ran L0/L1/L3/L4/L5 only — so factor betas were whatever an ad-hoc run last left behind, which silently disabled the R² screen, zeroed book tilts, and pushed scenario analysis onto hand-written default betas. |
| 2026-07-23 | **New surfaces `/book`, `/risk`, `/method`** (ADR-0025). `/book` merges what was split across `/trades`+`/portfolio`+`/research` into one expandable row per position (thesis, counter-thesis, catalysts, full sizing chain, factor tilts, per-scenario stress, cap utilisation) plus the screening funnel. `/risk` renders stress scenarios, correlation heatmap, cap-headroom bars, book factor tilt, risk cards and drawdown. `/method` answers the design brief's "explain how you would do this" with live `scoring_config` weights, a worked HypeScore example, `pipeline_runs` stage health and the citation-guardrail audit. |
| 2026-07-23 | **Fabricated UI content removed.** Hardcoded per-theme investment theses (`deriveThesis`), "synthetic but stable" sub-score attribution (`splitDelta`), the per-ticker `FALLBACK_THESIS` map, an invented Kelly-fraction sizing formula, a TradeScore panel whose stated 0.55/0.45 weights disagreed with its own 0.85/0.15 arithmetic, fabricated per-source counts (`mentions x 0.62/0.30/0.08`), permanently-green "Pipeline healthy", hardcoded "12 themes / 32 tickers", stale prompt version, and "Conviction: HIGH" on every pick. Replaced with real queries or `EmptyState`/`QueryErrorState` that name cause and remedy. |
| 2026-07-23 | **Two data-integrity fixes found while wiring the UI.** (1) `themes.*_score` persist as [0,1] while `hype_score` is 0-100; the heatmap and `SubScoreBars` rendered both on one scale, so every sub-score cell read "1" on deep red. `toDisplayScore` in `frontend/lib/themeSignals.ts` is now the single conversion point. (2) `persist()` wrote `corr_score = minmax_norm(abs(price_corr))` while `hype_score()` consumes a bare `abs(price_corr)` — so the persisted sub-scores could not reproduce the persisted score (a phantom +15.0 on live data). Now persists the value actually used. |
| 2026-07-21 | Project scaffolded: `CLAUDE.md`, `PROGRESS.md`, `ADR-0001–0008` (individual files, im-Jarvis format), design spec, implementation plan, GitHub Actions CI. |
| 2026-07-21 | Python pipeline complete: B1 VADER sentiment, B2 Brave News, B3 Reddit, B4 Yahoo Finance, B5 HypeScore, B6 TradeScore, B7 daily refresh, B8 theme discovery — 59 tests passing. |
| 2026-07-21 | Frontend complete: C1 layout+Supabase, C2 dashboard+ThemeFeed+HypeGauge+MarketCorrelationChart+DataSourceStatus, C3 trades page, C4 portfolio page, C5 research page, D1 vercel.json. Build passes. |
| 2026-07-21 | Deployed to Vercel + Supabase: schema applied, RLS configured, seed data loaded. Phase 1 live. |
| 2026-07-21 | Bug fix pass on Phase B pipeline: replaced `"NOW()"` string with real ISO timestamp, added asset-map fallback for bootstrap timing, rewrote `call_brave_mcp.js` to actually call the Brave News API, added the missing Tier 1 asset seed (`LATERAL VALUES` INSERT for 29 rows). 60 tests passing. |
| 2026-07-21 | Phase 3 implemented: `backend/services/trade_ranker.py` (rank top 5 longs/shorts + size $100M portfolio) + `backend/services/risk_engine.py` (VaR, CVaR, Sharpe, Beta, HHI, daily P&L). `scripts/daily_refresh.py` now persists to `trade_candidates`, `portfolio_positions`, `portfolio_risk`, `portfolio_returns`. 106 tests passing. |
| 2026-07-21 | Live bootstrap migration applied to live Supabase (`004_bootstrap_live.sql`): added `hype_score` + `trade_score` columns to `theme_signals_history`, seeded 29 Tier 1 asset mappings. `daily_refresh.py` made robust to the pre-migration state (catches `APIError` on missing column, falls back to sentiment-only `TradeScore`). `.env.example` template committed. First end-to-end live run succeeded: 10 longs persisted, $100M sized into 10 positions, +0.84% daily return, HHI=1000. 107 tests passing. |
| 2026-07-21 | Phase 5 built: M1 `macro_indicators` + `macro_daily_history` tables (migration 005), M1 FRED+yfinance fetcher (`backend/data/macro_fetcher.py`), M2 `factor_exposures` table (migration 006), M2 Ken French FF5+UMD fetcher + rolling regression (`backend/data/factor_fetcher.py`), M3 rule-based regime classifier (`backend/services/regime_classifier.py`), M5 LangGraph research agent skeleton (`backend/agents/research_agent.py`) with LLM stub provider (MockLLM), new research page reads from `research_recommendations` table (`frontend/app/research/page.tsx`), migrations 005+006+007 applied to live DB, regime seed row inserted, `backend/__init__.py` + `backend/services/__init__.py` added to fix package imports, `apply_migration.py` script added. 151 tests passing. |
| 2026-07-21 | Renamed Q1/Q2 test-naming off production tables: `q1_recommendations` → `research_recommendations`, `q1_agent_runs` → `research_agent_runs`. Migration 008 applied to live Supabase (renamed in place, RLS policies renamed). Renamed files: `backend/agents/q1_agent.py` → `backend/agents/research_agent.py`, `backend/services/q1_agent.py` (table refs + `run_q1_agent` → `run_research_agent` + print log messages), `scripts/daily_refresh.py` (import + log messages), `tests/backend/test_q1_agent.py` → `test_research_agent.py`. New `MockLLMProvider` tests added; `size_positions` tests aligned with the actual one-pass implementation. 198 tests passing. |
| 2026-07-21 | **Research-first frontend redesign.** Replaced the original Bloomberg-terminal dashboard with a conviction-first layout: macro regime hero, top 3 themes as cards with thesis+sparkline+sub-score bars+catalyst/crowding/Δ1d, watchlist with momentum, filterable Trade Ideas table with thesis column, allocation bar (not treemap), risk grid with units context, factor exposure panel. Global nav (was missing), live feed status bar. Old orphaned components removed (`HypeGauge`, `ThemeFeed`, `MarketCorrelationChart`, `PortfolioTreemap`, `DataSourceStatus`). New design tokens in `tailwind.config.ts` + `globals.css`. Build/typecheck/lint clean. Deployed to Vercel. |
| 2026-07-21 | **ADRs 0009–0011 (UX provenance layer).** Recognized that the platform's quantitative outputs (HypeScore, TradeScore, regime classification, factor betas) are difficult to audit in their current UI. Adopted Perplexity-Finance-style provenance patterns: research-first design philosophy (ADR-0009), citation footnotes on every numeric claim in the Q1 thesis (ADR-0010), per-theme derivation drawer showing raw signals → normalization → weights → final score (ADR-0011). Architecture updated to add L7 (UI derivation layer). |
| 2026-07-21 | **Q1 thesis layer documented across all surfaces.** Updated CLAUDE.md, PROGRESS.md, ARCHITECTURE.md, spec §1/§2/§14, and added ADRs 0012–0014 to reflect that the Q1 reasoning pipeline is implemented end-to-end: L0 macro ingest + L2 factor exposures + L3 regime + L4 risk feed the L5 reasoning agent (`run_q1_agent` with 8 nodes including `compute_book_metrics` and `run_scenario_analysis`), whose citation-guarded output renders on `/research`. The thesis layer was previously only in spec §14 — now it's visible at every doc surface. |
| 2026-07-21 | **Lens mode shipped (multi-asset / credit / rates / equity / fx / commodity).** Migration 009 adds `theme_assets.asset_class` with backfill for the Tier 1 universe. `run_q1_agent` accepts a `lens` parameter that filters `screen_candidates` and injects a lens-specific framing instruction into the LLM prompt. New `<LensSelector>` segmented control on `/portfolio` and `/trades` lets the user re-filter the book by asset class. Credit lens is the natural fit for Andromeda Capital's mandate. ADR-0015 documents the design. |
| 2026-07-22 | **T9 + T20 + T22 review findings (Important) landed.** `compute_and_persist_risk` migrated to the derive-aware `compute_risk` API and persists the full L4 `NumericDerivation` bundle to a new `numeric_derivations` JSONB column on `portfolio_risk` (migration 015). Legacy `compute_risk_metrics` shim removed (no other callers). `SECTOR_MAP` / `GEO_MAP` `.get(asset, "Other")` fallbacks in `trade_ranker.py` + `book_metrics.py` replaced with direct-lookup so unmapped tickers raise `KeyError`; SPY/IWM/BULL added to taxonomy so the existing tests stay green. `compute_trade_scores` now plumbs per-theme `elapsed_days` from the most recent prior `theme_signals_history.run_date` and the brief's `compute_hype_scores([])` empty-input crash is short-circuited. Three discrete commits (`cb9a286b`, `edb1070e`, `2aa788f2`); 227 backend tests green. |
| 2026-07-22 | **System architecture diagram + doc-sync rule.** `ARCHITECTURE.md` now leads with a single `mermaid` flowchart that wires every L0–L7 node, every Supabase table, every external data source, the LLM provider swap (MiniMax → Anthropic), the cron trigger, and the Vercel frontend — color-coded so the two LLM calls (`classify_news` and `reason_picks`) are visually unmistakable. `CLAUDE.md` gains a mandatory "Doc Sync Rule" section that tells every agent to update the mermaid diagram + `ARCHITECTURE.md` + `PROGRESS.md` + an ADR (when warranted) in the same change. Diagrams rot faster than code; this rule is the antidote. |
| 2026-07-22 | **T1–T28 full-system review landed.** Derivations module is live — `backend/derivations/{numeric.py,advisory.py}` + TS mirrors in `frontend/lib/derivations/` + L7 status/portfolio/research read-models (`StatusBadge`, `FreshnessLabel`, `UncertaintyBand`, `CumulativeReturn`, `DailyPLHistory`, `ExposureSummary`, `ThesisBlock`). L5 (`q1_agent.py`) emits a strict `AdvisoryDerivation` (T18: fallback cannot be `verified`); L4 (`compute_risk`) returns `NumericDerivation` (T9); per-position provenance columns added to `portfolio_positions` and `research_recommendations` (T13 / migration 013); L4 provenance column on `portfolio_risk` (T15 / migration 015). `pipeline_runs` table populated per stage (T10, migration 012); `portfolio_cumulative_return` table populated compounded daily (T14, migration 014). `classify(ticker)` taxonomy seam between `frontend/lib/assetMetadata.ts` and `trade_ranker.py` / `book_metrics.py` (T20). `compute_trade_scores` plumbs per-theme `elapsed_days` (T22); `hype_calculator` uses `abs(price_corr)` (T22). Doc surface: `docs/baseline/*` (T1), `docs/lineage/MATRIX.md` (T12), `docs/verification/MATRIX.md` (T25), `docs/risks/RESIDUAL.md` (T28), `docs/runtime/EXECUTION_GRAPH.md` (T16). End-to-end Playwright e2e tests in `frontend/tests/e2e/{dashboard,portfolio,trades}.spec.ts` (T25). 227+ backend tests green. |
| 2026-07-22 | **T26 full-suite verification.** Backend: 209 passed with only the two documented `test_brave_client.py` failures left untouched. Frontend production build and `tsc --noEmit` pass. Moved four root-level Playwright specs (accessibility, derivation drawer, lens, research) into the configured `frontend/tests/e2e/` directory, bringing discovery to 24 tests across desktop/mobile. Production E2E run: 4 desktop accessibility tests passed; 8 desktop feature tests failed because production does not yet expose the asserted T11–T23 UI, and all 12 mobile tests could not launch because WebKit is not installed locally. |
| 2026-07-23 | **Q1/Q2 remediation pass (review follow-up).** Closed the substantive gaps a multi-lens review surfaced. **P0:** `q1_agent.verify_citations` now reconciles the cited *value* against the source within tolerance, not just the source key (ADR-0019); real news wired into L5 — `daily_refresh.persist_theme_news` → `theme_news` (migration 018) → `q1_agent._load_recent_headlines`, replacing the hardcoded `news_headlines=[]` (ADR-0020). **P1:** `scripts/backtest_hype.py` — the missing validation spine: Spearman IC of HypeScore vs forward 1/5/20d returns + decay + pooled IC, pure core unit-tested (ADR-0022). **P2:** robust median/MAD momentum replacing the raw z-score (ADR-0021); signed `crowding` label + `signed_corr` preserved alongside the folded hype corr (migration 019, ADR-0021); source-tagged mock data + per-theme `data_source` provenance (migration 020) and `scripts/check_data_integrity.py` R0 fingerprint guard (ADR-0023); `theme_discovery.py` now implements the LDA∩embedding agreement and persists Tier 2/3 to the `discovered_themes` shadow table (migration 021). New ADRs 0019–0023. Backend suite **302 passing** (was 222). Infra still user-owned: apply migrations 018–021 and re-run `daily_refresh` to overwrite the R0 seed rows. |
| 2026-07-23 | **Migrations applied + live pipeline run + Gemini provider.** Applied migrations 018–021 to production (verified: `theme_news`, `discovered_themes`, `theme_signals_history.{signed_corr,crowding,data_source}`, RLS policies). Ran `daily_refresh` against prod: 16 macro series, 33 factor betas, regime late/risk-on, **24 headlines → `theme_news`**, 0 candidates (themes below the 50 threshold — the honest outcome). The new `data_source` label immediately surfaced that **all 8 themes are `mock`** — i.e. both Brave *and* Reddit fell back (previously invisible). Set + validated a real **Brave** key (live headlines returned; open follow-up: Brave returns relative `age` — needs `page_age` ISO so per-day mention counts work). **Reddit dropped** — its API is now approval-gated (Responsible Builder Policy) and its terms restrict commercial/LLM use; Brave-only is the more defensible path. Wired **Google Gemini** as a third L5 provider (`_llm_complete`, priority MiniMax→Anthropic→Gemini, default `gemini-flash-latest`; ADR-0026, migration-free) — live-tested end-to-end (fence-strip + JSON parse) and mock-unit-tested. |
| 2026-07-23 | **L5 produces a VERIFIED $100M book end-to-end (MiniMax-M3) + macro coverage expanded.** Drove the pipeline until a real, citation-verified, non-fallback book persisted to production (`research_recommendations.advisory_derivation.display_status='verified'`, `fallback_used=false`). Fixed the cascade of pre-existing bugs that only surfaced once a real run reached L5 *with candidates*: UTF-8 stdout (a `→` status print crashed the run right before L5), datetime→JSONB serialization on **both** `numeric_derivations` and `advisory_derivation` (writes were silently failing → `run_q1_agent` returned None → "agent declined"), idempotent risk fallback (upsert not insert — a re-run hit the `run_date` unique key), NULL factor betas in `_make_factor_table` (`unsupported format string passed to NoneType`), an explicit `LLM_PROVIDER` selector (an **ambient shell `ANTHROPIC_API_KEY`** was hijacking runs meant for MiniMax/Gemini), MiniMax `max_tokens` 24k + JSON mode + robust fence-strip (M3's reasoning truncated the JSON `content` to empty), per-pick→top-level citation aggregation, and a relaxed "up to 5 each" pick demand (the thin single-theme candidate pool made the model over-reason into empty output). **Citation guardrail** extended to value-grounding + derived-metric grounding (VIX term structure, 2s10s slope, real rate) + a small ungrounded-minority tolerance (**ADR-0027**) — a value the model *computed* from grounded inputs is no longer treated as a hallucination. **Provider default** is now MiniMax (paid tier, no free-tier wall; Gemini free-tier 20-req/day was the wall); `research_agent_runs.model_id` logs the model that actually answered. **Macro (L0) expanded 16→22 series** (`backend/data/macro_fetcher.py`): removed dead FRED codes (`CPALTT01USM`/`TEDRATE`/`DPRRE` — 404/discontinued), added `CPIAUCSL` (CPI), `DFF` (Fed funds → Fed Policy theme), `BAMLC0A0CM` (IG OAS → Corporate Credit theme), `DFII10`/`DGS5`/`DGS30`; corrected HY OAS unit label (bps→pct — FRED reports percent); `macro_indicators` now **replaces** the day's snapshot instead of appending duplicates; `FRED_API_KEY` consolidated into root `.env` (was only in `backend/.env`, so `daily_refresh` silently fetched no FRED). New ADRs 0026 (Gemini), 0027 (value-grounding). Backend suite **323 passing**. |
| 2026-07-23 | **HypeScore normalization consistency (ADR-0028) + attention-density fix.** `hype_score` fed `volume`/`momentum` min-max'd across themes but `corr` as a **raw `abs()`** (the T22 change) — on a noisy short window `\|corr\|`≈0.1–0.4, so the 30% corr weight structurally under-delivered, compressed the distribution, and pinned themes below the 50 threshold (a likely cause of "no positions"). **ADR-0028**: min-max `abs(corr)` across themes like volume/momentum (supersedes T22); keep `sentiment` absolute because its **sign** is information; `persist()` stores the same min-max `corr_score` so display = score (preserves the PROGRESS-42 reproducibility fix). Live effect: themes clearing 50 went **1 → 2**. Diagnosed the honest caveat: `price_corr` was **degenerate (all 0)** because mentions were only 0–3/day, so the min-max lift came from the 0.5 neutral fallback, not a real corr signal. **Root-cause fix**: Brave `count` 20→50 and the theme↔price **correlation window widened 7→30 days** (`CORR_WINDOW_DAYS`; momentum stays on the trailing 7) so the mention series has enough spread to correlate. ADR-0028 filed. Backend suite **324 passing**. |
| 2026-07-23 | **Correlation was dead since day one — date-type alignment bug fixed (the true ADR-0028 root cause).** After ADR-0028's min-max still showed `price_corr` degenerate (all 0), traced it to `backend/data/yahoo_client.correlation_with_mentions`: it intersected an **ISO-string** mention index (`build_theme_signals` keys the series with `.isoformat()[:10]`) with a **`datetime.date`** price index — the intersection is *always* empty → `<5 common dates` → returned `0.0` for **every theme, every run**. So the 30% correlation weight had never contributed. Fixed by coercing both indices to `datetime.date` before aligning (+ regression test with mismatched index types). Correlation is now **live and varying** (China Growth +0.39, US Dollar +0.26, Inflation −0.35, …) and genuinely discriminates: China Growth leads at hype **82.6** on real corr, not the neutral +15. Net result — a **verified long/short book** persisted (`display_status=verified`, `fallback_used=false`): **LONG FXI + SHORT UUP**, a coherent long-China / short-USD pair citing the new `DFF` (Fed funds) and `DFII10` (real yield) series, with measurable counter-theses. Backend suite **325 passing**. |

## Build tasks

| # | Task | Status |
|---|---|---|
| 1 | Project scaffolding: environment, schema, env vars | completed |
| 2 | Python scoring pipeline: sentiment, data fetchers, calculators | completed |
| 3 | Next.js frontend: all 4 pages and components | completed |
| 4 | Deploy to Vercel + Supabase | completed |
| 5 | Phase 1 complete: working frontend reading sample data | completed |
| 6 | Phase 2 complete: daily_refresh.py runs end-to-end via cron | pending (live run succeeded; cron trigger still to be set up) |
| 7 | Theme discovery bootstrap: run LDA + embedding clustering | pending |
| 8 | Phase 3: trade ranking + position sizing + risk engine + daily P&L | completed (2026-07-21) |
| 9 | Phase 4: real-time upgrade (FastAPI + Redis) | deferred |
| 10 | Phase 5: Q1 reasoning pipeline (L0-L6) | completed (L0–L4 + L5 8-node agent + L6 writeup) |
| 11 | Research-first frontend redesign (regime hero, conviction cards, alloc bar, factor panel) | completed (2026-07-21) |
| 12 | Provenance layer: citation footnotes + ThemeDerivationDrawer (L7) | in-progress (L7 components in flight; citation infrastructure done) |
| 13 | Lens mode (ADR-0015) — `lens` param on L5 + `<LensSelector>` UI + migration 009 | completed (2026-07-21) |
| 14 | Provenance columns on existing tables + derivations module (T9/T13/T19) | completed (2026-07-22) |
| 15 | Pipeline runs + since-inception cumulative return (T10/T14, migrations 012/014) | completed (2026-07-22) |
| 16 | L7 status / portfolio / research read-models + TS derivation mirrors (T11/T18/T19) | completed (2026-07-22) |
| 17 | Taxonomy seam `classify(ticker)` + TradeScore `elapsed_days` + `abs(corr)` (T20/T22) | completed (2026-07-22) |
| 18 | E2E Playwright coverage (T25) + verification matrix + residual risk register (T28) | completed (2026-07-22) |
| 19 | Architecture docs sync + ADRs 0016–0018 (T27) | completed (2026-07-22) |
