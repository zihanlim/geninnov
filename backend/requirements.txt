# Core
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.11.0          # backtest_{edge,hype}.py — spearmanr; volatility_models — GARCH MLE
cvxpy>=1.9.1           # backend/services/optimizer.py — L5 position sizing.
                       # ON THE DAILY CRITICAL PATH: without it size_positions falls back
                       # to conviction weighting. Must also be in ci.yml AND
                       # daily-refresh.yml, which pip-install explicit lists rather than
                       # this file.

# HTTP + config
requests>=2.31.0       # backend/data/macro_fetcher.py (L0 critical path), polymarket_fetcher, q1_agent
python-dotenv>=1.0.0   # scripts/check_data_integrity.py, scripts/backtest_hype.py

# Data ingestion
yfinance>=0.2.0
 praw>=7.7.0
 httpx>=0.27.0

# NLP
nltk>=3.8.0
vaderSentiment>=3.3.0

# Theme discovery (optional for bootstrap phase)
sentence-transformers>=3.0.0
umap-learn>=0.5.0
hdbscan>=0.8.0
gensim>=4.3.0

# Supabase
supabase>=2.0.0

# Scheduling
apscheduler>=3.10.0

# Testing / dev
pytest>=8.0.0
pytest-cov>=4.1.0
