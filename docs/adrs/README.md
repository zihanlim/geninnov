# Architecture Decision Records

One ADR per file. Immutable once accepted — superseded ADRs are marked `superseded by ADR-NNNN` rather than rewritten.

## Index

| ID | Title | Status | Date |
|---|---|---|---|
| [0001](0001-supabase-over-postgres.md) | Supabase over self-hosted PostgreSQL | accepted | 2026-07-21 |
| [0002](0002-vercel-for-frontend.md) | Vercel for frontend hosting | accepted | 2026-07-21 |
| [0003](0003-daily-cron-over-fastapi.md) | Daily cron over real-time FastAPI backend | accepted | 2026-07-21 |
| [0004](0004-no-redis-in-phase1.md) | No Redis for caching | accepted | 2026-07-21 |
| [0005](0005-vader-over-paid-sentiment.md) | VADER over paid sentiment API | accepted | 2026-07-21 |
| [0006](0006-minmax-over-zscore-hypescore.md) | Min-max normalization over z-scores for HypeScore | **superseded by [0042](0042-absolute-hype-subscores.md)** | 2026-07-21 |
| [0007](0007-two-method-theme-discovery.md) | Two-method theme discovery (LDA + embedding clustering) | accepted | 2026-07-21 |
| [0008](0008-tier1-macro-anchors-practitioner.md) | Tier 1 macro anchors defined by practitioner judgment | accepted | 2026-07-21 |
| [0009](0009-research-first-design-philosophy.md) | Research-first design philosophy (vs Bloomberg-terminal aesthetic) | accepted | 2026-07-21 |
| [0010](0010-citation-footnotes-everywhere.md) | Citation footnotes on every numeric claim | accepted | 2026-07-21 |
| [0011](0011-theme-derivation-drawer.md) | Theme Derivation Drawer (raw signals → normalized → weighted → final) | accepted | 2026-07-21 |
| [0012](0012-citation-guardrail-llm-defense.md) | Citation guardrail as the primary LLM defense in the Q1 pipeline | accepted | 2026-07-21 |
| [0013](0013-deterministic-stochastic-split.md) | Deterministic L0–L4 / stochastic L5 only (constrained-reasoning pattern) | accepted | 2026-07-21 |
| [0014](0014-candidate-set-hard-filter.md) | Q1 picks hard-filtered to the L5 candidate set (LLM cannot pick outside data) | accepted | 2026-07-21 |
| [0015](0015-lens-mode-asset-class.md) | Lens mode: triggerable asset-class filter on the L5 reasoning agent | accepted | 2026-07-21 |
| [0016](0016-signed-weights-portfolio-accounting.md) | Signed-weights portfolio accounting convention (`direction` derived from sign) | accepted | 2026-07-22 |
| [0017](0017-since-inception-cumulative-performance.md) | Since-inception cumulative performance is compounded daily (`portfolio_cumulative_return`) | accepted | 2026-07-22 |
| [0018](0018-provenance-read-model-seam.md) | Provenance read-model seam between Python derivations and TypeScript status components | accepted | 2026-07-22 |
| [0019](0019-citation-value-reconciliation.md) | Citation guardrail reconciles cited values, not just source keys | accepted | 2026-07-23 |
| [0020](0020-theme-news-store-for-l5.md) | theme_news store feeds the L5 agent real news | accepted | 2026-07-23 |
| [0021](0021-signal-robustness-momentum-crowding.md) | Signal robustness: robust momentum + signed crowding | accepted | 2026-07-23 |
| [0022](0022-hypescore-ic-backtest.md) | HypeScore validation via Information-Coefficient backtest | accepted | 2026-07-23 |
| [0023](0023-data-provenance-and-fabrication-guard.md) | Data provenance labels + fabricated-data (R0) guard | accepted | 2026-07-23 |
| [0024](0024-persist-book-analytics-not-prompt-strings.md) | Persist book analytics as structured records, not prompt strings | accepted | 2026-07-23 |
| [0025](0025-book-centric-information-architecture.md) | Book-centric information architecture (`/book`, `/risk`, `/method`) | accepted | 2026-07-23 |
| [0026](0026-gemini-third-l5-provider.md) | Google Gemini as a third L5 LLM provider | accepted | 2026-07-23 |
| [0027](0027-citation-value-grounding.md) | Citation guardrail grounds on value, not source-label exactness | accepted | 2026-07-23 |
| [0028](0028-minmax-correlation-consistency.md) | Min-max \|corr\| across themes (supersedes T22 raw-abs) | **superseded by [0042](0042-absolute-hype-subscores.md)** | 2026-07-23 |
| [0029](0029-two-sided-book-decouple-direction-revive-momentum.md) | Two-sided book: decouple direction from the hype gate + revive momentum | accepted | 2026-07-23 |
| [0030](0030-unify-l5-candidate-pool-with-l1.md) | Unify the L5 candidate pool with L1 (screen_candidates consumes rank_trade_candidates) | accepted | 2026-07-23 |
| [0031](0031-edge-score-direction-signal.md) | EdgeScore: anchor long/short direction to trend + regime, not sentiment | accepted | 2026-07-23 |
| [0032](0032-edge-carry-value-abstention-sizing.md) | EdgeScore Stages 3–4: Carry, Value, abstention, conviction sizing | accepted | 2026-07-23 |
| [0033](0033-edge-stage5-sentiment-ic-weights.md) | EdgeScore Stage 5: sentiment demotion + IC-fit weights | accepted (IC claim **corrected by [0044](0044-carry-ic-was-measured-on-a-superseded-signal.md)**) | 2026-07-24 |
| [0034](0034-scoring-config-anon-read-policy.md) | Public read policy on scoring_config (frontend sees live weights) | accepted | 2026-07-24 |
| [0035](0035-volume-subscore-7day-average.md) | HypeScore Volume = 7-day average mentions, not the 1-day count | accepted | 2026-07-24 |
| [0036](0036-carry-as-excess-yield-over-funding.md) | Carry = excess yield over funding (two-sided); a missing component renormalises rather than scoring 0 | accepted | 2026-07-24 |
| [0037](0037-position-limits-bind-and-the-rest-is-cash.md) | Position limits actually bind; what they refuse is held in cash, not renormalised away | accepted | 2026-07-24 |
| [0038](0038-per-asset-direction.md) | Direction is a property of the asset, not its theme (the reason the book had no shorts) | accepted | 2026-07-24 |
| [0039](0039-scope-by-attention-abstain-by-asset.md) | Scope is chosen by attention; abstention is decided per asset (completes ADR-0038) | accepted | 2026-07-24 |
| [0040](0040-published-book-is-the-book-of-record.md) | The published book is the book of record (positions/returns/risk recomputed on L5 output) | accepted | 2026-07-24 |
| [0041](0041-regime-as-a-dial-not-a-cliff.md) | The regime is a dial, not a cliff — continuous risk appetite drives direction, the label is display only | accepted | 2026-07-24 |
| [0042](0042-absolute-hype-subscores.md) | HypeScore sub-scores are absolute, not relative to the day's peer group (supersedes 0006, 0028) | accepted | 2026-07-24 |
| [0043](0043-single-company-universe.md) | Single companies enter the universe (only meaningful once direction became per-asset) | accepted | 2026-07-24 |
| [0044](0044-carry-ic-was-measured-on-a-superseded-signal.md) | The carry IC justifying its 0.34 weight was measured on a superseded signal — corrects 0033 | accepted | 2026-07-24 |
| [0045](0045-turnover-on-names-without-a-verdict.md) | Turnover is measured on names without a verdict; candidate overlap is signed by both directions before thresholding | accepted | 2026-07-24 |
| [0046](0046-attention-chooses-what-we-look-at-not-what-is-tradable.md) | Attention chooses what we look at; a sub-attention theme holding a decisive name is admitted anyway | accepted | 2026-07-24 |
| [0047](0047-conviction-needs-a-vol-floor.md) | Conviction needs a vol floor, absolute rather than a percentile | accepted (**scope corrected by [0053](0053-the-published-book-was-sized-by-hype.md)**) | 2026-07-25 |
| [0048](0048-count-independent-ideas-not-candidates.md) | Count independent ideas, not candidates — the pool holds 13 long and 5 short, so five-and-five is no longer a universe problem | accepted | 2026-07-25 |
| [0049](0049-the-guardrail-does-not-read-the-prose.md) | The citation guardrail never inspects the thesis prose, and grounding cannot verify a count — every integer 0-9 grounds | accepted | 2026-07-25 |
| [0050](0050-separate-agent-churn-from-market-churn.md) | Measure agent churn separately from market churn — replicate reason_picks on frozen inputs | accepted | 2026-07-25 |
| [0051](0051-llm-timeout-bounded-silence-not-the-call.md) | LLM_TIMEOUT_SECONDS bounded the gap between bytes, not the call | accepted (**cause corrected by [0052](0052-a-stall-cost-three-attempts-not-one.md)**) | 2026-07-25 |
| [0052](0052-a-stall-cost-three-attempts-not-one.md) | A stall cost three attempts, not one — reason_picks retried timeouts; corrects 0051 | accepted | 2026-07-25 |
| [0054](0054-a-daily-publication-not-a-scanner.md) | The interface is a daily research publication, not a scanner — no streaming, no alerts, no filters; but the scannable layer must differentiate | accepted | 2026-07-25 |
| [0055](0055-an-acceptance-battery-for-the-model-that-writes-the-book.md) | An acceptance battery for the model that writes the book — frozen fixtures with a written-down right answer; structural checks gate, directional ones report | accepted | 2026-07-25 |
| [0056](0056-an-instruction-is-not-a-guardrail.md) | An instruction is not a guardrail — measure whether the thesis explains the independent ideas it declined, and let the panel say which case it is | accepted | 2026-07-25 |
| [0057](0057-stability-is-a-per-trade-fact-not-a-percentage.md) | Stability is a per-trade fact, not a book-level percentage — mark which positions survived every rerun | accepted | 2026-07-25 |
| [0058](0058-explanations-are-owed-per-empty-slot.md) | Explanations are owed per EMPTY SLOT, not per declined idea — corrects 0056; the frozen-input harness also confirmed the prompt works (ARKK named 3/3) | accepted | 2026-07-25 |
| [0059](0059-a-single-date-ic-is-not-validation.md) | A single-date IC is measured, not validated — the IC harness crashed before persist, and the panel would have flipped green on one day | accepted | 2026-07-25 |
| [0060](0060-a-share-cannot-exceed-the-whole.md) | A share cannot exceed the whole — net share is withheld on a market-neutral book instead of rendering ±1000% | accepted | 2026-07-25 |
| [0061](0061-a-false-excuse-is-worse-than-none.md) | A false excuse is worse than none — reject an availability claim about a name that was in the pool; ADR-0049's rule extended from numbers to facts | accepted | 2026-07-25 |
| [0062](0062-run-date-is-not-a-write-timestamp.md) | A run's date is its run_date, not a write timestamp — the landing page dated the 07-25 run 07-24 by reading themes.updated_at | accepted (**description corrected by [0070](0070-forward-dating-was-never-implemented.md)**) | 2026-07-25 |
| [0063](0063-one-beta-bar-across-every-surface.md) | One sample bar for beta across every surface — and the recorded beta-direction guardrail is NOT built, because the claim is ambiguous rather than false | accepted | 2026-07-25 |
| [0064](0064-the-audit-page-blamed-the-pipeline-for-its-own-arithmetic.md) | The audit page blamed the pipeline for its own arithmetic — /method recomputed EdgeScore without ADR-0036's renormalisation and published a RECONCILIATION FAILURE against a correct pipeline | accepted | 2026-07-25 |
| [0065](0065-check-the-published-book-not-only-the-generation.md) | Check the published book, not only the generation that produced it — the thesis guardrails now re-run daily against the persisted book_view | accepted | 2026-07-25 |
| [0066](0066-not-computable-must-persist-as-null.md) | "Not computable" must persist as NULL, not 0.0 — eight of nine positions had a renormalised score beside components written as zero | accepted | 2026-07-25 |
| [0067](0067-a-column-must-name-the-subset-it-measures.md) | A column must name the subset it measures — "Avg |rho| to book" averaged only pairs flagged at 0.70, so an empty column read as uncorrelated | accepted | 2026-07-25 |
| [0068](0068-a-cap-breach-is-not-decided-by-float-error.md) | A cap breach is not decided by floating-point error — a book clamped exactly to its 35% cap reported "US (35.0% > 35%)" | accepted | 2026-07-25 |
| [0069](0069-run-date-is-utc-not-the-local-clock.md) | `run_date` is UTC, not the local clock — a UTC+8 local run stamped a day ahead of the scheduled job, splitting positions from the published book | accepted | 2026-07-25 |
| [0070](0070-forward-dating-was-never-implemented.md) | Forward-dating was never implemented — the convention in 0062 was inferred from an artifact of a UTC+8 local run; corrects 0062's description | accepted | 2026-07-25 |
| [0071](0071-pool-metrics-are-not-book-metrics.md) | Pool metrics are not book metrics — the prompt labelled equal-weighted pre-selection pool figures "BOOK METRICS", and the thesis published a 31.67pp cap breach the book does not have | accepted | 2026-07-25 |
| [0072](0072-persist-the-books-correlation-structure.md) | Persist the book's correlation structure, not just its flagged tail — an empty flagged list left every surface saying only "nothing crossed 0.70" | accepted | 2026-07-25 |
| [0073](0073-a-factor-tilt-is-a-number-the-model-may-not-retype.md) | A factor tilt is a number the model may not re-type — the thesis called a book at Mkt −0.50 "market-neutral (Mkt -0.02)", restating the pre-selection pool | accepted | 2026-07-25 |
| [0074](0074-stress-both-tails-not-just-the-crash.md) | Stress both tails, not just the crash — all four scenarios were risk-off, so a net-short book's worst case read −0.0%; added a risk-on melt-up | accepted | 2026-07-25 |
| [0075](0075-per-pick-betas-are-joined-not-authored.md) | Per-pick betas are joined, and a regime shape is computed, not authored — all ten positions carried the same copied tilts, and a +34bps curve printed as "0.34 bps" became "an inverted curve" | accepted | 2026-07-25 |
| [0076](0076-a-guard-reports-every-failure.md) | A guard reports every failure, not the first one — three defects sat in one thesis and surfaced one per run because main() returned at the first | accepted | 2026-07-25 |
| [0077](0077-withhold-the-number-instead-of-forbidding-it.md) | Withhold the number instead of forbidding its use — the prompt block relabelled by ADR-0071 prepended its own `=== BOOK METRICS ===` header two lines below it | accepted | 2026-07-25 |
| [0078](0078-a-disqualifier-you-cannot-locate.md) | A disqualifier you cannot locate is not falsifiable — six of ten counter-theses named the 200-day MA as the trigger and nothing said what it was | accepted | 2026-07-25 |
| [0079](0079-adr-0078-described-a-book-that-had-been-replaced.md) | ADR-0078 described a book that had already been replaced — the evidence was read before a re-run launched in the same iteration, and written down after | accepted | 2026-07-25 |
| [0080](0080-a-gate-that-collects-nothing-is-not-a-gate.md) | A gate that collects nothing is not a gate — `pytest scripts/ \|\| true` collected zero of 44 test files and reported green; pytest becomes a gate, ruff/mypy stay reports at 217 errors | accepted | 2026-07-25 |
| [0081](0081-step-numbered-lineage-panel-on-book.md) | Worked-example lineage panel on /book (additive only): numbered StepNumbered primitive + a <details>-collapsed panel that surfaces one position's ingestion → theme → sizing → risk trail, with —  Source not yet persisted instead of fabricated citations | accepted | 2026-07-25 |
| [0082](0082-euler-risk-decomposition-on-the-final-book.md) | The book's risk decomposes by name, and a hedge is allowed to be negative — Euler decomposition on the final book, where a negative contribution is a hedge rather than a slice to clamp | accepted (backend live; render pending) | 2026-07-25 |
| [0083](0083-a-lineage-step-must-be-able-to-carry-its-own-proof.md) | A lineage step must be able to carry its own proof — one `reconcile()` verdict shared by /method and /book, three-state so "not yet scored" is never reported as a reconciliation failure, tolerances named per scale; corrects ADR-0081's Kelly-criterion sizing formula, which came from the external mockup and is not the model | accepted | 2026-07-25 |
| [0084](0084-method-splits-by-reader-question-not-by-copy.md) | /method splits by reader question, not by copy — 14.5 screens became `/method` (how a number is built) + `/method/evidence` (did it run, who checked it), sharing one `MethodBody` and one fetch so two chapters cannot disagree about a vintage; anchors hop client-side because a URL fragment never reaches the server; argues down the "four pages" non-goal and amends `design-goals.md` in the same change | accepted | 2026-07-26 |
| [0085](0085-direction-cannot-be-carried-by-hue-alone.md) | Direction cannot be carried by hue alone — goal 3's desaturation test has never passed and cannot at AA (`--long` vs `--short` is 1.29:1 desaturated, `--long` vs `--accent` 1.03:1, and any two inks clearing 4.5:1 on white are at most ~3.3:1 apart); direction becomes glyph + wordmark with hue as reinforcement, `--brand` is deleted as a duplicate of `--short`, and enforcement is scoped to the enumerated chip vocabularies with a widening clause | accepted | 2026-07-26 |
| [0086](0086-a-labelled-rail-that-collapses-rather-than-a-glyph-rail.md) | A labelled rail that collapses, rather than a glyph rail — collapsed 56px is the widest rail that preserves the two-pane layout (two panes need 1,304px, 91% of a 1,425px viewport), expanded 200px is the reader's explicit trade; amends the sidebar non-goal, which only ever objected to UNLABELLED glyph rails | accepted | 2026-07-26 |
| [0087](0087-a-chat-that-cannot-do-arithmetic.md) | A chat that cannot do arithmetic — `/ask` plans, fetches with the pages' OWN deterministic functions, then writes prose whose every numeral is adjudicated cited / quoted / unverified. Two LLM calls per question, no tool loop, no streaming (a guardrail cannot check a token already on screen). Adds the repo's first route handler and a spend guard that fails closed; amends design goal 5 and the four-destinations non-goal | accepted | 2026-07-26 |
| [0088](0088-a-stress-scenario-that-does-not-transmit-through-market-beta.md) | A stress scenario that does not transmit through market beta — S1-S5 are all a market shock scaled by a factor beta, so a position the factor model cannot see is invisible to the whole battery: on the live 2026-07-25 book NOC (`beta_mkt` −0.014, a 4.7% short whose thesis is geopolitical) had a 9bp worst case across all five, and 23.6% of gross was effectively unstressed. `S6_supply_shock` transmits through `SECTOR_MAP` instead and is inflationary, so duration stops hedging; it scores +1.48% and does **not** become the worst case, but it surfaces the book's short-gold-plus-short-defense position for the first time | accepted | 2026-07-26 |
| [0089](0089-a-citation-the-reader-can-follow.md) | A citation the reader can follow — the theme drawer rendered the headlines behind a HypeScore as plain, unclickable text, which is goal 1's failure shape exactly: it looks like provenance and terminates in a dead end. The link was never missing from the pipeline, it was discarded — both fetchers return one and `build_theme_signals` rebuilt each item without it. Migration 042 adds `theme_news.url`; the same pass starts writing `sentiment`, declared since migration 018 and never written. No backfill (the responses were never stored, and reconstructing a URL would manufacture the provenance the column exists to fix), so pre-042 rows render as plain text with a stated cause and mock rows persist NULL rather than a placeholder | accepted | 2026-07-26 |
| [0090](0090-a-published-pick-must-be-falsifiable.md) | A published pick must be falsifiable — every validation surface in the repo looks BACKWARD at signals (IC backtests, replication, eval battery); nothing scored the books actually published. The blocker was that a pick carried no testable claim: `time_horizon` is LLM-authored free text with two fuzzy values, and it moved for the same position (NOC short was "2-4 weeks" on 07-24 and "1-3 months" on 07-25) — a model choosing its own horizon grades its own exam. `pick_outcomes` (m043) + a pipeline-assigned 21-trading-day spec, matched to `backtest_edge`'s forward 1-month window so /method and the scorecard cannot disagree about what "works". Rows are written PENDING at publication so the denominator is fixed before any outcome is known; `void` requires a reason and `hit` requires its arithmetic, both by CHECK constraint. No Brier score — conviction is a sizing input, not a calibrated probability. Fixed while all 23 claims across 4 books were still unresolvable (first maturity ~2026-08-20) | accepted | 2026-07-26 |
| [0091](0091-breadth-must-be-a-share-of-something-named.md) | Breadth must be a share of something named — `spx_breadth` was a boolean wearing a percentage (SPY above its own MA → 65.0, else 35.0), and because the sentiment rules are `<40` / `>60`, it fired one of them on every resolving day and made `neutral` unreachable: a four-input model that was a one-bit switch. Now the share of the 11 GICS sector SPDRs above their own 200d MA. Published rows are NOT restated — their books cite breadth | accepted | 2026-07-26 |
| [0092](0092-the-book-as-an-mcp-server-over-the-tools-that-already-exist.md) | The book as an MCP server, over the tools that already exist — ADR-0087 built read-only tools that import the SAME modules the pages render; they were reachable only through our own LLM. `POST /api/mcp` (Streamable HTTP, `2025-06-18`, stateless, POST-only, `Origin`-validated) exposes that identical registry, and `/llms.txt` indexes the site with the tool list GENERATED from `TOOLS` so it cannot drift. No rate limit, deliberately: the caller brings their own model, so unlike `/ask` there is no MiniMax quota to protect. Every tool declares an `outputSchema` that admits the absence case, and an unknown tool is a JSON-RPC `-32602` rather than a result with `isError`. Exporting `displayFact` was forced by a real bug — the first renderer appended `unit` as a suffix and emitted "10 count" and "0.593 pct" | accepted | 2026-07-26 |
| [0093](0093-a-published-book-that-changes-must-say-so.md) | A published book that changes must say so — `research_recommendations` is upserted on `run_date`, so a second run REPLACES the published book in place with no prior version anywhere. Not hypothetical: `research_agent_runs` held **17 runs for 2026-07-25** and **25 for 2026-07-24**, 70 across four books, and they disagreed — the 04:24 run produced the live book while the latest at 09:04 proposed a different one and was refused by the guardrail (`verified = false`). `book_revisions` (m044) records per-field what changed, with `reason` **NOT NULL** because an unexplained revision reads exactly like the silent overwrite it exists to prevent. Materiality is one basis point (economic, not a representation guard); appearance/disappearance is always material. First entry is a real correction — this session's S6 append | accepted | 2026-07-26 |
| [0094](0094-a-benchmark-the-book-can-be-measured-against.md) | A benchmark the book can be measured against — /risk drew a curve and a drawdown and never answered "versus what?". `macro_daily_history` already stored `^SPX` as a daily level (253 obs) while `_load_spx_returns` separately fetched `^GSPC` at runtime for beta and discarded it, so the pipeline paid for the series twice and kept neither in a readable shape. Migration 045 persists it, differenced and compounded from the BOOK's inception so both curves share an origin. The second path is **dashed in the same ink** with a pinned end-label, not a second hue (ADR-0085), and the comparison is **gated at 60 usable observations** — today there is **1**, so the chart states the count rather than drawing noise against noise. The book's own curve is NOT gated: deleting a correct chart to avoid a comparison we declined to draw punishes the reader for our caution | accepted | 2026-07-26 |
| [0094](0094-no-corroboration-gate-over-a-single-source.md) | No corroboration gate over a single source; disclose the count instead — the worldmonitor review proposed promoting a theme only once N independent source TYPES agree. Measured first: `theme_news` holds **1,005 rows, all `source = 'brave'`**. Reddit is fetched and tagged but `REDDIT_CLIENT_ID` is not a repo secret, so `reddit_client` returns `[]` rather than fabricating (ADR-0023 working). So HypeScore's attention rests on **one provider**, while four surfaces claimed "Brave News + Reddit" / "live Brave/Reddit responses". A gate at one source either always passes or never passes, so it was refused; `sourceIndependence()` derives and discloses the count instead, and the four overstatements are corrected | accepted | 2026-07-26 |
| [0095](0095-s6-scales-by-measured-disruption-when-there-is-a-reading.md) | S6 scales by measured disruption when there is a reading — ADR-0088's sector map is a constant, so the stress table read the same whether Hormuz was quiet or closed. worldmonitor's REST API is gated but its **MCP endpoint answers `initialize`/`tools/list` unauthenticated**, which is how the contract was read; `tools/call` needs a key (`-32001`), and the cheapest is **Pro at $39.99/mo**, not the $99.99 API tier. Because the tool declares an `outputSchema` the adapter is built against a documented contract, not a guess. Scales 0.5x-1.5x around a 40% baseline (magnitude only, never direction; worst chokepoint binds, not the average); every refusal returns the neutral multiplier WITH a reason. Live path implemented but unverified — no credential | accepted | 2026-07-26 |
| [0096](0096-the-book-is-net-short-sanctions-risk-and-never-said-so.md) | The book is net short sanctions risk, and never said so — the 2026-07-25 book held short BABA 8.7% + short PDD 9.25%, i.e. **30.2% of gross** in US-listed Chinese ADRs (HFCAA, Entity List, outbound-investment rules). Held SHORT, so escalation is a **tailwind** — the opposite of what a China-heavy list suggests. Mirror image of ADR-0088's short-GDX-plus-short-NOC finding: an unlabelled geopolitical-risk position that fell out of picks made on other grounds. Needs **no credential** — jurisdiction plus side is ours; the external pressure score would only add whether pressure is RISING. Three states (exposed / checked-clear / unclassified = "unknown, not absent"); gross is not netted away; a unit conflation is corrected (17.9pp of weight vs 30.2% of gross). Rendered on `/risk` from the persisted assessment; backfilled onto the live row and logged in `book_revisions` | accepted | 2026-07-26 |
| [0097](0097-external-positioning-can-see-a-fifth-of-the-book.md) | External positioning can see a fifth of the book, and that is the finding — every crowding measure was **internal** (HypeScore counts our own corpus; AttentionCrowding ranks our own themes), so none could answer *is everyone else already in this trade?*. CFTC Commitments of Traders answers it and is **free and keyless**, unlike the $39.99/mo worldmonitor feeds. **Coverage is the headline:** exactly 2 of 10 live positions map to a contract (22.2% of gross), `crowded_share <= coverage_share` by construction, and all 8 unmapped positions are stored **with a reason**. Miners are NOT mapped to the metal and sector ETFs NOT to the commodity — that would have lifted coverage to ~35% and is refused because manufactured coverage inflates the number a reader trusts. **SVXY is inverse:** long SVXY is short VIX, and without the flip the verdict is exactly backwards — a wrong *side*, unlike a wrong *number*, has no symptom, so a test asserts the two comparisons disagree. Third finding this week whose substance was a direction nobody stated. Agreeing with a crowd is disclosure, never a signal. The Tue-observation / Fri-publication lag is carried as an *observation* date. ETF flows were asked for and are **not** here: `get_shares_full()` returns zero rows for every ETF tested, and volume is turnover not flow | accepted | 2026-07-26 |
| [0098](0098-an-absence-must-say-which-kind-of-absence-it-is.md) | An absence must say which kind of absence it is — design goal 2 was honoured case by case and re-argued each time, because `display_status="unavailable"` plus free text covered *we tried and could not*, *the question does not apply*, and *we never looked*. Those ask different things of a reader ("come back" vs "stop waiting"). Adds an `epistemic` axis (`known`/`unknown`/`not_applicable`) **independent of presentation**, defaulting to `known` — safe ONLY because a known absence is rejected, so forgetting the field is impossible on the dangerous path. Both absences require a reason; the fallback lands on the **weaker** claim. Timestamp **roles become types** (Observed / Published / Retrieved / Effective, isinstance-checked), because measuring CFTC freshness from retrieval reports a five-day-old reading as two days old. The TS mirror is now **enforced** by a parity test with a non-vacuity check rather than asserted by a comment. A validation rule that could not fire was removed rather than kept for symmetry. Scope stated: the type, not yet the sweep; `AdvisoryDerivation` untouched | accepted | 2026-07-26 |
| [0099](0099-a-capability-with-no-caller-is-not-implemented.md) | A capability with no caller is not implemented — ADR-0095 built `fetch_signal`, `run_scenario_analysis_with_scenarios` and the `scenario_results_to_dict` scenarios parameter, and **none had a production caller**. 17 unit tests passed because each component was right in isolation; `/risk` rendered S6 with no statement of which calibration ran. "Implemented but unverified" undersold it — even WITH a credential nothing would have called it. Wires all three, fetches once per run and reuses it, never returns None or a bare 1.0, and pins the **path** rather than the parts (the key test asserts that omitting the scenarios argument LOSES the reason). Published row backfilled by **append**, logged per ADR-0093, verified byte-for-byte against the wired pipeline's own output, idempotent. Rule: when work ships behind a credential we lack, the reachable half — is it called, does its output reach a surface — is verifiable today and must be | accepted | 2026-07-27 |
| [0100](0100-a-guard-that-lives-in-a-component-guards-one-consumer.md) | A guard that lives in a component guards one consumer — the "withhold a statistic below its declared minimum sample" rule existed three times (Python, risk board, metric tiles) synced by a comment, and **zero** times in `lib/chat/tools.ts`, which feeds `/ask` and the MCP server. With **three** rows in `portfolio_returns`, a **Sharpe of 6.32 against a minimum of 60** was quotable as a cited fact by an external model while the `/risk` tile suppressed the same number. Moves the rule to one module keyed by `portfolio_risk` column; the chat tool withholds and NAMES the shortfall, telling the model to say "not published at this sample size" rather than "unavailable". An unknown sample does NOT withhold — a read error must not black out every figure. Thresholds now checked by parsing the Python. Rule: **a rule enforced at a render site is not enforced** | accepted | 2026-07-27 |
| [0101](0101-delete-the-unwired-duplicate-rather-than-wiring-it.md) | Delete the unwired duplicate rather than wiring it — `services/exposure.py` had no caller but its own test, while ARCHITECTURE drew an edge feeding the trade ranker and MATRIX named it the producer of three `/portfolio` figures (a retired page). It was also **wrong for this repo's data**: fixtures use signed `weight`, real picks use unsigned `weight` + `direction`, so `net_exposure` returns GROSS and the concentration functions KeyError. Five tests passed against a fiction. `book_metrics` already computes all five concepts correctly on the live path, so wiring it would have created a duplicate that starts out wrong. **Rule: wire when it is the only implementation; delete when a live one exists — the tie-breaker is which one real data flows through** | accepted | 2026-07-27 |
| [0102](0102-ask-opens-in-context-rather-than-living-as-a-ribbon.md) | `/ask` opens in context, rather than living as a ribbon — the question was whether the "ask the book" AI should be a ribbon over every page. It should not: an always-open chat panel is the chat-first model [ADR-0054](0054-a-daily-publication-not-a-scanner.md) rejects, and functionally becomes the fifth destination the four-destination nav rule prevents. But the current separate `/ask` page is **context-blind** — asking "why is this the size?" from a position row means leaving the row and retyping the name. Proposal: keep Ask an invoked control, but on pages with a focusable subject open a lightweight, dismissible, **non-streaming** (citation-guardrail-preserving) overlay pre-seeded with the current route + focused position/theme, with an "open full session" deep-link to `/ask` (which stays home for long transcripts, goal 7). The fix for context-blindness is context-awareness, not a ribbon. Owned by the other session's chat workstream — proposal, not a change. **Rule: the chat is a way of reading the page, so it should open from the page** | proposed | 2026-07-27 |
| [0103](0103-the-themes-page-is-a-terminal.md) | The Themes page is a terminal: viewport-locked panes, not a scroll — `/` measured **4,186px, 4.7 screens** on the live deployment, with one dataset rendered **three times** (~1,200px: heatmap, then "Top 3 by attention" cards, then "Watchlist momentum" bars) and **2,113px of always-on evidence** on the only page in the app with no progressive disclosure. The recommended fix was to de-duplicate inside the existing layout; the user chose the terminal, with the costs stated in advance. Reverses **design goal 7's inner-scroller ban** and **ADR-0009's Bloomberg-terminal non-goal**, for `/` only. Does **not** touch [ADR-0054](0054-a-daily-publication-not-a-scanner.md) — no query composition, no filters, no polling, no streaming; the terminal is a layout, not an interaction model. Four constraints bound it: locked at `≥1024px` only (mobile still scrolls), every pane `id`-addressable (deep links were half the cost), duplicates **deleted** not relocated, and the **Ledger palette unchanged** — a terminal on warm paper is still warm paper. Accepted cost with no full mitigation: **Ctrl+F cannot reach text scrolled out of a pane**. **Rule: a viewport lock needs a measurement and an ADR, never "for consistency"** | accepted | 2026-07-27 |
| [0104](0104-a-live-news-panel-that-cannot-be-cited-and-says-so.md) | A live news panel that cannot be cited, and says so — **amends ADR-0105**, which refused it. Requested, argued twice, reaffirmed. The deciding objection was never the non-goals but that **a broadcast is the one thing here a reader cannot trace**; answered by DECLARING the exception in the panel, open or closed, and pinning that by test. Cadence answered in place (the book's run_date renders beside the stream). Nothing loads until opened — **measured at 0 requests to Google before open, 62 after**. All nine channel IDs verified against youtube.com; the first Al Arabiya ID was a channel that does not exist and is now pinned as a negative. `/ask`, MCP and the guardrail untouched | accepted | 2026-07-27 |
| [0105](0105-most-of-our-tables-record-when-we-asked-not-when-it-happened.md) | Most of our tables record when we asked, not when it happened — a worldmonitor widget review rejected the live news video panel, 3D globe, finance radar and Country Instability Index (four fail the triage at step 1; the video panel because **a stream is the one element a reader could not trace**). The surviving idea, a source freshness board, revealed that `macro_indicators` has **no observation column at all** and `factor_exposures` only pipeline-assigned dates, so for 2 of 6 sources "how old is this reading" has no answer in our schema. `age-unknowable` becomes a first-class verdict, every age names the role it was measured from, and five states stay distinct: current / stale / silent (asked, got nothing) / unconfigured (never asked) / age-unknowable | accepted | 2026-07-27 |
| [0106](0106-the-themes-page-is-a-grid-that-scrolls.md) | The Themes page is a grid that scrolls — reverses 0103's viewport lock and per-pane scrollers. Fractional grid rows divide a fixed height between panes, so every pane must clip what does not fit: measured at 1920 the regime pane rendered **"FACTOR TILT OF BOO"** and printed five factor betas as **"+0"/"-0"** (real values −0.50, +0.22, +0.32, +0.43, −0.38) behind a vertical AND a horizontal scrollbar, with six scroll contexts on one screen. A tilt of −0.50 shown as −0 is a different number, not a compressed one. Rows become `auto`, TerminalPane owns no scroller, the macro regime returns to a full-width banner, and live news becomes a top-right floating dock that still makes no third-party request while closed. Pane ids and aria-labels are retained | accepted | 2026-07-26 |
| [0107](0107-the-optimizer-sizes-what-l5-chose.md) | The optimizer sizes what L5 chose — a constrained mean-variance solve replaces the fixed-point cap-clamping loop, with the sector/geo/single-name limits as **solver constraints** rather than post-hoc clamps. L5 still picks the names and sides; `s_i·w_i >= 0` pins direction, which also keeps the problem linear. **Reimplemented, not lifted**: im-Jarvis's `_quantise_weights` clips every weight at zero regardless of its own `long_only` flag and renormalises to sum 1, which on a long-short book deletes every short and returns a fully-invested long-only book that looks valid. `gross <= 1` replaces `sum(w) == 1` so ADR-0037's cash survives. Conviction sizing remains as fallback AND as the measured baseline. On the live book: vol 8.39%→7.44%, expected return +0.675%→+0.728%, return/vol 0.080→0.098 — but SVXY quadruples to 16.1%, which is mean-variance error-maximisation showing up on the first live input | accepted | 2026-07-27 |
| [0108](0108-expected-returns-are-constructed-not-assumed.md) | Expected returns are constructed from a measured IC, not assumed — EdgeScore is a ranking signal in [−1,1], not a return forecast, and handing it to an optimizer as μ is a units error no test on the optimizer could catch. Grinold–Kahn `μ = IC × σ × z`, with IC read from `backtest_results` and **never defaulted**; blend weights renormalise over the components actually measured (3 of 5); shrunk 50% toward zero per ADR-0033, so a weak signal shrinks the book automatically. z is **scaled, not demeaned** — L5 already chose the sides, and demeaning would give a below-average long a negative μ. A non-positive composite yields no μ at all rather than a flipped one | accepted | 2026-07-27 |
| [0109](0109-what-was-read-across-from-im-jarvis-and-what-was-not.md) | What was read across from im-Jarvis and what was deliberately not — 10 modules ported and wired, 4 skipped as duplicates of live implementations (ADR-0101's rule), and **22 named as not-ported with the specific data each would need**: no CUSIPs so no bond maths, no private assets, no trade blotter, no orders. Float not `Decimal` (this repo routes nothing and its columns are `REAL`); 252 not 260. The index a future author should read before assuming some im-Jarvis calculation is available here | accepted | 2026-07-27 |
| [0110](0110-crowding-caps-what-it-can-see.md) | Crowding caps what it can see, and says how little that is — **supersedes ADR-0097's "never feeds sizing" clause and only that clause**. A position whose *effective* side agrees with a speculator extreme has its single-name limit tightened to 50%; every other name is **absent from the cap map** and keeps the base cap, bit-for-bit (pinned by test). A cap, not a μ haircut: crowding says the exit is narrow, which supports holding *less*, not a claim about returns — and a cap is visible in `binding_constraints` where a μ adjustment would leave a smaller weight unattributable. Both sizers apply the same map (ADR-0053). The COT fetch moves ahead of sizing so one reading both sizes and explains (ADR-0099). **The bias is named, not designed away**: penalising only the observable advantages the unobservable, so `coverage_share` is persisted every run to make the drift watchable. On the live book **nothing is tightened** — coverage 22.2%, both observable positions mid-range — and that is published as the result | accepted | 2026-07-27 |
| [0111](0111-mu-is-the-return-input-and-what-it-is-not.md) | μ is the return input, and here is plainly what it is not — `optimizer_result.mu` is the only per-name number denominated in expected return, but its cross-sectional content is `sign × \|EdgeScore\| × σ`: it converts a rank into return units and adds no view about how far a name can travel. Bar 1's fourth input is satisfied **in units, not in substance**. Three proxies rejected (an LLM-emitted target with no source to check it against, the S5 melt-up shocks which are authored constants, and `pct_from_ma` which measures distance-to-disqualifier and is backwards for this) — a proxy that makes a gap look closed is worse than the gap | accepted | 2026-07-27 |
| [0112](0112-a-backtest-of-these-weights-is-not-a-track-record.md) | A backtest of these weights is not a track record — answers "should we backfill the books so we have ex-ante?" with a correction and a narrower build. **Ex-ante needs no backfill**: it borrows history from the CONSTITUENTS and is live on a three-day-old book. What backfilling would buy is the *realised* figures, and `backfill_regime.py` already refuses that — a simulated book written to `research_recommendations` fabricates the audit trail of a product whose claim is an auditable trail. What ex-ante genuinely cannot give is the PATH — drawdown depth, downside asymmetry, behaviour on days the market falls — so the published weights are held fixed over 252 days of constituent returns in their own column (migration 048). Caveats are STORED strings and a literal `is_track_record: false`, because the column is reachable through MCP where page copy does not travel; the panel renders them ABOVE the numbers. An unpriced holding is excluded, not zeroed; a short window returns no statistics at all rather than nulls that read as measured | accepted | 2026-07-27 |
| [0113](0113-the-mark-is-the-artwork-and-the-plate-is-fenced.md) | The mark is the artwork, and its navy is fenced to the mark — the supplied logo replaces the placeholder gradient `A` tile, traced to one `<path>` in `lib/brand.ts` and rendered by three things that cannot all import it (a favicon has no module graph, a touch icon has to be a raster), so a drift test asserts the icon's path is byte-identical to `MARK_PATH`. **Completes ADR-0085 §3**: `--brand`/`--brand-dim` were deleted there and survived in exactly one call site — this tile — and go now. `--logo-plate: #161b38` replaces them, a token only so the palette test can see the one navy in the project, and fenced to the mark: not a surface, not a chip, not an ink, and no utility class consumes it. 26px not 22px, because a thin-limbed silhouette blobs at the size a letterform survived. The home link gains an unconditional `aria-label` — below 640px it was a coloured square with no accessible name | accepted | 2026-07-27 |
| [0114](0114-the-domain-knowledge-was-already-here.md) | The domain knowledge was already here, and one of its numbers had the sign backwards — bar 3 reads as "buy domain expertise"; what it surfaced is that the repo already encodes per-name judgement (reviewed scenario shocks, the COT no-contract rationales, measured factor loadings, sanctions channels) and never routed it to the prompt. `position_dossier` assembles it and **adds nothing** — a section inviting the model to supply domain knowledge would arrive without a source key, so the guardrail could not adjudicate it. Assembling it immediately exposed **`S1_vix_spike` paying `SVXY: +0.20` ("short-VIX benefit")** on a −0.5x INVERSE VIX product: measured `beta_mkt +2.08`, and S1's own −18% market shock implies ≈−37%, a 57-point error on what was the **second-largest live position**. S4 had it too; S6 had it right. Corrected, and guarded by PROPERTY — short-vol must lose in every risk-off scenario. The contradiction was always in the file; reading it by scenario hid it | accepted | 2026-07-27 |
| [0115](0115-a-complex-is-one-idea.md) | A correlation complex is one idea, so it gets one name's worth of capital — ADR-0048 has COUNTED complexes since it shipped and nothing ever CONSTRAINED them, so the single-name cap was trivially evadable: three names at rho 0.9 could hold 60% of gross while each reported headroom. Live book: ten tickers collapse to three long ideas, and the largest spans FOUR themes. Started from "which member do we choose?" — the existing rule is `max(|edge_score|)`, which picked **SVXY** (−0.5x inverse VIX, daily-rebalanced, sibling XIV terminated 2018) to express long equity beta over QQQ and IWM, at 10.79% of the book. **Answer: do not choose.** If the members are one idea, every selection adds an unforced idiosyncratic bet; cap the complex and let the covariance split it. Reuses the sector/geo group-cap machinery as a third grouping. An instrument-integrity screen was drafted and rejected — it makes the choice better instead of removing the need to choose | accepted | 2026-07-27 |
| [0116](0116-the-optimizer-chooses-the-instrument-the-thesis-argues-the-idea.md) | The optimizer chooses the instrument; the thesis argues the idea — ADR-0115 said "cap the complex and let the covariance split it" without measuring it, and it does not split: mu is `sign x |EdgeScore| x sigma` (ADR-0111) and inside a near-singular complex the linear term decides, so a **5% mu difference produces a 100/0/0 corner**. The cap bounded the exposure while the instrument CHOICE stayed `max(|EdgeScore|)`, relocated into the solver where it is invisible. Widening the menu alone makes it worse — a wider field for one weak signal (Michaud). **Fix is two changes that only work together:** equalise mu across a complex, so the split falls to the quadratic term and the book carries the exposure through its least-variance member (SVXY → 0%, with no hardcoded quality table); and offer the optimizer the whole complex, not just the names L5 listed. L5 now writes an `exposure` per pick so a substituted instrument is not orphaned — an expression row restates no prose and inherits no citations. Valid only while gross is SLACK. Also names the idea in `binding_constraints`, replacing `long::0` | REVERTED same day | 2026-07-27 |
| [0117](0117-a-falsifiability-guarantee-that-lives-in-a-yaml-step-is-not-a-guarantee.md) | A falsifiability guarantee that lives in a YAML step is not a guarantee — ADR-0090's first job is *record the commitment*: every published pick gets a row at publication so the denominator is fixed before any outcome is known. **Nothing enforced it on the publishing path.** The only writer was `resolve_outcomes.py`, invoked as a sibling step in `daily-refresh.yml`, so a book published any other way — a manual run, a dispatch that failed between steps — published claims nothing recorded. Measured 2026-07-27: the live book had **9 picks and 0 rows, 28% of every claim ever published**, permanently ungradeable and invisible because the panel counts what IS recorded, so a missing claim reads as a tidier denominator. `commitment_rows` + `daily_refresh` recording at publication (no network — `resolve_pick` on an empty series is already `pending`), **insert-if-absent** so a repair can never write `pending` over a resolved `hit`, and a guard comparing published picks to recorded rows (ADR-0040 applied to bookkeeping). 07-27 repaired with the same function; all 5 books now reconcile 32/32. Bar 4's blocker is now genuinely only the calendar — no older history exists to backfill. Third instance of one shape after ADR-0099 and ADR-0100: a property asserted in a docstring and enforced somewhere else | accepted | 2026-07-27 |
| [0118](0118-a-complex-is-capped-in-risk-because-capital-is-not-neutral.md) | A complex is capped in RISK, because a capital cap is not neutral between the instruments expressing one idea — ADR-0115 capped a complex in capital and ADR-0116 tried to make the members interchangeable by adjusting mu. It cannot be done: `mu = IC x sigma x z`, so averaging **mu** hands the lowest-vol member a Sharpe no instrument has (measured: gross 58.4% -> **98.8%**), and averaging the **signal** leaves equal Sharpe, where the member returning most per unit of CAPITAL wins — the highest-vol one (SVXY took 0.19999988, the whole complex). Both are artifacts of what the cap constrains. **In risk units the bias disappears:** holding member i alone at budget B takes weight B/sigma_i and returns `IC*z*B` regardless of i, so no member dominates on cost of expression — and the residual correlation then makes spreading strictly better (+0.25% at rho 0.99, **+8.5% at rho 0.70**), which is ADR-0115's basket without a table of which instruments are sound. SOC constraint (`||Sigma_C^.5 w_C||`), so MV stays a QCQP; `_psd_sqrt` uses eigh because a complex is near-singular BY CONSTRUCTION and Cholesky raises on it. **Added to the capital cap, never substituted** (ADR-0110's rule); budget scales with the BOOK's median vol, or a levered complex would earn a bigger budget for being volatile. Dry-run on the live book FIRST this time: gross 58.4% -> 54.7%, no position moves >1.5pp | accepted | 2026-07-27 |
| [0119](0119-svxy-is-equity-risk-and-the-taxonomy-said-haven.md) | SVXY is equity risk and the taxonomy called it a haven — found while asking why the book keeps expressing equity beta through SVXY; the answer was upstream of the sizer. It was classified `rates` in FOUR places, and `ASSET_CLASS_RISK_BETA['rates'] = -1.0`, so EdgeScore's regime term scored it **+0.55 (favour) in a risk-OFF tape** — the tape the same repo measures at **-35%** — and -1.00 (fade) in risk-ON, where it measures +18%. Inverted at both ends, on the largest-weight EdgeScore term, for the book's second-largest position at 10.79%. Every fact needed was already here (beta +2.08, the -35% shock, cot_fetcher's inverse=True); nothing reconciled them with the taxonomy. Now `equity` everywhere (m049) — `US Equities` also being the conservative cap reading, since SPY/QQQ/short-vol are one risk. **Honest about direction:** on today's risk-ON tape this makes SVXY ~8% MORE attractive, which is correct and the opposite of the motivation; the value is the risk-off swing of -1.10. Third SVXY defect in one day, same root cause — the repo validates computations far harder than inputs, and no check asserts an asset's class against its own measured beta. A test was PINNING the bug (`assert SECTOR_MAP['SVXY'] == 'Rates'`) | accepted | 2026-07-27 |
| [0120](0120-the-guard-reads-the-taxonomy-not-just-the-book.md) | The guard reads the taxonomy, not just the book — ADR-0119 ended by naming this gap: three SVXY defects in one day (stress signs, market beta, classification) were all wrong **inputs** that every computation then propagated faithfully, and all seven existing checks read the OUTPUT. A class is a directional claim — `regime_direction_bias` multiplies `ASSET_CLASS_RISK_BETA` by risk appetite, so equity/credit assert a positive market beta and rates/fx a negative one — and `factor_exposures.beta_mkt` measures whether it is true. Flags only a MATERIAL contradiction (|beta| >= 0.5): TLT at +0.11 against a rates class is a policy statement its beta does not refute, and a guard that cries wolf is ignored by the second week. commodity asserts 0.0 so is never flagged (ADR-0036). **Found a second instance within minutes of being written:** EWZ, the iShares MSCI BRAZIL ETF, classified `fx` (risk beta -1.0, a haven) with measured beta **+0.98** — a Brazilian equity fund scored as something that rallies when risk is sold (m050). The code and the DB already disagreed and nothing compared them: `LENS_TICKER_FALLBACK['equity']` listed EWZ while `theme_assets.asset_class` said fx. Coverage travels with the verdict (18 of 23 checkable; 30 tickers still carry a null class). Regression tests carry the DEFECTS, not the fix | accepted | 2026-07-27 |
| [0121](0121-the-fix-missed-the-map-that-mattered-and-the-guard-said-clean.md) | The fix missed the map that mattered, and the new guard said clean — ADR-0119 diagnosed SVXY correctly and fixed `SECTOR_MAP` and `theme_assets.asset_class`. **Neither is what `regime_direction_bias` reads.** There are TWO asset-class maps: `trade_ranker._ASSET_CLASS_MAP` is what `classify()` returns and therefore what reaches EdgeScore; the DB column is the lens filter (ADR-0015). The code map still said `rates`, so after a migration, an ADR and a commit the regime term was **still inverted**. ADR-0120 then built a guard for exactly this defect and pointed it at the DB column — reporting *"every classified asset's beta agrees with its class"* while the defect was live. **A false assurance is worse than no guard:** an unknown prompts a look, this gives a reason not to. Fixed the map with the consequence (risk-off bias +0.55 -> -0.55), pointed the guard at the same seam (coverage 18/23 -> **47/53**, so the original was checking the smaller half), and added `check_asset_class_maps_agree` — ADR-0064's one-formula-one-place applied to a lookup table. Tests assert the SEAM (`classify()` -> `regime_direction_bias`), not the constant. The generalisable failure: **I fixed a name, not a path** — SVXY appears at eight sites and nothing traced which one the stated consequence flowed through | accepted | 2026-07-27 |
| [0122](0122-a-tickers-identity-lives-in-six-tables-and-none-agreed-to-agree.md) | A ticker's identity lives in six tables and none agreed to agree — ADR-0121 ended on "I fixed a name, not a path"; two pairs were still unreconciled. **`DEFAULT_TICKER_BETAS` vs measured**: held SVXY at -0.60 against +2.08, wrong sign and 2.7 apart. It is the stress model's FALLBACK, used only when live FF5 is missing — which is exactly why it rots: the degraded path is never exercised on a good day, so its assumptions are consulted only once everything else has failed. **`LENS_TICKER_FALLBACK` vs `_ASSET_CLASS_MAP`**: disagreed in BOTH directions the same day (SVXY under rates/credit at beta +2.08; EWZ under equity while classed fx). The beta check is deliberately tolerant — a fallback is a PRIOR, not today's point estimate, so only wrong SIGNS and gaps >1.0 fire. Calibrated so the two largest live gaps (SLV +0.25 vs +1.08, XLE +0.80 vs +0.04) do NOT fire and are left alone on purpose. Lens check is strict with one documented widening (credit legitimately holds rates). Four pairs now checked; `GEO_MAP` and `_theme_default_assets` still have none because **neither has a measurement to check against** — saying so beats inventing a proxy. The real fix is one owning record per ticker, which would make five of these checks unnecessary, and is its own ADR | accepted | 2026-07-27 |
| [0123](0123-a-group-cap-is-projected-not-declared-close-enough.md) | A group cap is projected onto, not declared close enough — `_round_weights` FLOORS each magnitude, absorbing the ~1e-8 a solver leaves on a SINGLE name. It cannot fix a GROUP: flooring three weights removes at most 3e-8 while the solver satisfies a SUMMED constraint to its own tolerance. Measured 2026-07-27: complex total **0.20000334** against a 0.20 cap — 3.3e-6 over, 300x what flooring reaches and three orders above `CAP_EPSILON` (1e-9), the figure `exceeds_cap` judges by. So `/risk` could report a governance violation on a correctly-solved book, which ADR-0068 forbids. **Loosening CAP_EPSILON was rejected**: its own comment calls it a representation-error guard and NOT an economic tolerance, and widening it would both make it the fitted thing it disclaims and silently permit a real 3bp breach. `_project_group_caps` scales an overshooting group back onto its limit; each name takes the strictest factor of its groups in one pass (scaling down only reduces other totals, so it lands under, never over), magnitudes only shrink so single-name and gross stay satisfied, and it reports itself rather than rescaling silently. **Logged as open TWICE (ADR-0116, ADR-0118) and left, because it looked cosmetic** — the size of a number is not the size of the defect | accepted | 2026-07-27 |
| [0124](0124-a-comment-doing-a-tests-job-is-how-the-defect-survived.md) | A comment doing a test's job is how the defect survived — asked whether the taxonomy consolidation was the right fix, the honest audit said it would have prevented ONE of the four SVXY defects (the missed edit site); the other three were a wrong VALUE nothing validated. The corrected S1 entry's own comment does the arithmetic that would have caught the original: beta +2.08 x mkt -0.18 = -0.37 implied against +0.20 stated — written as prose, after the fact. `check_scenario_overrides_reconcile` enforces it: a direct shock may not VIOLENTLY contradict its scenario's own factor path — flags only sign-disagreement AND gap >= 0.25, because same-sign gaps of any size are views (S6's sector channel, ADR-0088) and small sign-crossers are channels (GLD's flight-to-quality bid, S2's XLE at 0.07). Pre-fix entries sat at 0.57 and 0.32. **The threshold is a judgement calibrated against known failures, and the ADR says the quiet part**: third such guard in two days, all tuned to catch what was already found, false-negative rate unknown. CI is primary enforcement (constants vs constants), the nightly guard is the backstop. Consolidation deferred with its precondition verified (nothing mutates the maps at runtime). The principle: when a comment explains a number BY ARITHMETIC, that arithmetic is a check waiting to be written | accepted | 2026-07-28 |
| [0125](0125-one-ticker-one-record-the-maps-are-views.md) | One ticker, one record — the taxonomy maps are views. ADR-0121's failure shape: sector, geo and asset class in THREE separately-editable dicts across two modules, a fix landed in two and missed the one with the consequence. `ASSETS: dict[str, AssetRecord]` (frozen) is now the owning record and the three maps are derived projections — zero call-site churn, and the half-edit is UNREPRESENTABLE rather than merely detected. **Zero value changes, provably**: the literal was GENERATED from the live dicts and the views asserted byte-for-byte equal to a pre-edit snapshot. In code, not the DB, deliberately — a misclassification in code is a reviewable diff; in a table it is an invisible UPDATE, and the DB column stays as the lens filter, cross-checked nightly and deliberately NOT auto-synced. `LENS_TICKER_FALLBACK` is NOT derived (holds CDX/HY, outside the 53-ticker universe — deriving would silently shrink it, a value change this move is forbidden to make). Preserves and pins the concurrent session's same-object alias. Deferred behind ADR-0124 on an honest audit: this prevents the missed-edit class (1 of 4 defects), validation prevents the wrong-value class (3 of 4) | accepted | 2026-07-28 |
| [0126](0126-a-chart-without-a-value-axis-is-a-shape.md) | A chart without a value axis is a shape — the five quant illustrations were arithmetically sound (sum of contribution_to_vol = 0.08049207 against a persisted portfolio_vol of 0.080492074) and presentationally not. THREE HAD NO VALUE AXIS: the waterfall printed "volatility" and nine tickers and nowhere printed **8.05%**, the number it exists to communicate; values lived only in `<title>` tooltips, which are hover-only and absent on touch. TWO MARKS WERE CLIPPED BY THEIR OWN viewBox, both from centring a mark on a coordinate that IS the plot boundary — the total bar at `slot*(n+1)` measured 683.5 to 724.5 against 720, and fixed-width histogram bars overhung by half a bar (~8px at 41 bins, x = **-128** at 3 bins). Bars now derive from bin EDGES and clamp; the regression test sweeps book sizes 2/3/5/9/14 because the bug was invisible at the size the unit test happened to use. COLOUR CARRIED THE WRONG MEANING: `--long` vs `--short` measures **deltaE 6.1 under deuteranopia** (validated, not eyeballed), and the scatter put the SIGN OF RISK on colour while direction — what colour means everywhere else in this app — rode on a 1px radius difference with no legend. Colour now means direction, long is a filled dot and short a ring, risk sign is the y-axis. The fan is an emphasis chart (it had drawn p90 and p99 in the SAME token at different opacity) and its legend stops calling p95 a horizon: p95 is a confidence level, 21d is the scored horizon (ADR-0090). Also: CorrelationMatrix's two adjacent counts now share a denominator | accepted | 2026-07-28 |
| [0127](0127-the-cross-asset-term-measured-one-asset.md) | The cross-asset term measured one asset — the brief defines a theme as a narrative driving CROSS-ASSET moves, and `hype_corr_weight` (**0.30**, a third of HypeScore) is the only term claiming to measure it. It measured ONE instrument. `build_theme_signals` looped a theme's mapped tickers guarded by `if not pd.isna(corr)` and broke on the first — a test that could never fail, because `correlation_with_mentions` returned the **0.0 sentinel** for every unmeasurable case and never NaN. So Fed Policy, mapped across eight instruments spanning rates/commodities/equity, was scored on whichever ticker Postgres returned first under an ordering nobody chose; and a theme moving four asset classes together scored IDENTICALLY to one tracking a single ETF. Worse, an unmeasurable first ticker scored a hard 0.0 — a 30-point deduction for a gap in our data — while seven mapped instruments had full history. Same defect class as ADR-0121: **a guard that cannot fail reads as a check that passed**. Now: `correlation_with_mentions` returns **None**, never 0.0, so "not measurable" is un-ignorable at the call site; every mapped instrument is measured and collapsed **per asset class** (strongest signed reading wins within a class); the sub-score is the mean over MEASURED classes of `min(1, |c|/0.50)`, so breadth and magnitude are both priced; and the denominator counts classes measured, never classes mapped — ADR-0036's renormalisation rule applied to a second formula. `hype_score` renormalises when correlation is absent, but the all-four-present arithmetic is UNCHANGED so /method still reconciles byte-for-byte | accepted | 2026-07-28 |
| [0128](0128-a-theme-we-did-not-name-in-advance.md) | A theme we did not name in advance — the brief asks which themes are trending; this system answered which of **eight** were, hard-coded twice (rows in `themes`, keyword lists in `THEME_KEYWORDS`). A narrative matching none of the eight lists was not low-scoring, it was **structurally invisible**: no counter, no series, no row. Of the brief's own examples, "central bank pivot" is covered (US-only), "dollar debasement" only partially, and **"AI capex cycle" not at all**. `theme_discovery.py` cannot fix this — its corpus is `theme_news`, collected BY those eight queries, so it can only find sub-themes of what was already named; and its SBERT/UMAP/HDBSCAN/LDA stack is not installed in CI (`import gensim` fails, 14 tests red). Now: `MARKET_SEED_QUERIES` collects an **un-themed corpus** into `market_news` (no mock fallback — a frequency tracker reading template headlines would "discover" the template's vocabulary); `narrative_tracker.py` tracks every 1–3-gram's **share of voice** daily with a robust velocity against its own history, dependency-free, deterministic, 39 tests. Two details are load-bearing: a **MAD floor** on the velocity scale (ADR-0047's argument — a phrase flat for eight days has MAD 0, and refusing to score a break from a stable base declines exactly the phrases most likely to be new), and a **two-letter** minimum token length, because three deleted `ai`. `covered_by` names the anchor theme already asking for each phrase, so a surge in `fomc` is Fed Policy working and a surge in `ai capex cycle` is a miss. `NarrativeTrends` plots it under ADR-0126 (value axis, direct labels + leaders, figures table) on a **validated** `--series-1..5` palette. SHADOW: nothing here sizes a position — a phrase is not a theme until someone maps it to instruments, and that step is deliberately not automated | accepted | 2026-07-28 |
| [0129](0129-ai-capex-is-a-theme-and-the-tracker-found-it-sideways.md) | AI Capex is a theme, and the tracker found it sideways — ADR-0128 argued the AI capex cycle was structurally invisible here. The first live run proved it more sharply than the argument did: across **455 documents, AI appears in exactly TWO**, and both arrived through the **Corporate Credit** query — *"Goldman Offers Way to Trade AI Junk Bonds"* and *"Bond market anxiety is growing over AI capex budgets"*. The largest capex cycle in the market was reaching this system only as a side-effect of asking about junk bonds. The tracker made that legible; **seeing is not trading**, and ADR-0128 deliberately did not automate the next step. So: `AI Capex` becomes an anchor theme with **13 instruments across 4 asset classes** (m050), mapped as a CHAIN — compute (SMH/NVDA), manufacture (TSM), the spenders (MSFT/GOOGL), power (VST/XLU), electrical plant (GEV/VRT), copper (FCX), **funding (JNK — where the evidence actually came from)**, duration (IEF), gas (UNG). Verified on real prices: all 13 measurable, breadth **2 of 4 classes material**. **Three new sectors** because a sector is what the 30% cap binds on — one bucket would let the book hold three legs of one chain and call it diversified; MSFT/GOOGL stay in Tech Growth for the mirror reason, being 40%+ of QQQ. **Taiwan is its own geo**: inside EM it would net against Brazil and China. Two guards earned their keep — ADR-0088's S6 test failed the instant three new sectors existed (**7 positions would have been silently unstressed**), and a test caught that adding `dollar` as an alias had annexed **"dollar debasement"**, the very narrative ADR-0128 names as its motivating example; a one-word keyword now covers only the one-word phrase. Also: `data` stopped being a stopword, because stopping it silently destroyed **"data center"**. Honest framing: the tracker proposes, **a human maps**, the engine sizes — automating the middle step would mean inventing an instrument map from a phrase | accepted | 2026-07-28 |
| [0130](0130-fourteen-tests-were-ungated-by-where-an-import-sat.md) | Fourteen tests were ungated by where an import sat — `theme_discovery.py` claimed its agreement logic was *"factored into pure functions that are unit-tested without the models"*. Factored, yes; testable, no: **four module-scope imports** (gensim / sentence_transformers / umap / hdbscan) meant importing the module for a pure function imported **torch**. CI had already hit this and worked around it — `--ignore=tests/backend/test_theme_discovery.py`, justified as *"it stays covered by the monthly workflow actually running"*, which conflates **a job executing** with **logic being tested**: the monthly run proves `run_discovery` did not crash, and proves nothing about whether Tier-2 agreement promotes at the right overlap threshold. The measurement that settles it: **not one of the 14 excluded tests touches a model** — grep for `SentenceTransformer|umap\.|hdbscan\.|models\.Lda|corpora\.` in the test file returns nothing. Fourteen tests were ungated entirely by where four import statements sat. ADR-0121's failure in another costume, and the exact gap ADR-0128 recorded and routed around. Fix: move the imports inside `run_discovery` **past the corpus-size gate** (steps 1 and 5 need no model, and loading torch to discover a 12-document corpus is pure cost), drop the `--ignore`, and replace the comment with a guard — `TestImportsWithoutTheModels` blocks the ML stack via `sys.meta_path` in a **subprocess** (in-process, `sys.modules` would mask a module-scope import that fails on a clean machine) and greps for a heavy import at column 0. Suite **1097 → 1113 passing, 0 failing, no new dependency installed**. Narrower claim now true: the *pure* logic is tested; the ML path is still covered by execution, not assertion | accepted | 2026-07-28 |
| [0131](0131-a-complex-is-safe-to-cap-and-unsafe-to-shop-from.md) | A complex is safe to cap and unsafe to shop from — closes the menu-widening question ADR-0118 left open ("buys the precondition, not the feature"), judged against the republished book it was gated on. The motivating case dissolved: under the corrected taxonomy the model did not pick SVXY at all. And the book's own complexes refute the premise, measured: the duration complex {AGG,IEF,SHY,TLT} is a **chain, not a clique** — SHY-TLT pairwise is **0.632, below the 0.70 bar that defines the complex** (components are transitive; correlation is not), so a widened sizer could swap the book's deliberate front-end hold (SHY, ~1.9y) for TLT (~17y): a 9x duration change inside "one idea". **The asymmetry is the principle**: capping an over-merged component is only ever too TIGHT (SHY and TLT sharing one budget is conservative); shopping from it silently swaps the thesis while every constraint reports satisfied. Caps stay exactly as built. The one genuinely substitutable pair — GLD-IAU at **1.000**, wrapper substitution — is named and deliberately not built: cost_model has no per-name spread/liquidity input, so the choice would rest on nothing measured. Revisit only if per-name cost data lands, and then over PAIRS at rho >= 0.95, never components. `independent_ideas` inherits a conservative caveat: a chained component counts as one idea, so the pool reads thinner than it is, never deeper. (0127-0130 claimed by the concurrent session's in-flight ADRs; their rows will slot above this one) | accepted | 2026-07-28 |
| [0132](0132-the-universe-is-measured-in-independent-ideas-per-side.md) | The universe is measured in independent ideas per side — supersedes ADR-0043's add-by-list framing (its additions stand: NUE, UNH, JPM are in the book doing work). Measured on the republished 07-27 book: long 17 names -> **11 ideas** (six of slack against the 5-a-side ask); short 13 names -> **6 ideas** (ONE of slack), because **9 of 13 short names collapse into two complexes** (gold x5, China x4) — shorts cluster harder than longs since natural short expressions share macro drivers. The side is independence-thin, not name-thin; a tenth name in a cluster adds basket depth the caps will bound and zero breadth. **The acceptance test is the function that already exists**: re-run `independent_ideas` with the candidate included, accept if the side's idea count rises (ADR-0048: one number, one meaning). Caveats: the count runs on the 30-cap agent pool (widening the cap is a different lever with an LLM-context cost), and ADR-0131's chain caveat means a side may be LESS thin than reported — the error never overstates readiness | accepted | 2026-07-28 |
| [0133](0133-two-methods-that-fail-differently-agreeing.md) | Two methods that fail differently, agreeing — ADR-0128 said *"agreement between them is a stronger signal than either alone"* then *"wiring that comparison is not done"*. Wiring it surfaced why it was unsafe to build first: **three of eleven candidates in the live discovery run are built on publisher names, two of them Tier 2** — the tier that means two independent methods agreed. `dollar / forecast / **fxstreet**` and `nato / **pravda** / ukraine`. They agreed on a byline. `theme_discovery.preprocess` kept its own tokenizer carrying all three defects fixed in the tracker hours earlier (no attribution stripping, no publisher stoplist, `len(t) > 2` which deletes `ai`), so corroboration would have **laundered publisher noise into the strongest badge the system emits**. Fixed at the source — preprocess now delegates to `narrative_tracker.tokenize`, ADR-0064's one-formula-one-place applied to a wordlist. That also closed a hole in **ADR-0130**: preprocess imported gensim INSIDE a function that ADR called pure, so `cluster_term_sets` still failed without the stack and CI — which does not install gensim — **would have gone red on the next push, caused by ADR-0130's own removal of the `--ignore`**. The guard asserted `callable(...)`, true of any defined function; it now RUNS them. Threshold **measured, not picked**: at >=1 shared token the live top matches are `oil`/`dollar`/`fxstreet` (one word in common, pure chance), at >=2 they are `credit spreads` ~ *credit/high/spreads* and `federal reserve` ~ *federal/rates/reserve*. Live: **20 of 506 corroborated**. The claim is not "two votes beat one" but that the methods **fail differently** — frequency is fooled by repeated boilerplate, clustering by a coherent-but-tiny topic — so neither failure explains both. Still shadow; `days_stale` travels with every match because discovery is monthly and this is not a simultaneous second opinion; an unmatched signal is returned **bit-identical** | accepted | 2026-07-28 |
| [0134](0134-a-prior-and-a-discovery-are-different-claims.md) | A prior and a discovery are different claims — asked whether AI was *discovered* or merely added. It was added, which is defensible; what was not is that **the record could not tell you which**. Every row in `themes` read `source = 'practitioner'`, `discovered_at = NULL` — all nine — so `AI Capex`, created that afternoon after a measured signal and an explicit instruction, was **byte-identical in provenance to `Fed Policy`**, a line in migration 001 from an opening list. On a product whose claim is that every number can be interrogated, the origin of a theme — the thing deciding which numbers get computed at all — was the one fact with no provenance. Migration 051 adds `promotion_basis` / `promoted_on` / `promotion_evidence`, asking a question `source` does not: **did a measurement precede the decision?** Four values — `practitioner_prior` (an opening list, no measurement claimed and none needed), `operator_directed` (a person named it; evidence may exist but did not come first), `measured_discovery` (**the only value claiming the system found it**), `unrecorded`. **AI Capex is `operator_directed`, and that is the point**: the strong retrospective number (AI/capex in 4 of 8 LDA topics over the un-themed corpus, incl. `alphabet/capex/guidance/meta`) sits under a `retrospective` key beside the 2-of-455 signal that actually preceded the call, because a payload recording only the strong number would have laundered the ordering. **A prior with no evidence is not a defect** — `evidenceExpected()` is false for it, ADR-0066's absent-is-not-zero applied to a justification — and the chip is a label, not a ranking: only `measured_discovery` takes emphasis, because Fed Policy is a different kind of claim, not a worse theme. Nothing is `measured_discovery` yet, which is the honest state; the 2026-08-01 discovery run is the first chance to earn it | accepted | 2026-07-28 |
| [0135](0135-a-thesis-must-not-assert-a-position-the-book-does-not-hold.md) | A thesis must not assert a position the book does not hold — asked whether any AI names were actually chosen (two were: GEV, VRT). Reading VRT's published thesis to answer surfaced a live defect: it argues *"**the long VRT / short MSFT structure** below isolates the capex-deployment vs capex-monetization gap"*, and the 2026-07-28 book has **no MSFT position**. MSFT was not invented — the engine put it **short at EdgeScore −0.322**, the theme's most convicted name — it simply did not survive selection, so the agent reasoned about a pair and **published one leg**. Every number in the sentence is correct (53.6 is the HypeScore, 81.82% is breadth), which is exactly why it passed: the citation guardrail verifies cited **numbers** and has no notion of a **position**. ADR-0049 already said the badge certifies figures not prose — right for *interpretation*, but "the book holds a short MSFT" is not an interpretation, it is a checkable statement about our own output, and it was false. The design turns on **claim vs comparison**, because a naive check fires on correct writing: the same run's GDX thesis says it is *"the strongest expression of the gold-short trade from the {GDX, GLD, IAU, NEM, SLV} cluster"*, which names a correlation complex and is ADR-0116 being **explained**. So braced listings are masked, and only a direction word attached to a ticker or a position noun following it counts; one claim per ticker; a pick's own asset exempt. It **warns rather than rejecting** — a citation failure means a number is wrong and the book is unsafe, this means one sentence over-claims while every figure is sound — and the caveat travels **on the pick** so /book, /ask and MCP read it from one payload. Verified live: **exactly one finding**, VRT → MSFT. Does NOT fix the behaviour, only labels it; the detector is lexical by choice, because a guardrail that needs a model can hallucinate | accepted | 2026-07-28 |
| [0136](0136-repair-the-thesis-rather-than-footnote-it.md) | Repair the thesis rather than footnote it -- ADR-0135 caveated the false claim and said the real fix was L5 control flow. Making it required checking something that ADR **assumed**: where the leg was lost. `research_agent_runs.raw_output` shows the model's raw picks **already were** the final nine -- **MSFT was never emitted**. Nothing downstream dropped a leg; the model described a companion position it did not take, so ADR-0135's *"did not survive selection"* was right about the symptom and **wrong about the mechanism**, and the correction moves the fix off a stage that never lost anything. Two changes: the **prompt** now forbids asserting a position in a ticker outside the model's own picks (citing the failure, and preserving *"you may still COMPARE"* so it does not suppress correct prose), and `repair_thesis` **excises the offending clause deterministically**. Not a second LLM call, for three compounding reasons: it spends the nightly book's shared MiniMax quota, it reruns a **stochastic** step over figures already verified so a correct book can come back worse, and it can introduce **new numbers** needing re-verification -- turning a prose fix into a citation problem. Excision only ever REMOVES text, so every surviving figure was already checked, and ADR-0013's determinism holds. **It declines more often than it fires**: sentence/semicolon boundaries only (a comma cut leaves a fragment), at least 55% of the text must survive or the clause was carrying the argument, never a single-clause thesis. **The edit is never silent** -- ADR-0093 applied to prose: the caveat names the ticker, **quotes the removed text**, and affirms the figures. Closes the seam too, since a thesis ending on a dangling semicolon reads as truncated and invites doubt about its numbers. Live: **1 claim before, 0 after**, all four verified figures kept | accepted | 2026-07-28 |
| [0137](0137-basis-points-are-not-a-hundredfold-error.md) | Basis points are not a hundredfold error -- the 2026-07-28 run logged *"Cited value 279.0 does not match source 'BAMLH0A0HYM2'=2.79"*. **The model was right**: 279bp IS the high-yield OAS, and FRED serves the series in percentage points. Worse than a naive check -- **the prompt asks for exactly the form the verifier rejects**, its worked example being `{"source": "BAMLH0A0HYM2", "value": 380.0}` against a series storing ~3.80, so a model following instructions precisely produces an ungrounded citation every time. They survived only inside the 20% ungrounded allowance, meaning the tolerance budget was being spent on the guardrail's own contradiction rather than on real model error. The frontend has reconciled this since `formatSlopeBps`; the backend never learned it -- the same unit confusion ADR-0091 found in the regime classifier, one layer down. Fix: `BPS_QUOTED_SOURCES` declares per series that bps is the market convention, **for SPREADS only**. A spread is a difference between two yields and is quoted in bps (*"HY at 279 over"*); a **level** is quoted in percent and nobody says "469bps" for the ten-year -- so `DGS10`, `DGS2`, `DFII10`, `T10YIE`, `VIXCLS` are deliberately absent and a 100x claim on them still **fails**. That distinction is the whole safety argument: the obvious implementation, a blanket "also try /100", would accept a genuine order-of-magnitude error on every series in the snapshot, turning the guardrail against fabricated magnitudes into one that waves them through. Not a wider tolerance -- a **per-source declaration of a domain fact**, each entry carrying its reason. The prompt is fixed too, since leaving the check to absorb a contradiction the instruction creates treats the symptom. Reconciliations are **reported, not silent**. Replayed live: exactly the two tolerated citations now pass (HY OAS 279/2.79, IG OAS 80/0.8), source-key matches 19 -> 21 | accepted | 2026-07-28 |
| [0138](0138-a-date-filter-the-api-silently-ignored.md) | A date filter the API silently ignored -- a reader saw 2025 headlines in the ribbon; the DB said 2001: the 2026-07-28 run persisted **386 headlines, 43 pre-2026, oldest 2001-11-16**, all evergreen explainers ("Junk Bonds - Econlib", EIA's "What is OPEC+"). `call_brave_mcp.js` sent the lookback as `from=<date>` -- **not a Brave parameter**; the API's filter is `freshness` (`YYYY-MM-DDtoYYYY-MM-DD`) and Brave ignores unknown params WITHOUT ERRORING, so every fetch since the script existed was an unfiltered relevance search. The window was computed in Python, passed to Node, appended to the URL, and discarded by the API -- it existed only in this codebase's intentions. Compounding it, an unparseable `page_age` was stamped with TODAY's date, and the ribbon sorts `published_date desc` -- a page of UNKNOWN age was shown FIRST, as today's news. Fix in three layers: `freshness` range at the API (the 50-result page must be spent on fresh results, ADR-0028's density argument); `date: null` never today for unknown age (ADR-0066 applied to timestamps -- null persists as NULL, sorts `nullsFirst: false`, counts toward NO mention bucket); and `_within_lookback` drops pre-window items in Python for BOTH fetchers, because the API-side filter failed silently for the script's whole life and its silence must never be trusted twice. L1b's corpus shared the helper: 120 of 220 market rows predated their 7-day window. Repaired the published run in place (91 + 120 rows deleted); earlier run_dates left as collected -- they record what their day's scores were computed from. Undated items are KEPT by the Python drop: "date unknown" is not "old" | accepted | 2026-07-28 |
| [0139](0139-dollar-debasement-indicator.md) | A dollar-debasement pressure reading on the regime row -- the regime row carried only `cycle` x `sentiment`, and both readings are silent on monetary erosion: a late-cycle risk-off book can coincide with real yields collapsing and a gold bid, and the row cannot say so because it was never asked to. `narrative_tracker` (ADR-0128) cited *dollar debasement* as its motivating example of a narrative the L1 anchors could not see, but a tracker is evidence a NARRATIVE exists, not a measurement of underlying pressure. The data is already on disk: `DFII10`, `DX-Y.NYB`, `GC=F` are all in `macro_daily_history` (`macro_fetcher.py`). Add `debasement_pressure` (0-100, composite) plus FOUR component columns (real_yield, dxy_decline, gold_rise, comovement) so the reading is a fact with evidence, not a black box (ADR-0091). Composite weights (30/25/25/20) live in code, not config -- moving them is a code change. Co-movement term -- corr of DAILY changes in real yield vs gold over the window -- is the debasement-specific diagnostic: it discounts gold moves real yields are not driving; the mechanical DXY/gold overlap (50 of the 100 points) is accepted and disclosed, not removed. Units declared per ADR-0137: DFII10 in percent, DXY drawdown-from-26w-peak and gold return as fractions. 26w lookback (one option cycle / one FOMC cadence), 14-day shadow before the `MacroCrossCurrents` panel mounts -- and the shadow strips the columns from the L5 snapshot too, since `q1_agent` freezes the regime row via `select("*")` (ADR-0100). No new table. Pair-shipped with ADR-0140. **Shadow lifted day one by operator direction** — the 270-row chronological backfill performed the shape/NULL validation the 14-day accrual existed to gather; recorded in the §5 postscript | accepted | 2026-07-28 |
| [0140](0140-hawkish-dovish-pivot-indicator.md) | A hawkish/dovish-pivot reading on the regime row -- Fed Policy is the only theme whose narrative embeds a directional claim (*hawkish, dovish, rate cut, rate hike*), but the regime row carries no posture field. The Q1 agent can narrate posture in a thesis (citation-verified, ADR-0012), but a reader of `/book` who wants posture as a MEASUREMENT has to read prose and reverse-engineer it. Data on disk: `DFF`, `DGS2`, `DGS10` -- the textbook signal is fed funds futures (CME FedWatch, gated API) which this ADR deliberately routes around; the 2s10s curve and recent DFF trajectory together encode the same information with a documented delay/noise penalty. Add `fed_posture` (hawkish/neutral/dovish), `fed_pivot_delta` (signed SMALLINT, range -2..+2 so magnitude is preserved -- a boolean forces a reader to look at two adjacent rows), three provenance numerics (rate change 13w bps, curve change 13w bps, current steepness bps), and a `fed_posture_evidence` JSONB so the page can render the pivot label without a second round-trip. 13w window = one FOMC cadence. Curve leg reads steepening as DOVISH (market pricing cuts) and flattening as hawkish -- an earlier draft had this inverted against its own component table. Pivot is computed against the posture ~13 weeks prior (sign(dovish)=+1, so +2 = hawkish -> dovish), not stored redundantly. L5 prompt gains one line: pivot claims must cite `fed_posture` and `fed_pivot_delta`, NULL means no claim. Posture ink reuses --long/--short so hawkish == red and dovish == green across the page (ADR-0085's directional vocabulary). Pair-shipped with ADR-0139 / migration 052 / migration 053. **Shadow lifted day one with ADR-0139's** — one coordinated change: exclusion removed, prompt rule + citations wired, `MacroCrossCurrents` mounted | accepted | 2026-07-28 |
| [0141](0141-the-corpus-and-its-denominator-were-both-ours.md) | The corpus and its denominator were both ours -- asked why AI, plainly the dominant market conversation, read as marginal. Three nested defects. **(1)** Theme discovery never read the un-themed corpus: ADR-0128 built `market_news` to break the circularity `corpus_from_theme_news` describes in its OWN docstring, wired it into the tracker and **not into that function**, so discovery stayed circular after the ADR claiming to fix it (`grep -c market_news` returned **0**). **(2)** The tracker's denominator was **94% self-selected** -- 100 un-themed docs against 1688 fetched by asking about Fed/dollar/oil/China. AI was **34% of the unbiased sample and 7.0% of the blend**, diluted 5x into looking marginal, and the board duly read `us` 14.1, `dollar` 13.5, `oil` 12.8 -- every one a thing we went looking for. ADR-0128's own reasoning caused it ("excluding themed news leaves too small a denominator"): a bigger denominator of self-selected text **corrupts** the estimate, and `covered_by` cannot repair a denominator. **(3)** A seed query named a narrative -- `"capital spending"` is a capex synonym and returned **52 of 100 documents and 33 of the 34 AI-mentioning ones**, making the 34% a statement about the query. Fixes: discovery reads both corpora (clustering wants text and computes no share); the tracker scores **only the un-themed** one (a share needs an unbiased denominator); ten narrative-neutral seed queries, corpus 100 -> 295, AI **8.8%** spread across queries. Free because `narrative_signals` had one day and **zero** velocities -- a day later every series would have carried a discontinuity | accepted | 2026-07-28 |
| [0142](0142-frequency-cannot-tell-a-narrative-from-a-register.md) | Frequency cannot tell a narrative from a register -- with the bias fixed the board read `earnings` 27.6, `price` 13.7, `q2` 10.6, `growth`, `revenue`, `analysts`, with `ai` at #12. That is **the vocabulary of financial writing, not a set of narratives**. Asked whether TF-IDF would separate them. **No -- three measured rejections.** TF-IDF produced a **byte-identical** top-10 to share, and provably so: this counts DOCUMENT frequency (`phrases_in` returns a set), so `tfidf = x·log(1/x) = -x·ln(x)`, which is **monotonically increasing for x < 1/e = 0.368** -- every phrase is far below that, so it is an order-preserving transform that cannot reorder anything. TF-IDF damps term-frequency inflation, which was designed out on day one. **Background lift** ranked `q2` 211x, `revenue` 184x, `guidance` 143x -- all with background count **zero**, because the only accumulated corpus is `theme_news` (Fed/dollar/oil/China), which never discusses earnings: ADR-0141's bias in a new place. **Capitalisation** fails too -- **64% of headlines are title-cased**, and the sentence-cased remainder yields `by`, `investing.com`, `the`, `times` as top entity candidates. What discriminates is **change** (velocity, self-referenced against a phrase's own history, so it needs no representative background) and then **price** -- the brief defines a theme as a narrative *driving cross-asset moves*, which is not a linguistic test at all. **The generalisable point: this was being attacked as a linguistics problem when the definition is economic.** Nothing added now; both instruments need the same accumulated corpus. Falsifiable in ~4 runs: `ai` carries positive velocity, `earnings` sits near zero | accepted | 2026-07-28 |
| [0143](0143-the-price-link-gate-and-what-it-refuses-to-say.md) | The price-link gate, and what it refuses to say -- ADR-0142 concluded the narrative/register problem is economic, not linguistic, and said the gate "cannot be built earlier" for want of history. **That was wrong**: `market_news` carries `published_date` and its 295 rows span **8 publication dates**, so the mention series is reconstructable today. Built as the SAME statistic the anchors face (ADR-0127's `correlation_with_mentions` -> `per_class_corr` -> breadth, ADR-0064's one-formula-one-place), over **share of voice per publication date** because collection volume swings 13-to-80 across the window and raw counts would correlate with the news cycle instead of the phrase. **The live run rewrote the design.** It returned `linked` for **all fourteen** phrases tested -- 0.84 to 1.00, 4-5 of 5 classes material, `earnings`/`wall`/`q2`/`analysts` apparently moving the whole book -- because `sessions = 5` and a Pearson r at n=5 sits near ±1 by chance. **The gate was measuring its own sample size.** The first cut returned `linked` with a separate `provisional` flag, which is exactly ADR-0059's single-date-IC failure: the true answer behind an opt-in the caller must remember to check (ADR-0100). So **`insufficient_history` is the verdict itself** below `MIN_SESSIONS_FOR_VERDICT = 20`, with two deliberately distinct floors -- 5 to COMPUTE, 20 to BELIEVE -- and the raw subscore, per-class correlations and session count all kept on the record, because refusing a verdict is not hiding a measurement. Currently returns `insufficient_history` for everything, which is the correct output; useful around 2026-08-17. Gates nothing yet by construction, and `rank_by_link` (linked before ambient, breadth before magnitude) is written but unused for the same reason | accepted | 2026-07-28 |
| [0144](0144-a-second-provider-that-is-an-archive.md) | A second provider that is an archive -- two open findings, one fix. ADR-0094 disclosed that the attention signal rests on **one** provider (a corroboration gate over N=1 either always passes or never can). ADR-0143's price-link gate needs ~20 trading sessions and had 5. Asked whether Brave could backfill: **no**, measured on identical 45-day windows -- Brave puts **48% of its documents in the last 7 days** with older days holding 1-5, against GDELT's **18%** with older days holding 3-9. Brave is a recency RANKING; GDELT is an ARCHIVE. And a Brave backfill would be worse than waiting: 39 dates would **clear the 20-session floor with fabricated density**, flipping `insufficient_history` into a confident false `linked` -- **the backfill would defeat the guard by satisfying it**, which is `backfill_regime.py`'s standing refusal to reconstruct HypeScore, now with numbers. So: GDELT DOC 2.0, free and keyless like `cot_fetcher`. Four constraints encoded because each was learned by tripping it -- **1 request/5s server-enforced** (pacing lives in the client, since a rate limit honoured only when the caller remembers is not one), **throttling arrives as plain text under HTTP 200** (the body is tested, not the status; a naive `.json()` would raise instead of retry), **`sourcelang:english` is mandatory** (without it the corpus filled with French market coverage), and **UTF-8 decoded explicitly** (inference produced mojibake). A **wall-clock budget** bounds the fetch, since the retry ladder can burn ~131s per query and ~22 minutes for ten against a ten-minute pipeline; on expiry it returns what it has and reports how many queries it skipped. GDELT **supplements** Brave (39 docs/day recent vs 6) rather than replacing it. Stated: the client's own first live fetch is still pending -- the API shape was measured by direct probe and the client has 16 unit tests, but probing exhausted the rate limit | accepted | 2026-07-28 |
| [0145](0145-theme-trends-share-among-anchors.md) | The theme trends board plots a share of something it names -- the operator asked for Google-Trends plots of the themes, and the metric decision is the ADR: each anchor's `mention_count_1d` over the day's total across themes, captioned as **relative attention AMONG the anchors, not share of an unbiased corpus** (ADR-0141's bias, disclosed instead of laundered -- the narrative board carries the unbiased claim). HypeScore-over-time rejected (a same-day cross-sectional RANK plotted as a trend reads "theme fell" when the truth may be "another rose"); peak-normalised 0-100 rejected (hides that Fed Policy 50% and Inflation 4% are different magnitudes); raw counts rejected (collection volume swings 13-to-80 with fetcher health). NULL mention day = gap AND out of the denominator, never zero (ADR-0066); zero-total day yields no points (0/0 is not a share). The plot is `TrendPlot` IMPORTED from NarrativeTrends -- prop widened to a structural `TrendSeries`, one geometry for both boards (ADR-0064) -- on the same validated five slots, exported not re-declared; themes past the fifth live in the figures table with no dot. No backfill: Brave is a recency ranking, a reconstructed history would be today's survivors wearing past dates (ADR-0144) | accepted | 2026-07-28 |
| [0146](0146-a-detector-and-a-comparison-are-different-charts.md) | A detector and a comparison are different charts -- the two attention boards rendered identically, and the narrative board's top-5-loudest lines charted the trajectories of `earnings`/`price`/`q2` (the register, ADR-0142) when its question is *"is anything accelerating that nothing watches?"* -- a STATE question. Themes keep their lines (fixed membership, named entities: comparison is what lines do). Narratives become a **share x velocity detection plane**: covered phrases hollow context, uncovered the filled payload (state is the encoding, so no categorical slot is spent), `emerging` ringed off the backend's own status so no threshold constant gets a second home (ADR-0064). **A phrase with unmeasurable velocity is NEVER plotted at y=0** -- it sits in a labelled rug positioned by its share, count printed, overflow counted; today the whole board is a rug and an empty plane that says why, which is ADR-0066 drawn rather than written. Trajectories move to own-scale sparklines in the table. An `AttentionFunnel` strip between the boards states the lifecycle as counts (tracked -> unwatched -> emerging -> anchors, "--" for emerging while unmeasurable), from one exported function. Rejected: threshold guide-line (copies VELOCITY_MATERIAL to a second home), y=0 plotting (324 claimed measurements that never happened), Sankey (five numbers say it in a sentence) | accepted | 2026-07-28 |
| [0147](0147-the-mandate-is-a-parameter-not-an-ambient-fact.md) | The mandate is a parameter, not an ambient fact -- `/book` claims "$100M", "$38.1M held in cash" and "the US geography cap is binding", and there was nowhere a reader could learn what the mandate WAS. Nine constraints across five modules, mirrored by hand into TypeScript under a comment asking the next person to keep them in step. The comment failed twice: single-name and geo shipped **transposed** (0.35/0.20, judging every position against the wrong ceiling in both directions), and `gross_exposure_pct` read **2.0** against an optimizer enforcing `max_gross = 1.0` since ADR-0037 -- a published limit permitting leverage the sizer cannot produce (ADR-0123). Two more from the same cause: riskBoard looked the caps up in `scoring_config` rows that were **never seeded** (all ten limits silently `house_default`), and `net_exposure_pct`/`beta_abs` existed only in TypeScript with nothing in the backend constraining either. `mandate.py` becomes the single source; `OptimizerConstraints.from_mandate()` makes sizing **f(signal, mandate)** so one sizer serves the nightly run, a re-size under a different capital base, and a caller-supplied mandate over MCP without being able to disagree; migration 054 seeds the values (ADR-0037: "an operator decision, not an implementation one"); `Mandate.sources` carries per-field provenance because the honest answer differs per field; enforced vs monitored are **separate types**, a different promise rather than a label; `mandate-drift.test.ts` parses the Python and fails on disagreement (ADR-0100's mechanism -- a comment is not one). Deletes `TradeDerivationDrawer`, unreferenced and describing the pre-ADR-0037 allocator (">= 3 members", excess "redistributed... then normalised"), every line wrong. **No browser control may set it** (goal 5): the book is built at 21:30 UTC, so a cap changed at 14:00 changes nothing on screen -- the panel says so in words rather than implying it by the absence of a button | accepted | 2026-07-29 |
| [0148](0148-the-signal-is-published-separately-from-the-book.md) | The signal is published separately from the book -- every consumer surface (`/book`, all ten MCP tools) emitted only the SIZED output, denominated in one hypothetical fund's $100M and 20/30/35, so a portfolio app running its own capital base could consume none of it. The one number it needs, `conviction = |EdgeScore| / vol`, was not a first-class output at all -- it appeared only as a column inside a sizing derivation. `q1_agent` already had the right shape (`reason_picks` -> `verify_citations` -> `size_positions`); what was missing is that the intermediate was never persisted. `extract_signal` is called BETWEEN the retry loop and sizing -- the one point where picks are final and nothing has sized them -- which makes the mandate-free claim structural rather than a promise: there are no weights in scope to leak. One row per NAME (no column could hold a weight), `conviction` carried because a ratio is identical at $100M and $5bn, sector/geo carried because they are INSTRUMENT identity a consumer's own sector cap needs (from ASSETS, so no second copy -- ADR-0121/0125). The guarantee is an ABSENCE and absences are invisible in review, so `FORBIDDEN_FIELDS` is asserted against a deliberately hostile input: a fully sized book handed to the extractor. Scores persist None, never 0.0 (ADR-0066). New MCP tools `signal` and `size_book` | accepted | 2026-07-29 |
| [0149](0149-request-time-sizing-calls-the-real-optimizer.md) | Request-time sizing calls the real optimizer -- a caller should size today's signal under THEIR mandate, and `optimizer.py` is cvxpy with no browser path to it. A TypeScript reimplementation is refused on the record: ADR-0107 documents that the source it was read across from clips every weight at zero regardless of its own `long_only` flag and renormalises to sum 1, which on a long-short book DELETES EVERY SHORT -- and a second sizer would be found wrong by a reader noticing the workbench disagreeing with the book, the worst possible discoverer. So: `compute_api.py` holds all logic and is testable with no Vercel/network/deploy, `api/compute/size.py` is a thin shim, namespaced under `/api/compute/*` because Next.js owns `/api/chat` and `/api/mcp`. Verified over real HTTP: default mandate binds the 20% cap, a $500M/8% caller mandate binds at 8% and returns $40M notionals -- capital scales notionals and leaves weights untouched, which IS the proof a mandate is a parameter. Refusals are refusals not clamps (`max_gross: 99` -> 400 with the bound). A name with no EdgeScore enters at mu=0 and is NAMED: "no view", not a measured zero, holdable for variance reduction only (ADR-0108's refusal, said out loud). Absent covariance falls back to a diagonal AND says it assumed zero correlation. Rate limit fails OPEN where `/ask` fails closed -- `/ask`'s budget is tomorrow's book, this holds no quota. Widens CLAUDE.md's "no backend service" explicitly. NOT YET DEPLOYED: first deploy must reproduce published weights bit-for-bit | accepted | 2026-07-29 |
| [0150](0150-a-recommendation-has-no-pnl.md) | A recommendation has no P&L -- `compute_daily_return` sums weight x price return and subtracts nothing, on a book whose mean one-way turnover is **95.1% per run** (200% between 07-22 and 07-23). Priced with this repo's OWN cost model: published series **+0.758%** (latest row, not the series peak), held book net of costs **-0.724%**, a **1.48pp overstatement -- the sign flips**; $855,975 paid on $100M; ~19%/yr at 51% turnover, ~29%/yr at 77%. `rebalance_cost` existed but priced the WRONG delta (conviction book -> published book, WITHIN a run). So a book that is HELD is carried forward, moved toward each recommendation, charged for the move, with a compounding NAV. Return is earned on the book held THROUGH the period and cost charged for moving off it -- computing it on the new book is the look-ahead that makes a strategy appear to time its own trades. `tracking_error` is ZERO because a turnover budget's value would have to come from somewhere and `max_turnover` has existed since ADR-0107 with nothing ever setting it; inventing one is fitting a parameter to an aesthetic (ADR-0047), so it is stored at zero rather than faked. Does NOT touch `portfolio_returns` -- ADR-0112's rule. Signed weights, because +10% -> -6% is 16% of turnover not 4% (ADR-0101). Missing price -> None, not a flat day | accepted | 2026-07-29 |
| [0151](0151-the-portfolio-is-not-the-book.md) | The portfolio is not the book -- ADR-0150 built a book that is HELD (carried across runs, charged for its own trading, compounding NAV) and it rendered as ONE PANEL low on `/risk`, while `/portfolio` -- the URL a person types to find what is held -- was still `redirect("/book")`. Asked "where is the portfolio?", the honest answer was "buried, and the URL you'd guess redirects away from it". ADR-0025 retired that route for a correct reason -- three pages rendering the SAME ten positions -- and the premise is now false: `/book` is `research_recommendations.picks` with no returns of its own, `/portfolio` is `book_holdings` with a cost-netted series, and on the live history they disagree BY MORE THAN THE RETURN (+0.76% vs -0.72%). Two pages that disagree about what you own are not duplicates. Fifth nav destination, permitted by the non-goal's own text ("it was never a count of URLs") and recorded in design-goals rather than absorbed. `/trades` and `/research` stay redirects -- they ARE still duplicate views. Two bugs surfaced that no test caught: `extend_held_book` upserted without deleting, so a name that left the book kept its row and 2026-07-28 held **13 rows for a 9-name portfolio** after the pipeline re-ran; and a `1e-9` drift threshold against a float32 `REAL` column reported a fully-rebalanced portfolio as drifted on all nine names (now `1e-6`, matching WEIGHT_DUST -- ADR-0068 recurring) | accepted | 2026-07-29 |
| [0152](0152-a-true-page-nobody-asked-for.md) | A true page nobody asked for -- reverses ADR-0151 the same day. Every step of 0151's reasoning is correct (the HELD book is not the duplicate ADR-0025 retired) and the conclusion is still wrong, because it answers a question the brief does not ask. `task.md` Q1 is "what are your top five long and short trades, and why" -- the deliverable is TEN TRADES WITH REASONS, and the $100M is scale framing, not a mandate to run money. Neither question asks what the book EARNED. A page reporting NAV also makes "what is your track record?" the obvious next question, which six sessions cannot answer and which ADR-0090/0112 already refuse to claim. **A page can be true, well-built, and still a cost** -- it takes attention to arrive at and rule out. Route returns to a redirect, nav returns to four. KEPT, because it fixes a defect that predates the page: `book_holdings`, the nightly extension, and `CostDrag` on `/risk` -- the realised curve was gross of costs on a book turning over 95.1% per run, and showing +0.76% published against -0.72% held is owed to a reader whatever the brief asks. Sharpens the bar for a fifth destination from "a different object" to "a different object THE BRIEF ASKS FOR". 0151 left in place, accepted-then-reversed | accepted | 2026-07-29 |
| [0153](0153-two-corpora-two-series.md) | Two corpora, two series, never merged -- the discovery layer held **358 phrases, one run_date, velocity on ZERO** and structurally never could have fired. The history was already being collected and thrown away: `market_news` had 462 GDELT documents across 41 days (ADR-0144 added GDELT for exactly that) and the nightly job computed ONE snapshot and discarded the other 40. Replaying them forced the corpus question. Measured: Brave gives 87-94 docs/day inside its 8-day window and **zero before it**; GDELT gives ~11/day across 41 days. A COMBINED series is dense and carries a 5-10x denominator discontinuity at 2026-07-21 (ADR-0141 one level deeper); an ARCHIVE-only series is comparable but at ~11 docs/day `MIN_DOC_COUNT=3` demands a phrase appear in 27% of the day, so the survivors are `prices`/`us`/`global` -- register, not narrative (ADR-0142). **Not a choice: they answer different questions.** A snapshot compares nothing across time so composition drift costs it nothing; a velocity is nothing BUT a comparison so drift is fatal. Both computed, each labelled, never sharing a comparison -- the pattern of the four VaRs (ADR-0082) and the three accountability instruments (ADR-0112). NOT one row with a dense share and an archive velocity: that velocity would not be the velocity OF that share. `load_history` filters by corpus and `track_narratives` threads it through read AND write -- without it every velocity is an artefact and **nothing would look wrong**, so a test asserts the filter and was verified to fail when removed. Result: 16 velocities where there were 0, first measurable 2026-06-22. Rejected: raising the GDELT budget (measured -- 1 query 232 docs, 4 -> 462, 10 -> the same 462; the limiter is coverage and HTTP 429, not the clock; committed then reverted same day), lowering MIN_DOC_COUNT (manufactures findings). **Still 0 emerging** -- the mechanism is unblocked, the corpus is still too thin to say anything, and those are different claims | accepted | 2026-07-29 |
| [0154](0154-maxrecords-caps-a-response-not-a-query.md) | `maxrecords` caps a RESPONSE, not a query -- the archive looked sparse and was TRUNCATED. One request spanning 45 days returns 250 articles for the WHOLE window (~5.5/day); the same query over 7 days also returns 250 (34/day). Both at the cap, so the remainder was never missing from GDELT -- it was discarded before reaching us. Six times the density from asking for less at a time, which is the difference between `MIN_DOC_COUNT = 3` being a **27%** share of the day (only newswire register clears it: `prices`, `us`, `global`) and **9%**. Windows run OLDEST-FIRST so a truncated fetch loses the recent end, which Brave already covers at 87-94/day, rather than tearing a hole in the only history there is. Budget 200 -> 600s, and the distinction matters more than the number: it was raised to 420s hours earlier on an INFERENCE (ten queries at ~30s) which the measurement falsified (1 query -> 232, 4 -> 462, 10 -> the same 462) and I reverted; windowing multiplies the request count for real, ~7 per query. Two bugs introduced by the loop and caught by tests that predate it: a query admitted by the outer budget check could fetch NOTHING (inner check ran before its first window), and a permanently-rejected query was retried once per window -- 7 doomed requests re-learning what the first said, ADR's no-pointless-retry rule reintroduced one level up. Does NOT fix corpus DIVERSITY: ten broad-market queries overlap however finely windowed, and widening the seed set edges back toward naming themes (ADR-0141's line). Rejected: a different provider (premature -- not sparse, truncated), raising maxrecords (250 is GDELT's ceiling), daily windows (450 requests, 45 min, on a 10-min job -- revisit if 7d is still thin) | accepted | 2026-07-29 |
| [0155](0155-a-share-is-only-comparable-to-a-share-of-the-same-corpus.md) | A share is only comparable to a share of the same corpus -- ADR-0154's windowing raised `corpus_size` from a median ~20-60 to **868 in one day**, so every phrase's denominator grew ~15x while its numerator did not and **all 34 measured velocities came back negative together**. Not a finding about the market; the signature of a denominator change. ADR-0153 had already stated the rule ("a series has to be counted out of the same kind of corpus every day") while guarding the PROVIDER mix; corpus VOLUME breaks the same rule through a door nobody watched, because volume was assumed to be a property of the world rather than of our fetch config. Three fixes, one family -- a constant calibrated against a corpus that no longer exists. **`MAX_CORPUS_SIZE_RATIO = 2.5`**: beyond it velocity is `None` with a reason, NOT a correction factor (we do not know what the share would have been, and inventing one is worse than declining); 2.5x each way is deliberately loose so an ordinary day is not suppressed. **`floor = max(MIN_DOC_COUNT, ceil(n_docs * MIN_DOC_SHARE))`**: 3 documents meant 27% of an 11-doc day and 0.35% of an 868-doc day, and neither an absolute count nor a share alone survives an 80-fold corpus change. **Persist loudest AND movers**: top-150 by SHARE discards exactly what the detector is for, since an emerging narrative is by definition quiet and accelerating -- 736 phrases on the day, 586 dropped, **0 emerging reported on a day with 34 velocities**. The guard fired on its first live run (1447 docs vs median 55 = 26.3x, velocity withheld) and a day with no velocities is a worse-looking board and a more honest one. After the fix: 141 positive / 132 negative, 28 emerging -- a detector returning one sign for every input is measuring itself. Withheld days are NOT recoverable, which argues for changing fetch config rarely and never silently. Rejected: normalising share by corpus size (that IS share), rescaling history (assumes the missing docs distribute like the observed ones, which a 6x deeper oldest-first fetch invalidates), raising MIN_DOC_COUNT (fixes today, breaks on the next shrink) | accepted | 2026-07-29 |
| [0156](0156-a-failed-feed-is-not-a-quiet-market.md) | A failed feed is not a quiet market -- the Brave quota died mid-run (HTTP 402, spend 4.99/4.99), the pipeline **reported success**, and three themes that collected ZERO documents were published with an identical `hype_score = 28.5714`: `100 x (0.20x0.5 + 0.20x0.5) / 0.70`, made entirely of two neutral defaults (sentiment 0.5 with no text to score, momentum 0.5 with no series) scaled UP by the missing-correlation renormalisation ADR-0036 added to prevent a different silent zero. `theme_news` fell 375 articles/9 themes -> 223/6 and `mention_count_1d` went 28,15,9,8,6,6,2,2,1 -> 2,1,0,0,0,0,0,0,0, masked ~a week by the 7-day average. **The information to prevent it already existed**: `call_brave_mcp.js` exits 1 with "Brave API returned status 402" on stderr and `brave_client` discarded both, returning `[]` under a docstring that called it production policy -- "a failed feed returns [] rather than fabricating" -- when `[]` IS the fabrication downstream, asserting a claim about the market rather than about the feed. Fix: `ProviderUnavailable` raised on non-zero exit carrying the upstream reason; `returncode == 0` with an empty list still returns `[]`, because a quiet theme is a real measurement and must stay scoreable; caught PER THEME so a mid-run outage does not discard the six themes that were fully measurable; `hype_score = None`, `trade_score = None`, and **all four sub-scores NULL** together, since persisting sentiment 0.5 beside a null score publishes the same fiction in four columns. NULL not 0.0 (ADR-0066) -- 0.0 ranks, charts, and reads as "nothing going on". The frontend guard already existed (`unscored-theme-is-not-zero.test.tsx`, written for a theme created between runs) and now covers provider failure too. **1337 tests passed throughout**: every unit was correct and the defect was in what two correct units meant composed, so the new tests pin the WIRING (that `build_theme_signals` actually sets `feed_error`) and both directions of the distinction. Does NOT fix the root cause, only its silence -- the corpus is still assembled from query-shaped fetches whose volume and composition we do not control. Rejected: aborting the run (turns a partial outage total), carrying yesterday's score forward (a quieter version of the same lie under today's run_date), fixing `call_brave_mcp.js` (it was already correct) | accepted | 2026-07-29 |
| [0157](0157-a-corpus-is-defined-by-naming-its-providers.md) | A corpus is defined by naming its providers -- RSS added as a THIRD news provider, and `load_market_corpus`'s default stops meaning "every row in `market_news`". Under the old rule adding a provider is an **INSERT**, so a new fetcher redefines the denominator of every share in the combined series with no scoring change and nothing to review: ADR-0155's failure through the door ADR-0155 did not guard -- fetch BREADTH, not depth -- and the 2.5x size guard would not even fire (383 docs onto ~1840 is 21%, inside the band). Measured against the real alternatives on the repo's own queries: **RSS 388/388 = 100% dated, no key, no host**; SearXNG news 84%, general **0%**, and it needs a host. `pubDate` is REQUIRED by the RSS 2.0 spec and emitted by the publisher, where every search-derived option infers a date from a result page -- and publication date is what buckets a document into a day, so a provider that guesses it carries neither a share series (ADR-0155) nor a `mention_count_1d`. Self-hosted metasearch was measured properly and **two of the three objections against it were wrong** (dates present at 84% and slug-agreeing 8/8; composition stable at 92.6-93.6% Jaccard); what held is that it is MORE recency-skewed than the provider ADR-0144 already rejected for that (52.1% of a 45-day window in the last 7d vs Brave 48%, GDELT 18%; 28 of 45 days vs 40), one engine carried **200 of 250** results so a single block collapses the corpus, and public instances 429/403 on the FIRST request. **RSS ships SHADOW**: absent from `COMBINED_SOURCES`, collected and tagged, read by nothing -- because the corroboration ADR-0094 asks for cannot be measured before two providers have run side by side, and switching it on immediately would move every share on the same day the data to justify it starts existing. NOT a Brave replacement: three of nine themes match **zero** RSS headlines (China Growth, US Dollar, Corporate Credit), because a general pool cannot be interrogated per theme. NOT an archive: a feed holds 10-50 recent items, no backfill. Undated items are DROPPED not stamped (ADR-0066), and a dead feed is REPORTED not absorbed (`all_failed` separates "quiet" from "no network"). First live run: 14/14 feeds OK and **0 items parsed**, no error anywhere -- an ElementTree Element with no subelements is FALSY, so `find("title") or find("atom:title")` discarded every title in every feed; silent emptiness reproduced inside the fix for silent emptiness, now pinned. Rejected: switching RSS on immediately, routing `theme_news` through RSS to stop paying Brave (three themes unscoreable), self-hosting, a separate shadow table (the explicit provider list is the general fix; a table would have fixed RSS only) | accepted | 2026-07-29 |
| [0158](0158-a-day-is-when-it-was-published-not-when-we-fetched-it.md) | A day is when it was PUBLISHED, not when we fetched it -- after ADR-0155 the archive series measured again (239 velocities, 28 emerging) but its **live end was blank**: 2026-07-29 held 128 phrases and velocity on **zero**, because that day's corpus read 1447 against a history median of 55.5. `market_news` carries `run_date` (fetched) AND `published_date` (dated), deliberately distinct so one fetch can deepen forty days -- and the two consumers disagreed about which defines a day. The backfill bucketed by `published_date`; the nightly job selected on `run_date`. One GDELT call returns 45 days of history, so all 1449 articles carry today's `run_date`: measured, they spanned **32 publication days** (06-14 to 07-26). The live job compared "everything fetched this week, published across five weeks" against "documents published on one day" -- same phrase, same provider, same depth, incomparable denominators. **A third door**: ADR-0153 guarded the provider MIX, ADR-0155 corpus VOLUME, ADR-0157 the provider LIST, and none could catch this because none of those had changed. The series was incomparable **with itself**. Fixing the bucketing exposed the layer underneath: replayed by publication date the newest days hold **9-16 documents against a median of 49**, so the guard withholds them for being too SMALL. ADR-0154 ordered GDELT windows oldest-first on the premise that "the live Brave corpus already covers the recent end densely" -- but the archive is **GDELT-only by definition** (ADR-0153), so Brave covers none of it and its recent end was never covered by anything; the run logged "9 of 10 queries not run to completion" and the days it missed were exactly the ones a daily job exists to add. Decision: publication-date bucketing everywhere; **one** implementation (`replay_archive`, called by both the script and the nightly job, since a backfill and a live extension bucketing differently is how a series becomes incomparable with itself); replay rather than snapshot, because later fetches fill in older dates and a forward-only snapshot can never incorporate them; and GDELT fetches the **newest window first**, then the rest oldest-first -- truncation now costs MIDDLE days, which are recoverable because GDELT is an archive, where the live end is not (tomorrow it is no longer the live end). `combined` KEEPS `run_date` bucketing and that is correct: ADR-0153 already says a snapshot compares nothing across time so drift costs it nothing, while a velocity is nothing but a comparison. Rejected: widening MAX_CORPUS_SIZE_RATIO to admit 26x (the guard was right), bucketing the backfill by run_date instead (makes every historical day mean "whatever we happened to fetch", changing retroactively on every re-fetch), waiting for the median to catch up (~15 runs of a blank latest day, converging on the wrong definition), pure newest-first (under sustained truncation it never reaches the old end -- the hole ADR-0154 protects) | accepted | 2026-07-29 |
| [0159](0159-the-plane-read-one-day-and-it-was-the-wrong-one.md) | The plane read ONE day and it was the wrong one -- after ADR-0158 the archive held **381 measured velocities across 35 days** and the detection plane on `/` was still blank, printing "No measurable velocities yet". `toSeries` derives `latestDate = max(run_date)` and EVERY consumer keys off it; the plane plots `latest.velocity`. **The newest day is structurally the least measurable**: GDELT publishes with a lag so the most recent publication day is always the thinnest when the job runs, and a thin day is exactly what ADR-0155's guard withholds a velocity for. Two correct behaviours compounding -- the guard rightly refuses a thin day, the board only ever looks at the day most likely to be thin -- and the result was a blank chart above a full series. Fix: `latestMeasuredDate(rows)` returns the newest run_date carrying any velocity; `toSeries(rows, asOf?)` takes an optional day; the fallback engages ONLY when the newest day is unmeasurable, so a healthy day is bit-identical. **Both coordinates move together** -- the plane is share x velocity, and a share from today against a velocity from Tuesday is not a point on any plane -- and the date is stated in the header and a note, because a chart labelled "run 2026-07-29" while plotting 07-27 is a worse lie than the blank it replaces. Weakens NOTHING in ADR-0155: no velocity invented, no threshold relaxed, a withheld day stays withheld; only WHICH day the view is drawn from changes, and never silently. `AttentionFunnel` and the uncovered count deliberately keep reading the newest run -- "what did today's corpus look like" is not the plane's question. Third distinct surfacing of one bug class in a day (ADR-0155 corpus volume, ADR-0158 the date field, this the view's anchor date); the common root is that a share is a fraction of a corpus and every part of the system must agree on which corpus and which day, and two of the three were invisible in the output. Rejected: plotting velocity as a time series (a better board, but a different question), relaxing the guard to populate the chart (how 28.5714 happened -- ADR-0156), changing `toSeries`' DEFAULT (moves AttentionFunnel off today without anyone asking), leaving it because ADR-0158's newest-first fetch made the live day measurable again (it did -- 11 docs to 109, 0 velocities to 37 -- but that fixed the SUPPLY, not the board's dependence on one day) | accepted | 2026-07-29 |
| [0160](0160-a-stall-is-retried-once-and-the-log-says-so.md) | A stall is retried once, and the log says so (**amends ADR-0052**) -- L5 took 22 min against a median of 3.5, and "is that normal?" was unanswerable from anything recorded. **ADR-0052's guard was defeated one level up**: `reason_picks` refuses to retry its own timeout (3 x 900s = 2700s, a 45-min stalled run being what was measured), but a timeout sets `state["error"]` and `run_q1_agent`'s loop retried on ANY error -- the live log printed "not retrying; a stall is not fixed by asking again" and "Retrying reason_picks (attempt 2/3)" on **consecutive lines**. Neither line wrong about its own function; together, a system doing the opposite of what it says. Nothing had ever driven that loop (ten args, Supabase persistence), which is why a contradiction this plain survived a green suite. **ADR-0052 was right about the cost and wrong about the remedy**: the same shape appears at TWO ceilings -- the module comment records "timed out at 420 on attempt 1 and succeeded on attempt 2", and 2026-07-29 timed out at 900 and succeeded on attempt 2 with a real ten-pick book, 45 citations, 93% grounded. Raising the ceiling did not stop attempt 1 stalling; **the retry is what rescues the book**, and enforcing ADR-0052 literally would have published an empty-thesis fallback. `MAX_TIMEOUT_RETRIES = 1` vs `MAX_REASON_RETRIES = 2`: two deadlines, never three. A CITATION failure keeps its full budget -- the model can produce better output for the same prompt, the case retrying was designed for. Loop **extracted** to `reason_and_verify_with_retries` so the budget is testable without a ten-arg fixture; reverting the constant fails 4 tests including the one counting actual deadlines, **verified not assumed**. Plus per-node timings, because `pipeline_runs` stored only the whole-stage duration so "several legitimate calls" and "one burned deadline plus a retry" -- opposite fixes -- were indistinguishable. Plus line-buffered stdout: Python block-buffers when not a TTY, which is every way this runs, and a redirected run showed **21 lines for 45 minutes** while L0-L5 completed behind them, making a hung job indistinguishable from a slow one. Timeout value deliberately UNCHANGED -- two ceilings failed identically and a third guess is the inference ADR-0154 records making and reverting. Rejected: zero retries (would have published a fallback), raising the ceiling again, shortening only the first attempt (attractive, but n=2 and risks killing legitimately slow generations -- the 120s mistake), testing run_q1_agent end to end | accepted | 2026-07-29 |
| [0161](0161-a-corpus-the-board-could-not-see.md) | A corpus the board could not see, and a source that feeds nothing -- a **fourth** Stitch archive triaged against `design-goals.md` failed the checklist at steps 1-4 like the three before it (`EXECUTE REBALANCE`, `42.4 GB/s`, scenario probabilities the six calibrated shocks do not carry, per-position unrealised P/L that ADR-0152 retired four hours earlier, green reused for `CONNECTED`/`CALIBRATED`/`NOMINAL`). Its data-health screen asked one keeper: **per news provider, how fresh, and is it feeding anything?** Our own board could not say. ADR-0105's `SOURCES` named **six** and `market_news` was not among them -- the un-themed L1b corpus behind `NarrativeTrends` and ADR-0143's price-link gate -- with **three** providers writing to it: gdelt 2,309 rows over 42 days, brave_market 667 over 9, rss 405 over 84. The page whose whole job is naming what the book rests on was silent about all three. **And the naive fix introduces a quieter defect**: listing `rss` with 405 fresh rows as `CURRENT` asserts it feeds the book, when ADR-0157 deliberately holds it OUT of `COMBINED_SOURCES` -- read by nothing until there is parallel history to test it against Brave. `current` would be a false claim made by the board that exists to prevent false claims. Decision: **three rows, never one** (merging asserts one shape where ADR-0144 measured three -- Brave a recency RANKING at 48% of a 45-day window in the last 7d, GDELT an ARCHIVE at 18% and 40 of 45 days; `brave` and `brave_market` also stay split, same provider asked a different question into a different corpus); a **sixth verdict `shadow`**, checked BEFORE freshness because "how old is it" is the wrong first question about a source nothing reads, with the age still in the note so declaring it hides no fact, and declared on the SPEC rather than inferred from rows because "nothing reads this" is a decision in `COMBINED_SOURCES`, not a property of the data -- design goal 2's rule applied to an enum rather than a boolean, the same split that gave `stale` its `unjudgeable`; `observabilitySummary` counts what is **read**, so shadow is excluded from `contributing` and stated separately (else a corpus nothing reads inflates the one figure answering "what is this built on" -- 8 of 9 rather than the true 7); and a **keyless provider is never `unconfigured`** -- GDELT and RSS need no credential so zero rows can only mean a failed feed, and Brave demonstrably HAS a key here, so all three degrade to `silent` per ADR-0156 rather than sending a reader hunting a secret that does not exist. Denominator moves **6 to 9**; the old sentence was not wrong about the six, it was wrong about being a complete list. Turning RSS on is now a one-line diff that visibly moves the headline -- the right friction for a decision ADR-0157 wants taken deliberately. Also rendered `/ask`'s **remaining-questions figure**, which the route computed and the Turn type carried and nothing read for the whole life of the feature: a COUNT, never the comp's `Spend: 14%` meter, because `chat_usage` stores questions and a percentage implies a cost gauge that does not exist; null is an unreadable cap (the dev path), not zero. Rejected: one aggregated `market_news` row, `rss` as `current`, omitting `rss` (reproduces the bug -- a provider writing to a table the board does not name), generating the shadow list from Python, `brave_market` unconfigured on zero rows | accepted | 2026-07-29 |
| [0162](0162-a-mark-you-can-see-but-cannot-name.md) | A mark you can see but cannot name is a mark you report as absent (**amends ADR-0146**) -- a reader asked why `ai` was missing from the detection plane on `/`. It was not missing. At 15.6% share and velocity +2.27 it was the **maximum of both axes** and therefore SET the x-scale, sitting in the top-right alarm corner -- as an unlabelled 3px hollow ring, because ADR-0146 added a third distinction it never argued for on top of its two good ones: covered marks are hollow (context), uncovered are filled (payload), *and only the payload gets a direct label*. So the muted channel was also the anonymous one, and a mark a reader can see but cannot identify is one they report as absent. Same defect, quieter, one region down: the "velocity not yet measurable" rug prints its **count** and never its **members**, so `chip` -- 7.3% of headlines, 7th by share against a 5-row table, `first_seen` that day and so genuinely unmeasurable -- appeared in **no text anywhere on the board**, its `<title>` the only copy of its name. ADR-0126's rule ("a tooltip is never the only copy") had been read as being about magnitudes, never identities. Decision: **every region names its loudest marks; an encoding may not also mean anonymous.** Covered marks labelled in `--text-tertiary` against the payload's `--text-secondary`, so the split still lands twice (fill AND ink) and naming context does not promote it; **two caps not one** (6 uncovered, 4 covered -- the payload is what the board is for); **ONE collision pass over both**, since a covered label overprinting an uncovered one costs the payload exactly the legibility the split protects; **leader lines on displaced labels**, the rule `TrendPlot` already applied to its end labels, brought here because ten labels instead of six push `us` and `oil prices` 50-60px below their own dots -- and an unconnected label that far away is not a weaker label, it is a label pointing at the wrong mark, with the leader carrying the mark's payload/context ink so following one never loses the encoding; and the **rug names its loudest four with shares** beneath the strip, the count saying how many velocities are withheld and the line saying which. `chip` in the rug was never the bug (`days_observed: 1` against `MIN_DAYS_FOR_VELOCITY = 4`; ADR-0066 forbids drawing it at y = 0) -- the bug was the rug not saying so out loud. ADR-0146's fill and region encodings are untouched. Rejected: rely on the tooltip (the reader had a mouse), enlarge the hollow marks (size is not free -- it attacks the payload/context contrast), widen the table alone (names `chip`, leaves `ai` a ring -- symptom in one region, defect in neither), label all 37 measurable marks (37 labels in 152px is not a chart) | accepted | 2026-07-29 |
| [0163](0163-a-comparison-surface-over-one-fetch.md) | A comparison surface over one fetch, and two adoptions that did not survive contact -- the Stitch archive re-read **markup-first** after the owner objected that the first pass (ADR-0161) was doctrine-first: read `design-goals.md` before the screens, sort everything against it, ship two things that were bugs the comp exposed rather than designs it proposed. Reading the 3,214 lines of `code.html` skipped the first time changed the answer. **`/book2` renders `BookShell` with `variant="next"` -- one fetch, two layouts**, because two routes querying `research_recommendations` separately are two chances to describe different vintages (ADR-0040), the reason `/method` and `/method/evidence` share one `MethodBody` (ADR-0084). Body moved to `components/book/BookBody.tsx`: a Next route module may export only `default` plus config names, so the plan's smaller-diff idea of a named export from `page.tsx` **fails the generated route types** -- the same move `PositionRow` already made. **Adopted:** thesis beside counter-thesis (we persist both halves and stack them, with `Sizing` beside the thesis, so the argument and its rebuttal are never read together -- a two-card swap); and **master-detail**, which beyond the comp's argument fixes inline expansion pushing only ONE column down and desynchronising Longs/Shorts. One component, three `chrome` modes (full|header|detail) rather than a second detail component that would recompute sixty lines of derivation. NOT the comp's right-hand sidebar -- arithmetic: its list is one column, ours is two, and a third leaves each table ~440px against `BOOK_ROW_MIN_W` 640 so every row opens a horizontal scroller; not sticky either, so no inner scroller exists. **Two REJECTED on measurement, which is the finding.** Visible `[FRED]` source tokens: built, then measured **0 of 10 live theses contain a `{N}` marker** -- ADR-0010's inline mechanism has never been exercised, L5 emits no anchors, the 42 citations are a flat footnote list and the model hand-writes source IDs into prose; a backend change, not a frontend one. Source-activity sparkline: rendered `brave_market 61 to 9 docs/day`, and it was an **artifact** -- **PostgREST caps a response at 1000 rows regardless of `.limit(4000)`**, so ordering by `published_date desc` drew a ~6-day truncation window with partial boundary days against real histories of gdelt **42** days, rss **84**, brave_market **9**. The same window-boundary-manufactures-a-shape failure as ADR-0155/0158/0159; needs server-side aggregation and must handle GDELT's lag making the newest day thinnest. **Correction kept in the record:** the panel was first reported as having two empty cells and a stale comment, and a five-card reshuffle was built on that -- both wrong, `Sizing` and `Factor exposure` carry no row/col and grid **auto-flow** fills exactly those cells, so the grid was always full and the comment accurate; grepping explicit `row-start` classes cannot see auto-placement. Rejected: a full `/risk2`+`/method2` set (near-identical copies, no adoptions to compare), a `?variant=` param or `next.config.js` rewrite (a second mechanism for a job the repo already solves with a shared component), an in-page toggle (a control whose only purpose is evaluation), and keeping the sparkline with a "6 days shown" caption (accurate about the drawing, misleading about the source) | accepted | 2026-07-29 |
| [0164](0164-the-accent-and-the-short-ink-were-the-same-colour.md) | The accent and the SHORT ink were the same colour, for about 6% of readers -- `--accent` goes crimson-pink `#c50c3e` to teal `#00687a`, and **the reason is separation, not contrast**. Both clear AA everywhere; the teal simply has more headroom (plain 5.43 to 5.83, over its own 10% tint 4.58 to 5.04). The deciding axis is one nothing in the repo measured: `chip-contrast.test.ts` enforces contrast between a foreground and its BACKGROUND, which is a different question from separation between two FOREGROUNDS that mean different things. Simulated (Machado 2009, severity 1.0), the old accent sat **dE 7.7 protan / 8.6 deutan from `--short`** -- below the ~10 floor where two colours stop being separable -- and 11.4 protan from `--long`. Protanopia and deuteranopia together are ~6% of males, so for them the hyperlink **was** the SHORT ink. `design-goals.md` goal 3 ("a proposal that colours `LONG` the same as a hyperlink has destroyed a semantic") was written to police incoming mockups and had never been pointed at the shipping palette; it was being violated from the inside, against `--short` rather than the `--long` it names. Teal clears 25 against both inks under both common deficiencies. The token's own comment had flagged the tension -- "only 8 degrees of hue from `--short` ... every further shade spent on contrast margin narrows it" -- after two darkenings bought for `badge-tier-anchor`; two floors were converging on one token and only one of them had a test. **Not pushed bluer:** `#0f6490` scores better against `--long` (protan 40.2) but the accent's distance to `--series-1 #2a78d6` falls 48.4 to 31.3 to 20.7 on the way, moving the collision onto the trends board rather than removing it. Also measured and outside the chip test: the **focus ring**, a non-text indicator under WCAG 1.4.11 at a 3:1 floor, because a ring is not a chip. **Regression accepted:** dE **4.6** from `--long` under tritanopia (was 110.7) -- a ~0.01% collision traded for a ~6% one, survivable only because ADR-0085 already puts a glyph or wordmark on every direction, which couples the two decisions. Deliberately **adds no test**: a separation floor needs an argued threshold and an adjacency rule, and pinning one unargued would be worse than the gap. Two token sites move together (Tailwind compiles to literal RGB and never reads the `:root` var); `accent-dim` re-derived for the tint-drift assertion. Before/after built from `HEAD~1` in a detached worktree rather than a CSS override, so the "before" is a real build | accepted | 2026-07-29 |
| [0165](0165-a-layout-gate-belongs-to-the-thing-it-gates.md) | A layout gate belongs to the thing it gates -- both attention boards on `/` were already built as plot ‖ figures (`2fr/1fr`, table right), and the layout **had never been seen**. Both were gated at `wide` (1424px), which is not a desktop gate: ADR-0084 derived it from `BOOK_ROW_MIN_W` and ADR-0086 pinned it at 1424-not-1440 for the **SideRail**'s scrollbar. Borrowing it priced these cards at a rail's breakpoint rather than at their own table's width, and a 1920x1200 screen at Windows 11's default **150% scaling reports 1280 CSS px** -- ~1265 after Chrome's classic scrollbar -- so the split stacked on the majority desktop case. Retargeting at `lg` (1024) is wrong and measurement says so: the SEVEN-column narrative table wants 388px against a 297px column, a 91px scroller, which is what the original comment feared and answered with someone else's constant. `xl` (1280) is wrong for the reason ADR-0086 already wrote down and this change nearly walked past twice -- a media query excludes the scrollbar, so the very window that motivated the fix reports 1265 and an `xl` gate would be dead on it. Decision: a **second breakpoint `figures: 1248px`**, a deliberate departure from ADR-0086's "one gate, one derivation, no second constant to drift" -- that rule guards against two gates for the SAME concern; these are two concerns, and collapsing them is precisely what caused the bug. **Derived from measured min-content** (wrapper to 1px, read `scrollWidth`), not estimated: narrative table 388px at `pr-3`, **364px at `pr-2`**, theme table 249px, figures column 371.7px at viewport 1248. `pr-3` -> `pr-2` on the narrative table only -- six gutters x 4px = 24px, and the gutter was the ONLY slack, since every column is min-content over a real value (`established`, `AI Capex`) so the next 4px truncates a reading rather than tightening a rule. The config **forbids re-deriving the gate as `(viewport - gutter - gap)/3`**: that shorthand was tried, runs through the `lg` gutter + `TerminalPane` + card `p-4` + gap, and overstates the column by ~17px (predicted 394, browser reports 377) -- enough to place the gate where the table still scrolls. `ThemeTrends`' own stated reason for `wide` was a separate error kept in the record: end labels "on top of the lines" cannot happen, because `TrendPlot` is a viewBox and narrowing scales labels and lines by the same factor -- what narrowing costs is label SIZE, not overlap. Verified at the boundary: **1248 splits, 1247 stacks**, 1265 splits with no scroller in either table; `wide` untouched so nothing on `/book`, `/ask`, TopBar or SideRail moves. Rejected: a container query (theoretically right -- the constraint is the card, not the viewport -- but a second layout mechanism for one card costs more consistency than it buys precision), dropping columns at narrow widths (every one is a reading ADR-0126 obliges the board to carry, and `Already watched by` is the field separating "Fed Policy is working" from "nothing is watching this"), and shrinking the `w-[84px]` sparkline column (measured, not binding -- the browser compresses it to 42px before min-content) | accepted | 2026-07-29 |
| [0166](0166-an-arrow-between-two-counts-is-a-claim-of-nesting.md) | An arrow between two counts is a claim of nesting -- `AttentionFunnel` read `193 phrases tracked -> 170 watched by nothing -> — emerging -> 9 anchor themes · 1 promoted from a signal`, and the request to make it "more graphical" surfaced that the strip was already asserting something false. **A funnel's visual grammar is subset-of.** The first two stages genuinely nest (`unwatched ⊆ tracked`, same `narrative_signals` rows). The last does not: queried live, `themes.promotion_basis` (migration 051 / ADR-0134) is **8 `practitioner_prior`** -- migration 001's opening list, which predates the tracker and was never in its corpus -- **1 `operator_directed`** (AI Capex) and **0 `measured_discovery`**. The chain claimed 9 themes survived from 193 phrases; the true number through that path is **at most one**. And "promoted from a signal" overstated even that one: migration 051 records the ordering as tracker sees AI in **2 of 455 documents** -> operator names the theme -> LDA confirmation runs AFTER, which is exactly why it is `operator_directed` and not `measured_discovery`. So the obvious execution -- one proportional funnel, width ∝ count -- would have made it **worse**, since a 9-wide bar under a 193-wide bar asserts "these survived" far more loudly than a `->` does. Decision: **proportion only where the nesting is real.** Two labelled halves (`Observed · narrative_signals` / `Committed · themes`); proportional bars inside the observed half alone; the committed half drawn as **nine pips on no shared scale**, one filled, so it cannot be read as a fraction of 193; **a rule between them, never an arrow**; the break stated in prose rather than inferred; and the basis counts rendered by value, where **the zero is the informative figure** -- no theme on this board was found by the pipeline before a human named it. `practitioner_prior` is deliberately not framed as a deficiency (migration 051: a prior is a legitimate way to start a universe). ADR-0066 unchanged and still binding -- with no measurable velocity the emerging stage is a **dashed track with no width**, never a zero-length bar, which is precisely what a bar chart's default rendering of a null looks like. **The page order does NOT change, and that is part of this decision:** asked whether the Narrative card should precede all Theme cards since the funnel runs narrative -> theme, the answer is no -- reordering `/` to match a chronology that produced ONE of nine themes would encode the same false derivation in layout form, and the narrative board is shadow and sizes nothing (ADR-0128), so leading with it would rank by pipeline chronology over reader value. Rejected: the single proportional funnel (the subject of this ADR), drawing the 1 promoted theme as a sub-segment of the 9 bar (~3px at 1/193 -- the most important fact as the least visible mark), and dropping the theme half entirely (removes the claim by removing the comparison, and *"nothing here has yet produced a theme"* is a real finding about a shadow signal that is only legible with both sides shown) | accepted | 2026-07-29 |
| [0167](0167-a-card-in-a-column-cannot-be-gated-by-the-viewport.md) | A card in a column cannot be gated by the viewport (**supersedes part of ADR-0165**) -- `AttentionFunnel` moves from a full-width band to the 1fr slot **beside** `NarrativeTrends` (2fr), `ThemeTrends` staying full width below. Better placement for what the strip now is: since ADR-0166 it reads the **same corpus, same day and same ADR-0159 fallback** as the board, so it is a readout ON that board rather than a stage between two of them. It also breaks ADR-0165 **one hour after that ADR shipped** -- the `figures` (1248px) gate was derived assuming each card is as wide as the section, and a card in a 2fr column is not, but a **viewport** gate cannot see that. At viewport 1265 the gate still fires while the card behind it shrank 1132px -> 780px. The bound is provable rather than estimated, because `main` is capped at `max-w-[1400px]`: widest 2fr card ≈ 880px, 848px inside, split 2:1 leaves the figures column **276px** against `SeriesTable`'s **364px** min-content -- fitting it needs ~1780px of section, which the cap forbids. **Tried in the browser anyway** (the owner asked to see it, and the arithmetic is easy to get wrong): forcing `2fr 1fr` onto the live 780px card renders the plot at **484px** -- viewBox 720, scale 0.672, smallest axis label **6.0px** -- with the figures column at 242px against 358px needed, so the tick and in-plane phrase labels collapse into an unreadable cluster AND the table truncates mid-word (`establishe`) behind a scrollbar. **No split ratio rescues it**: the plot needs ~640px to hold its smallest label at 8px, the table 358px, total 1018px inside a card with 746px -- and the binding constraint is the **plot**, not the table, so trimming columns would not help. Decision: where a card's width is set by its column rather than the viewport, **prove the bound from the `max-w` cap and drop the gate** -- `NarrativeTrends` and `AttentionFunnel` lose their internal gates entirely and always stack (a gate that can never open, or -- as `figures:` had silently become the moment the card moved -- one that opens when it must not); `ThemeTrends` **keeps** `figures:` because it is still full width and its four-column table is 249px. Same section, two cards, two answers, which is exactly what a viewport breakpoint cannot express. **Container query declined again but on different grounds than ADR-0165**: there it was consistency cost; here it would be genuinely correct and is still refused because the cap makes every evaluation known in advance -- a mechanism whose outcome is constant is a constant with extra machinery. If the cap is ever raised this reasoning expires, which is why the arithmetic lives in both components rather than only in this ADR. Measured after: board **780px** / funnel **390px** on one row at exact 2:1, `ThemeTrends` 1186px with its split intact, no table overflow, no page scroll. Costs recorded rather than left stale: **the narrative board's table moves back under its plot**, so ADR-0165's "both boards render 754.7px plot ‖ 377.3px table" is now true only of `ThemeTrends`; `figures` is down to **one** consumer; and the funnel is much shorter than the board beside it, leaving visible whitespace, since `items-start` is deliberate and stretching a card to meet its neighbour is padding. **The standing trap this names:** moving a card into a narrower column silently invalidates any viewport gate inside it -- no error, no failing test, just a layout that is wrong at widths nobody checks -- so both components now carry the bound in a comment where the gate used to be | accepted | 2026-07-29 |
| [0168](0168-narrow-the-viewbox-not-the-rendering.md) | Narrow the viewBox, not the rendering (**supersedes a conclusion of ADR-0167**) -- the narratives section sat at **829px against 393px**, and ADR-0167 had measured why the board's plot ‖ figures split could not be restored inside a 780px card: forcing `2fr 1fr` rendered the 720-wide plane at 484px, a **0.672x scale** putting the 9px axis labels at **6.0px**. It concluded *"no split ratio rescues it -- the binding constraint is the plot"*. Right about the ratio, **wrong about the constraint**, and the error is worth naming because it is easy to repeat: it treated the plot's width as fixed and layout as able only to scale it. An SVG with a viewBox has **two** widths -- the coordinate space it draws in and the CSS box it renders into -- and only their RATIO sets type size. Keep viewBox 720 and render at 386px → 0.54x, labels 4.9px, unreadable; set viewBox **380** and render at 386px → **1.016x**, labels **9.1px**, correct. Same column, same marks. What changes is which quantity gives: scaling down shrinks the TYPE, narrowing the viewBox spends the saving on DATA SPACE. Decision: the plane draws in its own geometry (`S_WIDTH 380 / S_PLOT_LEFT 40 / S_PLOT_RIGHT 92`, smaller than TrendPlot's 148 because these labels hang off dots rather than ending full-width lines) while `TrendPlot` keeps 720 for the still-full-width `ThemeTrends`; `S_WIDTH` is set near the **narrowest** column the card will offer so the plot only ever scales UP, since below 1.0 labels shrink and above it they grow and only one direction is recoverable; **`max-w-[520px]` bounds the scale**, because a plain `w-full` renders the plane at full card width whenever the columns stack (~1134px below the `figures` gate) = **2.98x** with 9px labels at 27px, a blown-up detail crop -- capped it holds ~1.37x; the figures column is **fixed at 340px, not a fraction**, since it has a hard 319px minimum and no use for more, so fixing it hands every remaining pixel to the plane; and `SeriesTable` keeps its sparkline column dropped (358 → **319px** measured) for the 21px of headroom. Also folded in: **one reading of the series** in `lib/useNarrativeSeries.ts`, moving the corpus (ADR-0153), the last-measured-day rule (ADR-0159) and the `asOfFallback` disclosure out of both components -- the day's other bug had the funnel defaulting to `combined` while the board passed `archive`, so *"193 tracked, velocity not measurable"* sat beneath *76 tracked, velocities to +2.27*, and aligning the two call sites by hand is not a fix but a coincidence maintained by vigilance (ADR-0163's argument, one level down). Measured after: card **780px**, columns **386 ‖ 340**, scale 1.016x, no overflow, height **829 → 560px** and the gap 436 → 167. **Real cost recorded:** `S_PLOT_WIDTH` falls 528 → 248 units, so marks pack into under half the horizontal space and in-plane labels lean harder on ADR-0162's anti-collision pass and leader lines -- verified legible, and if the tracked set grows denser this breaks first, with the fix being fewer labelled marks rather than a wider viewBox. Rejected: lifting the figures into their own 1fr-column card (**built and measured first** at 565 ‖ 675 with 37px headroom, discarded on the owner's direction that the table belongs in the same card -- it also put ADR-0126's relief outside the chart's own card boundary), and a container query (unchanged from ADR-0167) | accepted | 2026-07-29 |
| [0169](0169-the-process-was-real-and-invisible.md) | The process was real, and invisible -- `task.md` Q1 is answered as a **six-phase PM workflow** (mandate → alpha → catalyst/scenario → construction → execution → attribution) and this system performs five of the six, but **never said so**. ADR-0025 ordered the four destinations "as a PM's morning" and `TopBar.tsx` repeats it; that ordering lived **only in a code comment**, so a reader landing on `/` could not see that Themes feeds Book feeds Risk, and no surface stated the sequence. Decision: give the process **one surface and one label**, leave navigation organised by object. `lib/method/phases.ts` holds the map as data (two consumers -- the map and the chips -- so written twice they drift silently, the `anchors.ts` argument); **`/method` becomes the process map** and the build chapter moves to `/method/build`; a `PhaseChip` eyebrow sits on each destination header, and `/risk` renders **two**. The map is a server component with **no Supabase query**, deliberately NOT a `MethodBody` chapter -- that component fires all twelve queries per chapter on purpose (ADR-0084), which is right for two chapters of live figures and wrong for a page with no figure to be inconsistent about (**95.8 kB vs `/method/build`'s 194 kB**). Inbound `/method#<id>` links from ADRs and PROGRESS.md still resolve: a fragment never reaches the server, so `LegacyAnchorHop` already had to hop client-side off `routeForAnchor()`, and repointing `CHAPTER_ROUTE.build` is the whole fix. **Phase 5 renders its reason, not a blank** -- per ADR-0040 the book is a recommendation nobody has paid to put on, so there is no fill, borrow cost or slippage; an empty row reads as an unfinished product, a stated boundary reads as a decision, and inventing the figures would break ADR-0025's rule 1. **Six tabs rejected, and it was the strongest alternative**: it would make the workflow unmissable, and the usual objections are weak (phase 1 is not thin if built properly; phase 5's emptiness is a statement). It fails on a third point -- **two phases land on `/risk`**, the mandate a book is measured against and the scenarios that stress it, and splitting them to satisfy the numbering breaks the risk picture apart to serve the narrative. Also rejected: a persistent stepper (implies linear progress nobody makes, permanent vertical cost), renaming the four destinations (cosmetic, loses the labels readers scan for, still does not state the sequence). Costs recorded: the **canonical** URL for a formula changed, so docs citing bare `/method` for arithmetic now land one click away; and the map is prose rather than a live query, so `phases.test.ts` asserts every anchor is an id some component actually renders -- it bounds a dead target, not a misleading description | accepted | 2026-07-30 |
| [0170](0170-the-navigation-is-the-process.md) | The navigation is the process (**supersedes the navigation decision in ADR-0169**) -- that ADR surfaced the six-phase workflow on one page and REJECTED one tab per phase; the owner directed it anyway, on the grounds that **the sequence is what this artefact demonstrates** and a numbered strip teaches it before a reader has read a word. That is a judgement about what the product is FOR, and it outranks the layout convenience the rejection defended. ADR-0169's phase map, data model and `/method` surface all stand; only its conclusion is replaced. `TopBar` and `SideRail` now render `PHASES` in order -- **01 Mandate · 02 Alpha · 03 Scenario · 04 Construction · 05 Execution · 06 Attribution** -- generated from the same array `/method` renders, so strip and map cannot drift. **Two of ADR-0169's three objections survive as COSTS and are paid rather than argued away.** `/risk` answered phases 1, 3 and 6 at once and a tab strip cannot mark three of its own tabs current, so its body moved to `components/risk/RiskBody.tsx` filtered by `phaseShows` across `/mandate`, `/scenario`, `/attribution` -- **ONE `useEffect`, one set of queries**, the MethodBody+`chapterOwns` shape, because three routes with their own fetches are three chances to describe different vintages of one run (ADR-0040 → ADR-0084). Two placements are deliberate and easy to invert: **`limits` sits with the MANDATE** (the board measures against the panel above it), and **`attribution` -- per-position risk decomposition -- sits with SCENARIO, not with the phase of the same name**, because it is ex-ante while phase 6 is ex-post and the shared word disguises two questions. `/risk` survives as a **fragment-aware hop, not a redirect**: its anchors now live on three routes and a server redirect cannot see a hash, so it forwards `/risk#stress` → `/scenario#stress` client-side (in-repo links repointed directly so they do not flash-and-hop). **`/execution` is a route with no figures on purpose** -- a sequence skipping 4 to 6 reads as a missing page; per ADR-0040 the book is a recommendation, so there is no fill, borrow cost or slippage, and inventing one would break ADR-0025's rule 1. `/method` and `/risk` leave the nav (the method chapters explain EVERY phase, so naming them as one would be false); `PhaseChip` is deleted, the numbered tab carrying identity now. **`SideRail` needed a THIRD label**: 56px, un-wideable (ADR-0086 -- `/book`'s two-pane needs the pixels), ~9 chars at 10px, and `Construction` clipped live -- `Phase.short` fixes it with a **word, not a number**, since the rail's own rule is icon AND label and "04" does not tell a reader where it goes. Costs recorded: **reading the whole risk picture is three stops rather than one scroll**, `/risk` deep links flash a skeleton before hopping, three labels per phase is a maintenance surface (bounded by a test that fails above nine characters), and six tabs overflow a narrow viewport -- the row scrolls rather than dropping a phase, because a sequence that hides its last step is worse than one that scrolls | accepted | 2026-07-30 |

| [0171](0171-a-veto-acts-forward-because-a-track-record-cannot-be-edited.md) | A veto acts forward, because a track record cannot be edited -- an outside review found four Q1 gaps; **two were factually wrong** (the portfolio thesis and the deliberate pairs both exist, render on `/book` via `ThesisBlock`, and the live 07-29 `book_view` names *"the MSFT short paired with VRT and GEV longs expresses a hardware-over-software rotation"*), one was fair: **there was no editorial judgment**. Every filter in `screen_candidates` encodes a measurable property -- HypeScore, `|EdgeScore|`, factor R², lens, complex caps -- and none can express *"the attention on this name is one news cycle, not a signal"*, which is a judgement about whether a measurement means what it appears to mean. **The obvious implementation is the one that must not be built**: `pick_outcomes` sets `entry = close on run_date` (ADR-0090) and `resolve_outcomes.py:123` re-derives its claim set from CURRENT `picks`, so editing a published book would let a substitute inherit an entry price from before the decision (a swap on 07-30 into the 07-29 book scores from 07-29's close -- one day of free hindsight) **and** silently stop resolving the removed name. That is a track record an operator could launder by editing history. Decision: `editorial_vetoes` (m058) records a refusal of `(asset, direction)` with **`reason` NOT NULL by constraint** (the `book_revisions` guard, for the same reason -- an unexplained absence is indistinguishable from a screen bug), `direction` NULL meaning both sides because *"untradeable"* and *"this SHORT is crowded"* are different claims. Acts on the NEXT run; **never mutates a published row**. Placement is load-bearing both ways: **before the cap-30** so a refused name does not occupy an LLM context slot and push a good candidate out by proxy, **after the rule filters** so the funnel reads eligibility-then-judgement and vetoing an already-dropped name cannot double-count attrition. Surfaces as a `screening_funnel` stage shaped like ADR-0046's override -- **rendered even when it removed nothing**, since a funnel listing only stages that fired implies the others do not exist -- with reasons quoted **verbatim**, because the reason IS the judgement and paraphrasing it would author a string nobody wrote (ADR-0025 rule 1). Rows are **revoked, never deleted** (`revoked_requires_reason`), so *"why is this name back?"* stays answerable; `scripts/veto.py` is the operator CLI, not a button beside a position. Also: the fetch lives in `run_q1_agent` and NOT in the node, so `screen_candidates` stays pure and testable with no credentials -- putting it in the node broke two existing tests with `KeyError: supabase_url`. Costs recorded: **a veto cannot help today's book** (say so, or wait -- that is the price of the guarantee); a read failure degrades to *no vetoes applied* rather than *refuse everything*, so a veto can silently fail to apply and the warning names the table; **determinism (ADR-0013) now has a human input** -- reproducible GIVEN the veto set, but no longer a pure function of market data; and "never deleted" is a discipline, not a SQL guarantee, used once to remove this change's own smoke-test rows. Rejected: substitution into the published book (hindsight/laundering, and it would need a per-pick `entry_date` on the one table whose job is to be un-gameable), a dissent-only overlay (honest, but records judgment without ever acting on it), and veto-by-theme (that is what the attention gate already is) | accepted | 2026-07-30 |

| [0172](0172-every-phase-answers-its-own-question-above-the-fold.md) | Every phase answers its own question above the fold -- the app was hard for a PM to decide from, and the measurement says why: **25 screens, 1,444 numerals, ZERO inner scrollers**, so goal 7 was intact and density was not the fault. Two things were: **five of six phases had no decision surface** (`/book` had one, nothing else did), and **blocks sat on the tab whose question they did not answer** -- `/scenario` asked "what proves the thesis right and what do the other paths cost" with its stress matrix at **2622px**, while `/mandate` carried *versus the benchmark* and *these weights over the past year* (both backward-looking, i.e. phase 6) and `/attribution` was the thinnest page in the app at 1.5 screens. The fix already existed on ONE page: `AnswerCards.tsx` states it in its own header -- *"the measured defect this fixes is PLACEMENT, not paint"*. Decision: extract that row to `components/AnswerRow.tsx` and give every phase one, with the contract travelling with it -- **LABEL / FIGURE / CONSEQUENCE**, consequence COMPUTED from the same row the figure came from, figure linking into the panel that derives it, `source` naming the `table.column`, `figure={null}` rendering an em-dash whose consequence must say why, and **four cards never five** (a fifth pushes the evidence down, which is the defect reintroduced by its own growth). **Phase 3 renamed `Risk` and reclaims `/risk`** -- its question always WAS the risk question -- which also simplified ADR-0170, since `#stress`/`#attribution`/`#exposure`/`#concentration` resolve natively again and the fragment hop shrank to the three that moved. Moves, each justified by the destination's declared `question` and not by taste: cap utilisation `/risk`→`/mandate` (the limit board it measures against is there), benchmark + weights backtest `/mandate`→`/attribution` (backward-looking), track record `/method/evidence`→`/attribution` (phase 6 IS "was it right"), VaR by method `/mandate`→`/risk` (a risk analysis, not a limits check), stress scenarios 2622px→**top**. **Nothing collapsed**, at the owner's direction; the moves carried the whole gain. **A hard refusal travels with the component**: no card may show Sharpe/Sortino/Calmar/drawdown/realised beta/TE/IR, because those come from `portfolio_returns` -- costless on a book at **92.7% mean daily turnover**, where `CostDrag` measured the sign flipping (+0.76% gross vs -0.72% net) -- at 6 sessions against minimums of 30-252. Wrong on sample AND basis, and an answer row is the worst place for a wrong number; ex-ante figures (VaR, CVaR, tilts, HHI, stress, caps) ARE valid on a recommendation and are what the rows use. **Result -- the check this was written to pass, "no phase route where the block answering its own question sits below 1000px": all seven PASS**, `/risk` 2622→**576px**, `/mandate` 5.3→3.6 screens and 232→122 numerals, `/attribution` 1.5→2.5 (the point). **Three findings the rows surfaced that nothing had aggregated**: only **4 of 10 limits are sourced from `scoring_config`** (six are house defaults -- `LimitRow.limitSource` carried it per row and no surface summed it); **the same Σ sizes the book and reports its risk** (`covariance_from_returns` feeds both the optimizer and the VaR/MC path, so reported vol is a **lower bound**, and μ is shrunk 50% per ADR-0033 while Σ is not shrunk at all -- now disclosed on `/risk`'s fourth card); and `/` was reading `narrative_signals` **twice**. Costs: `/risk` grew to ~7.7 screens (gained VaR + the row, lost cap headroom), **three labels per phase** plus a `question` the rows generate FROM (widening it is now behavioural, not editorial), and `CorrelationMatrix` lost its measured pair. **Two mistakes recorded because both looked right**: 929 tests passed with `RiskBody` syntactically broken and every page 500ing (its only test reads the file with `readFileSync`, so nothing imports it -- tsc catches this, vitest structurally cannot; the break was a `{/* */}` comment between `&& (` and its element); and lifting `useNarrativeSeries` to the page made fetches go **2 → 6**, because a hook cannot be called conditionally so `const own = useNarrativeSeries()` still ran its effect -- the `shared` prop chose which RESULT displayed and removed no read. The hook took `{ skip }`; measured again, 2. **The prop alone was the plausible fix and it was wrong.** Rejected: collapsing (owner, and unnecessary), the worst-case figure on `/risk`'s row (ADR-0084 -- `/book` publishes it and links here; `/risk` shows BOTH tails instead), and a down-capture card on `/attribution` (TE needs 60 sessions and there are 6, so it would render an em-dash -- the fourth card reports **sample adequacy itself**, naming that the persisted Sharpe reads 5.14 and is withheld for exactly that reason) | accepted | 2026-07-30 |

| [0173](0173-the-turnover-cap-had-no-baseline-and-the-risk-it-reported-was-the-risk-it-optimised-against.md) | The turnover cap had no baseline, and the risk it reported was the risk it optimised against -- `book_holdings_performance` measured mean daily turnover **92.7%** over 7 sessions, peak **200.0%**, cumulative cost **0.973% ($973k)**, ~**35%/yr** annualised; gross **-1.26%** became net **-2.01%** over the window. `held_book.py`'s own docstring had already named the fix: "`OptimizerConstraints.max_turnover` has carried since ADR-0107 and nothing has ever set it... there is no measured basis in this repo for any particular number." **Two defects sat under that dormancy.** (1) The one existing test bound the constraint against `weights0` -- the CONVICTION book of the SAME run, which `weight_delta` and the frontier's "you are here" point both need as their baseline -- so even switched on it would have capped the optimizer's intra-run adjustment, not day-over-day churn: the exact defect migration 056 documented for `rebalance_cost` one layer along, where the MEASUREMENT got fixed and the OPTIMISER was never re-pointed. (2) `covariance_from_returns` fed both `optimize()` (minimising `w'Σw`) and the ex-post VaR/Monte-Carlo report at two call sites in `q1_agent.py`, so the SAME noisy sample Σ that was optimised against was reported AS the book's risk -- Markowitz error-maximisation, biased low by construction -- while μ was shrunk 50% (ADR-0033) and Σ received no equivalent treatment, with no argument for the asymmetry. Decision, two parts: `OptimizerInputs` gains **`weights_held`** -- yesterday's PUBLISHED book (`book_holdings.signed_weight`), fetched once in `run_q1_agent` mirroring ADR-0171's own pattern so `size_positions` stays a pure function of state -- and the constraint becomes `‖w − w_held‖₁ ≤ max_turnover`, applied ONLY when both exist (absence of a prior book is not evidence the book should hold still). `OptimizationResult` now reports TWO figures never conflated -- `turnover` (intra-run, unchanged) and `realised_turnover`/`turnover_cap` (day-over-day, new) -- fixing a consequence in passing: `binding_constraints` was checking "turnover at cap" against the WRONG quantity. `Mandate.max_turnover = 0.60`, a first cut against the data: the 30-58% band (5 of 7 sessions) is unaffected, the two 141.6%/200.0% outliers are roughly halved; seeded via migration 059, mirroring 054 exactly. Second: `covariance_from_returns` now shrinks toward a **constant-correlation** target (Ledoit & Wolf 2003) -- keeps each asset's own sample VARIANCE (per-asset risk attribution reads it), replaces every pairwise correlation with the sample's average -- at a **FIXED 0.25 intensity**, not Ledoit-Wolf's asymptotically-optimal one (deriving that needs the paper's π̂/ρ̂ estimators, a second nontrivial thing to get right for a marginal gain over a stated choice; `IC_SHRINKAGE` is the nearby fixed-and-stated precedent), wired INSIDE the one function every consumer calls so the optimizer and the risk report cannot diverge on which Σ is "the" covariance -- the property that made the original bias possible. Reported as `optimizer_result.cov_shrinkage_intensity`, and this morning's Risk answer row (ADR-0172) now reads the ACTUAL intensity off the persisted run rather than asserting a constant -- a pre-ADR run was genuinely unshrunk and says so; a shrunk one states the real number and is explicit "narrowed" is not "removed". Verified live: the mandate panel reads `Turnover (day-over-day) — 60% — config`, the currently-published (pre-deploy) book correctly renders **NO DATA** rather than a false `0% — OK`. 19 new backend tests, three adversarial to the specific historical mistake: binds-on-weights_held-not-weights0 (replaces the test that pinned the wrong baseline), does-not-bind-the-intra-run-figure (proves genuine decoupling), no-prior-book-means-unconstrained. Costs: the cap is a first cut at 7 sessions of history, not proven cheap in foregone return; shrinkage narrows the bias, does not remove it; TWO hand-built limit surfaces needed updating (`riskBoard.ts` AND `MandatePanel.tsx`'s separate `ENFORCED_ROWS`), which is duplication this change inherited rather than introduced. Rejected: binding to `weights0` and calling it done (that IS the mistake); Ledoit-Wolf's derived-optimal intensity (correct in principle, a second nontrivial estimator for unmeasured benefit); sklearn's `LedoitWolf` (shrinks toward IDENTITY -- wrong target, would flatten the variances this repo's risk attribution reads -- and an undeclared dependency risks the exact cvxpy/gensim class of failure already hit once); gating turnover by `returnSessions` (needs no history at all, unlike VaR/CVaR/beta) | accepted | 2026-07-30 |

| [0174](0174-a-constraint-cannot-bind-a-variable-it-was-never-given.md) | A constraint cannot bind a variable it was never given -- ADR-0173 landed same-day, and its first live run (2026-07-30) was the first real test: `optimizer_result.realised_turnover` reported **0.59999997**, exactly at the new 60% cap, `"turnover at cap"` in `binding_constraints`. `book_holdings_performance.turnover`, computed independently by `held_book.weight_delta` off the same two published books, measured **0.944951**. A 34.5-point gap, exact to five decimal places against five names (TLT, VRT, GDX, PDD, NOC) that were held 2026-07-29 and simply **absent** from the day's candidate pool on 2026-07-30 -- not reweighted to zero, never a decision variable at all. `optimize()`'s constraint is `cp.norm1(w - w_held) <= max_turnover` where `w` has one entry per name in TODAY's `assets`; a name held yesterday but outside that list contributes to neither the constraint nor its own `realised_turnover` audit, because both iterate `assets` alone -- new entries are counted correctly (`w_held.get(a, 0.0)` defaults to zero for them) but exits of dropped candidates are invisible on either side. **The exact defect `held_book.weight_delta`'s own docstring names**, one layer upstream: "iterating one side's keys alone would price an entry and miss every exit." Worse than the pre-ADR-0173 state, not merely as bad: `/mandate` showed "60% -- config", a number that reads as governed, while the true figure breached the cap by more than half again its own value. Decision: a name in `weights_held` but not in `assets` is treated as a **forced, constant cost paid before the solver runs** -- `forced_exit_turnover = Σ|weights_held[a]| for a not in assets`, `remaining_budget = max(0, max_turnover - forced_exit_turnover)`, and the solver's own constraint binds to `remaining_budget`, never a negative one (a forced cost the solver could not avoid is not a tighter constraint on what it CAN choose, it is a budget of zero). `realised_turnover` is corrected to solver-visible-distance-over-`assets` **plus** `forced_exit_turnover` -- now the same quantity `book_holdings_performance.turnover` measures independently, by construction, not by coincidence. `forced_exit_turnover` is a new NAMED field on `OptimizationResult`/`optimizer_result` (34.5 of 94.5 points is too large to fold silently into a bigger total), and `binding_constraints` now distinguishes an ordinary bind (`"turnover at cap"`, solver used its full remaining budget) from a breach the constraint had no power to prevent (`"turnover cap breached by candidate exits alone (X% of Y% budget, before any solver choice)"`) -- reporting the second as merely "at cap" would repeat the exact false-assurance failure this ADR fixes, with smaller numbers. 2 new adversarial tests, both would fail pre-fix: a partial forced exit correctly shrinks (not zeroes) the solver's remaining budget; forced exits alone exceeding the cap freezes every solver-visible name and reports a breach rather than a false bind. Costs: this does not make candidate churn cheaper, only visible and (within budget) resistible -- if the model's picks genuinely change entirely every run, `forced_exit_turnover` alone keeps landing near the cap, leaving the solver little or no room to reweight what it keeps, which is a real limit on today's fix, not a bug in it; **the already-published 2026-07-30 `optimizer_result` still carries the wrong pre-fix numbers** (positions/sizes for names within `assets` are unaffected -- this bug lived in the reporting and residual budget, not the sizing choice -- but the disclosed diagnostic is misleading until the next scheduled run or an explicit correction), left as an open operator question rather than decided here, since correcting a published diagnostic is the `book_revisions`/ADR-0093 governance question, not an optimizer one. Rejected: extending `w` to cover the union with exited names pinned to zero (mathematically equivalent, but adds dead variables every other group/complex constraint would need to special-case); disclosing the gap without fixing it (ADR-0173's shrinkage precedent doesn't apply -- this fix needs no further argument about which intensity, so shipping honest-but-wrong when correct-and-simple was available would be the weaker option with the stronger one in hand); silently raising the effective cap to absorb forced exits (makes the mandate's cap a function of the day's candidate churn instead of an operator statement) | accepted | 2026-07-30 |

| [0175](0175-a-detector-can-carry-trend-context-without-becoming-one.md) | A detector can carry trend context without becoming one -- ADR-0146 retired a top-5-loudest share-over-time line chart as the narrative board's PRIMARY view, because the top-5-by-share set was dominated by financial-writing register (`earnings`, `price`, `q2`), not narratives, while the board's actual question ("is anything accelerating that nothing watches?") is a STATE question a trajectory chart cannot answer -- `DetectionScatter` replaced it. The operator then asked for the line chart back, on the Alpha phase, placed above or below the velocity plot: a DIFFERENT request than the one ADR-0146 answered, since this is ADDITIONAL context beside a still-primary, unchanged scatter, not a replacement of it. A pinned regression test (`tests/unit/narrative-trends.test.tsx`) specifically asserted `<TrendPlot series={top}>` does NOT reappear in `NarrativeTrends.tsx` -- written to catch exactly this shape returning UNDOCUMENTED, which is the outcome this ADR exists to avoid, not to route around. Decision: `TrendPlot series={top}` returns as a labelled "Share over time" section ABOVE `DetectionScatter`, using the SAME top-N set `SeriesTable` already lists (no new phrase exposure), gated on `runs >= 2` (mirroring `ThemeTrends`' guard, since `TrendPlot` renders nothing from one point), and carrying the ADR-0146 finding INLINE as its own caption -- *"Top 5 by today's share, not by whether they mean anything -- the loudest phrase in a news corpus is routinely financial-writing register ('earnings', 'price', 'q2'), which is why the detection plane below judges by breakout, not by volume alone."* The pinned test is REWRITTEN, not deleted: it now asserts the scatter stays primary/default, plus two new assertions that the trend section renders (with its caption) at two-plus runs and renders nothing at one -- so an undocumented reappearance would still be caught, and a documented one is no longer confused with the thing the test was written against. Verified live on the 2026-07-30 run: the section renders between the legend and the scatter, direct-labelled lines with leader-line collision handling intact. 2 new tests + 1 rewritten, 940 frontend green, `tsc --noEmit` clean. Costs: the card is taller (not re-measured against ADR-0172's screen-height budget, which targeted different phases); two render sites for one `topSeries` ranking (mitigated by `TrendPlot` staying the one shared geometry implementation, per ADR-0064); a reader who skips the inline caption can still misread the line chart as ranking importance. Rejected: refusing the request and citing ADR-0146 (a materially different decision -- additive context beside an unchanged primary detector -- and the owner's direction on a weighed tradeoff overrides a prior recommendation, the same precedent ADR-0170 set over ADR-0169); filtering the top-N set to exclude register words before charting it (a real, separate problem in `narrative_tracker.py`'s phrase extraction, not a rendering decision, and conflating the two would make this larger and riskier than asked); deleting the pinned test rather than rewriting it (its job was never "this string must not appear," it was "this shape must not reappear undocumented") | accepted | 2026-07-30 |

| [0176](0176-two-plots-one-column-one-width.md) | Two plots, one column, one width -- the operator asked for ADR-0175's new "Share over time" `TrendPlot` and `DetectionScatter` to render the SAME width, both on the left with the figures table on the right, i.e. stacked in the scatter's own grid column rather than sitting above the whole grid at full card width. Same CSS width is free (both render `w-full`, so two elements in one grid column resolve to the same pixel width regardless of viewBox); legible labels at that width are NOT free -- `TrendPlot`'s only geometry was fixed at 720 wide (sized for `ThemeTrends`' full column), and an SVG's `w-full` scales UNIFORMLY, so dropping a 720-wide viewBox into the ~400px column this layout needs would shrink every font ~0.55x, the exact defect ADR-0168 measured and fixed for `DetectionScatter` -- reintroduced here on the second chart that now needs the same protection. Decision: `TrendPlot` takes `width`/`height`/`plotLeft`/`plotRight`/`plotTop`/`plotBottom` as OPTIONAL props defaulting to the current constants (renamed `DEFAULT_WIDTH` etc; `ThemeTrends`' no-props call site is pinned byte-identical to the old hardcoded render), and `NarrativeTrends` passes `width={S_WIDTH} height={S_HEIGHT}` -- the SAME constants `DetectionScatter` already uses, one declaration read by both rather than a second narrow-geometry number this file would have to keep in sync (ADR-0064's one-fact-one-home). Both plots now live inside ONE wrapping div that is the grid's first column (TrendPlot's section, then DetectionScatter), with the table staying the second, 340px-fixed column beside the stacked PAIR. `plotLeft`/`plotRight`/`plotTop`/`plotBottom` stay at their EXISTING absolute values even at the narrower width -- they are pixel space for end-labels and a leader-line gutter, which does not shrink just because the chart is narrower, the same reasoning `DetectionScatter`'s own `S_PLOT_LEFT`/`S_PLOT_RIGHT` already apply. Verified live via Playwright at 1500px: both SVGs measure identically (507x265px CSS, same left edge, viewBox `0 0 430 225`), no `<text>` overflows its own SVG on the real 2026-07-30 run including a 21-character end-label ("closes sharply lower") inside the unchanged 148-unit right gutter. 5 new tests (a byte-identical no-props-vs-old-defaults parity test, a 430x225 viewBox-overflow sweep across 1/3/5 series x 2/5/14/30 runs mirroring the existing 720-wide sweep, and a source-text pin that the call site actually passes `S_WIDTH`/`S_HEIGHT`), 954 frontend green, `tsc --noEmit` clean. Costs: below the `figures` (1248px) breakpoint the grid collapses and both plots stack full-width again -- still equal to each other, but no longer narrowed to `S_WIDTH`'s geometry, the same responsive tradeoff `DetectionScatter` already accepted, now paid by two charts; the date axis is more compressed (238 plot units vs `ThemeTrends`' 528 for the same chart type); `TrendPlot` gained six optional numeric props, a larger surface for a future caller to get wrong (e.g. `width` without `height`) with no validation pairing them. Rejected: relying on `w-full`'s own CSS scaling with no viewBox change (that IS the ADR-0168 bug, restated); a second, `NarrativeTrends`-local narrow-geometry constant instead of reusing `DetectionScatter`'s `S_WIDTH`/`S_HEIGHT` (two numbers required to always equal one CSS width is the ADR-0064 trap with an added desync risk); a single `geometry` object prop instead of six flat scalar props (marginally more compact, but needs its own partial-merge-with-defaults logic for a caller that only ever overrides two of six fields) | accepted | 2026-07-30 |

| [0177](0177-one-share-two-axes-one-scale.md) | One share, two axes, one scale -- `TrendPlot` and `DetectionScatter`, now stacked in one column (ADR-0176), both plot SHARE OF VOICE but on perpendicular axes (`TrendPlot`: Y; `DetectionScatter`: X) and each computed its own maximum independently -- `TrendPlot`'s `yMax` from the largest share across every point of the top-5 series it plots (x1.1 pad), `DetectionScatter`'s `xMax` from the largest LATEST share across every tracked phrase (x1.08 pad) -- different data, different padding, routinely different tick sets, so a phrase's position on one chart could not be read directly against the other. Operator asked for the two axes to match. Decision: `NarrativeTrends` computes ONE padded max over the UNION of what both charts actually plot -- `Math.max(0.01, ...series.map(latest.share), ...top.flatMap(points.share)) * 1.1` -- and passes it as a new optional `yMax`/`xMax` override prop on each (a full override, not a floor, since a shared axis means neither chart's own data may narrow the other back down), ONE padding factor replacing the two that only differed because each axis used to be computed alone. `niceTicks(0, sharedMax, 4)` is already the SAME function both charts call, so matching inputs produce matching tick sets with no separate tick-alignment logic needed. Verified live on the 2026-07-30 run: both charts render the identical tick set 0/5/10/15/20/25/30%. 4 new tests: `yMax`/`xMax` each proven to override the component's OWN computation (not merely accepted as a prop), plus an integration test proving the shared max reaches a series' HISTORICAL peak even when its TODAY value alone would compute a much smaller axis -- the case a naive "share the smaller chart's own max" would get wrong. 958 frontend green. Costs: the scatter's own padding silently changed 1.08x->1.1x as a side effect of unifying the two, not independently justified or tested for; `sharedShareMax` is a THIRD number now required to agree with both charts' internal share-reading logic, with no test deriving it FROM either chart's own code the way `risk-thresholds.test.ts` derives a threshold from Python -- a manual-agreement risk accepted, not closed; the override is all-or-nothing, no "at least this large" floor variant, sufficient for this one call site but would need revisiting for a second caller wanting different semantics. Rejected: a `minShareMax` floor each chart is raised to rather than a full override (the two could still diverge whenever one chart's own number happened to already be the larger one); rounding the shared max to a fixed nice-percentage boundary before `niceTicks` sees it (redundant -- `niceTicks` already does exactly this from any input) | accepted | 2026-07-30 |

| [0178](0178-a-labels-column-is-plot-width-not-spent.md) | A label column is plot width not spent, and one phrase is one colour -- two follow-ups to ADR-0176/0177. (1) `TrendPlot`'s DEFAULT geometry reserves a 148-unit right gutter for direct end-labels, sized for `ThemeTrends`' 720-wide box (20% of it); at the narrative board's narrower 430-unit box the SAME gutter ate 34%, squeezing five lines across up to 30 days into 238 units. Operator asked for the label text to move left, inside the x-axis's own width. (2) `TrendPlot` colours lines via `seriesColor(phrase, i)` -- rank rotation, with an `IDENTITY_COLORS[phrase]` check that never actually fires for a narrative phrase, since no tracked phrase is literally the string "AI Capex" (the theme name lives at `covered_by`, not in the phrase). `DetectionScatter` colours marks via a DIFFERENT function, `markColor`, checking `covered_by` directly -- so "ai" got the theme's pink hue on its DOT but a rank-rotation colour on its LINE, a latent inconsistency neither chart's own tests could catch since each only asserted against its own rule. Decision, labels: `TrendPlot` gains `labelInside?: boolean` (default false, `ThemeTrends` unaffected) -- true anchors each label at `x(lastDate) - 6`, `text-anchor="end"`, extending LEFTWARD from the line's own endpoint instead of starting in a separate gutter; the narrative board's call site pairs it with `plotRight={16}`, collapsing the 148-unit gutter and growing plotWidth 238 -> 370 units -- arithmetic, not cosmetic. Anti-collision STACKING is untouched (never depended on which side labels render); only the displaced-label CONNECTOR changes shape, from a rightward-then-diagonal polyline to a short vertical tie at the point's own x -- the same shape `DetectionScatter` already uses for its own displaced labels. Decision, colours: both components gain `colors?: Map<string,string>`, checked BEFORE either's own resolution logic; `NarrativeTrends` computes ONE map from `top` -- the only place with both a phrase's RANK (`seriesColor`'s input) and its `covered_by` (`markColor`'s input) -- `(covered_by && IDENTITY_COLORS[covered_by]) || SERIES_COLORS[i % 5]`, fixing "ai"'s latent bug as a side effect: both charts now resolve through `covered_by` once, correctly. Only `top`'s handful of phrases are ever in the map -- every mark beyond it keeps the existing coverage-only scheme, so ADR-0146's filled/hollow encoding is not diluted for the long tail it actually governs. Label INK is explicitly untouched -- still coverage-only per ADR-0162, since matching a mark's HUE to its line is a different claim from matching its label's ink to the payload's. Verified live on the 2026-07-30 run: SVG measures 507px with zero overflowing elements (up from a box that reserved over a third of itself for a label column), and "ai"'s line and dot both render `var(--theme-ai-capex)`, confirmed via actual rendered attributes -- "fed"/series-2, "oil"/series-5, "global"/series-4 likewise match. 17 new tests: label position inside vs outside plotWidth, the connector's new vertical shape, `colors` overriding each component independently, an end-to-end round-trip proving the SAME phrase gets the SAME colour on both charts, and a dedicated 12-case viewBox-overflow sweep at the EXACT production config (`width=430 height=225 plotRight=16 labelInside`) distinct from ADR-0176's sweep (which used the old 148-unit gutter). 975 frontend green. Costs: labels now sit closer to and can visually crowd the plotted lines, the tradeoff `DetectionScatter` already accepted for its own in-plane labels, now paid by `TrendPlot` too; a phrase's colour is decided one level up the tree, so a third caller wanting its own scheme must supply a full map, no partial-override path; `markColor`'s signature changed from a loosely-typed `{covered_by}` object to explicit `(phrase, coveredBy)` args, a larger diff at each of its four call sites in exchange for making the `colors` lookup's dependency on `phrase` visible rather than buried in a closure. Rejected: shrinking `LABEL_X`/font size to fit more in less width (the gutter's cost was structural -- 34% of the box -- not a tuning problem); giving EVERY scatter mark a rank-rotation colour, not just `top` (ranks beyond 5 would repeat `SERIES_COLORS` via `i % 5`, a NEW collision class between unrelated phrases that does not exist today); matching the direct-label INK to the mark's hue too (the exact promotion ADR-0162 forbids) | accepted | 2026-07-30 |

| [0179](0179-a-quarter-column-cannot-hold-an-820px-table.md) | A quarter column cannot hold an 820px table -- the owner asked for `/mandate` to open as ONE row: "The mandate" across the left half, "Risk-limit board" in the third quarter, "Cap utilisation" in the last. The layout argument is ADR-0172's own, one axis further -- the board and the cap bars are both READINGS of the mandate ("is the published book inside it?"), and a reader previously had to carry the 20%/30%/35% caps across ~1100px of scroll to check one against the other. What the row costs is width, and both narrow cards were built against a width they no longer have: `RiskLimitBoard` was a six-column table with an explicit `min-w-[820px]`, and `CapUtilisation`'s bar row switched to a three-column form at `sm` needing ~350px. Measured, not guessed: `main` is `max-w-[1400px]`, so 1336 content less 3x24 gap, /4 = **316px per column**, ~280 inside a card -- an 820px table in a 280px window is a 3x horizontal scroller, which `RiskBody`'s own pairing notes already call goal 7's failure rather than a fix for it, and the three-column cap row overflows ~70px into a `.card` that CLIPS rather than scrolls (the same defect its comment records fixing once for phones, arriving from the other end). Decision: take the row, and the limit board stops being a table. `RiskBody` renders mandate+limits as one `xl:grid-cols-4` row (`MandatePanel` at `col-span-2`, board and caps at one each), with `RiskMetricsGrid` moved out and left full width beneath it -- its five `md:grid-cols-5` tiles would wrap one per line in a 316px column. `RiskLimitBoard` becomes a stacked list: label + status badge, then Value / Limit / Headroom as a three-up `<dl>`, then the utilisation bar and %, then the note (which carries the source) and the `limit - scoring_config` chip. NOTHING was dropped -- every column of the old table is still on screen, and the `<dl>` exists because a `<thead>` does not: a bare `62.4% 100.0% +37.6%` under no heading is three naked numbers (goal 1). ONE rendering at every width, not a table below `xl` and a list above it -- two markups would put every figure on the card in the DOM twice, and a page whose numbers are duplicated for layout can have them disagree; it also kills the 820px scroller on phones, where the table was already scrolling. `CapUtilisation`'s bar drops BACK to its two-column form at `xl`, the same stacked form it uses below `sm`, gated on the layout's own breakpoint rather than a guess. Verified live at 1440 on the 2026-07-30 run: 660/318/318px on one row, `scrollWidth === clientWidth` on all three cards and the document (expanded caps included), no console errors; stacked fallback checked at 1280/1024/390; `tsc --noEmit` clean, `risk-sections`/`chip-contrast`/`mandate-drift`/`risk-board-*`/`cap-breach`/`section-nav` green. Costs: the section nav degrades -- `#mandate` and `#limits` now start at the same y and `SectionNav` picks the first in DOCUMENT order among visible, so "Limits" will rarely win the active state (both anchors still resolve; the highlight is what stops being informative), not fixed here; the row is lopsided (board 2250px vs mandate 1604 vs a collapsed cap card's 58), inherent to the requested allocation; density is lost below `xl` where the list is airier than the table was, mitigated by a `max-w-[520px]` on the `<dl>` but not erased; and reading utilisation DOWN a column across eleven limits -- what the table did best -- is gone, with only within-row alignment and the breached-first sort preserved. Rejected: keeping the table and letting it scroll (the trade the pairing notes exist to refuse); table below `xl` and list above via `hidden`/`xl:block` (duplicates every figure in the DOM); dropping the note column to narrow the table (measured -- six columns still need ~718px without it, so the note was not what made it wide); giving the board two columns and the caps none (fits the table, is not the layout that was asked for) | accepted | 2026-07-30 |

| [0180](0180-the-note-is-beside-the-board-the-source-is-not.md) | The note is beside the board; the source is not -- two same-day follow-ups to ADR-0179's mandate row. (1) `CapUtilisation` is a `<details>` with no `open`, right while it was the fourth card down a scrolling page, but as one of four cards in ONE row it spent a whole quarter column on a 58px summary bar beside a 1604px mandate panel. (2) The stacked limit board measured **2250px** -- taller than the panel beside it and the tallest thing on the page. Where the height went, MEASURED in the browser rather than estimated (11 rows, ~178px each): prose `note` 33px (66 for the long ones), the three-up Value/Limit/Headroom `<dl>` 30px, label+badge 21-23px, bar 15px, `limit - scoring_config` line 15px, and **~55px of pure padding and margin** -- 605px across the card for no information at all, the single largest line item. The largest item that carried anything was the prose, which is exactly what `MandatePanel` renders in its own "What it means" column 24px to the LEFT for every one of the eleven limits; the prose was not wrong, it was in the wrong card, and only because until ADR-0179 the two cards were 1100px apart. Decision: `<details open>` on the caps (the disclosure stays -- a reader who wants the board beside it without 19 bars can still close it -- but the default is visible, goal 7's 2026-07-30 narrowing reaching an EXISTING `<details>` rather than a new one being added); and on the board, `LimitDef` gains `source: string` -- the `table.column` the VALUE is read from (`optimizer_result.realised_turnover`, `portfolio_risk.var_95 / total_capital`, ...), split out of the prose it used to be buried at the end of and rendered under every row beside where the LIMIT came from, two different facts so both show and neither stands in for the other. The prose `note` moves to the row's `title` and the intro links to `#mandate` for it; `value / limit` and the headroom go on ONE line replacing the `<dl>`, reading as a pair because the intro says so -- the same idiom and the same justification as the `15.92% / 20.00% (80%)` bars in CapUtilisation directly beside it, which is what makes it not three naked numbers rather than 30px of column headings per row; spacing tightened (`py-3`->`py-2.5`, `mt-2`->`mt-1.5`, the `limit - ...` line merged into the source line). Measured at 1440 before->after: board 2250->**1531px**, caps 58->**1131px** (open), mandate 1604 unchanged, page 3026->**2381px** -- the page lost 645px despite one card going from collapsed to open; no overflow on any card or the document, no console errors. Costs: the board no longer explains itself in isolation (a deep link to `#limits` or a screenshot of the card gets figures and sources but not meanings -- one card away and named, not gone), and 5 of 11 source lines wrap at 280px; `title` is a weak home for the prose (hover-only -- no keyboard, no touch, not found by Ctrl+F -- a convenience for a mouse, not the traceability path, which is the source line that renders); `LimitDef` now has two fields that must not drift (`note` still names the source in its own words, `source` states it structurally, nothing enforces they agree because nothing renders both at once -- accepted, not closed); and row density is near its floor at four lines / ~105px, so the next cut has to remove a figure, not whitespace. Rejected: keeping the prose and shortening it (still two sentences about one limit 24px apart, and the shorter one is the worse one); per-row `<details>` for the note (11 disclosures on a card that is itself one of four, against a standing direction that this page stops hiding things); dropping the prose from `MandatePanel` and keeping it here (backwards -- that panel's job IS what the mandate means, the board's is whether the book is inside it); dropping the `limit - house default` line to save 15px (it is the difference between a limit somebody chose and one nobody did -- the fact the "5/11 sourced" answer card is counting) | accepted | 2026-07-30 |

| [0181](0181-three-columns-one-baseline.md) | Three columns, one baseline -- two more directions on the `/mandate` row (ADR-0179/0180), taken together because the second changes what the first has to mean. (1) "The mandate", "Risk-limit board" and "Cap utilisation" should align top AND bottom -- they measured 1604/1531/1131px from a shared top edge, three different bottom edges on one row. (2) The five risk-metric tiles go UNDER the cap-utilisation card, one below another -- they had been left full width beneath the row on the reasoning that `md:grid-cols-5` across four 316px columns would wrap to one tile per line, which stacked deliberately in one column is the layout rather than an objection to it. The second settles what "bottom aligned" can mean: if the last column holds a card AND five tiles, the caps CARD is not what aligns, the COLUMN is -- a card stretched to the row height there would push the tiles out of the row entirely. Decision: `items-start` comes off the row (the one paired grid on this page that does not use it) so cells stretch to the tallest; **each card carries its own `h-full`**, because a stretched WRAPPER with a content-height card inside aligns nothing -- which is exactly what the first attempt produced; `MandatePanel` and `RiskLimitBoard` are `h-full flex flex-col` with their closing note at `mt-auto`, so slack opens ABOVE the footer and each card's closing line sits on the row's bottom edge; `CapUtilisation` carries NO `h-full` (it shares its column rather than owning it -- content-height at the top, five tiles beneath, the column's height is what meets the other two); and `RiskMetricsGrid` becomes `grid-cols-2 md:grid-cols-5 xl:grid-cols-1` -- two up on a phone, five across from `md`, one per line from `xl` where the mandate row exists, same DOM at every width so no figure is rendered twice, with its `mb-6` dropped since spacing is now the parent stack's `gap-6`. Measured at 1440 on the 2026-07-30 run: mandate and board both **474 -> 2379px**, last column 474 -> 1604 (caps) -> 2379 (fifth tile) -- one baseline, three columns; tiles 318px stacked, 182px five-across at 1024, 173px two-across at 390; no overflow on any card or the document, no console errors; 975 frontend tests green, `tsc` clean. Costs: the row got TALLER (1905 vs 1604) because the tallest column now holds two things, though the page is still 2483px against the 3026 ADR-0179 inherited; two cards now carry real slack inside them (~300px in the mandate, ~370px in the board) which pinned footers make read as a deliberate bottom rule rather than a ragged edge, but it is the price of one baseline; `h-full` is load-bearing in three components for a layout decision made in a fourth, commented but not enforced, so a future card added to this row without it will silently not align; and a collapsed caps card is harmless only by luck of ORDERING -- it sits above the tiles, so closing it shortens the column rather than leaving a hole. Recorded because it cost an hour: **Chrome wraps a `<details>`'s non-summary content in a UA `::details-content` box**, so `flex-col` on the element makes THAT box the flex item and a `flex-1` child inside it does not grow -- measured, the card body stopped at 1068px inside a 1546px slot, while Firefox and Safari flex the children directly. Not needed in the end (the caps card no longer stretches), but the next person reaching for `flex` on a `<details>` should not have to rediscover it. Rejected: stretching the caps card and putting the tiles below the row ("under it" meant in its column; below the row is where they already were); aligning only the two full-height cards and letting the last column ragged-end (it IS the tallest column -- the other two align to it); `items-stretch` with no per-card `h-full` (measured as the first attempt: the cells stretched, the cards did not, nothing changed visually) | accepted | 2026-07-30 |

| [0182](0182-four-of-five-were-already-on-the-page.md) | Four of the five were already on the page -- the owner asked why the risk-metric tiles were on `/mandate` at all. They were never chosen for it: before the phase split `/risk` was one page and `RiskMetricsGrid` sat inside `<section id="limits">`, ADR-0170 cut that page by reader question and mapped `limits` -> mandate, so the grid travelled with the section it happened to live in; ADR-0181 moving it into the row's last column made the question visible rather than creating it. Read off the LIVE page at 1440 on the 2026-07-30 run: VaR `NO DATA - / 6.0%` on the board vs `Unavailable -` on the tile; CVaR `NO DATA - / 9.0%` vs `Unavailable -`; Beta `NO DATA - / 0.50` vs `Unavailable -`; HHI `1,403 / 2,000, headroom +597, OK` vs `1403 up +5, Exact`; Sharpe -- no mandate limit exists -- vs `Unavailable`. Four of five are the same `portfolio_risk` columns the board already reports AGAINST THEIR LIMIT AND HEADROOM, which on a page asking "is the book inside its mandate" is strictly the more useful form, and three of those four were stating the same ABSENCE twice, two cards apart, in two different wordings; the fifth, Sharpe, is a realised risk-adjusted return with no mandate limit at all -- phase 6's question, not phase 1's. Decision: the grid moves to `/attribution`, at the top of the `realised` section. `/risk` was rejected as a destination ON ITS OWN COPY -- its lede promises "every figure here is EX-ANTE, a pure function of the recommended weights and a 252-day covariance estimate", and all five are computed off the realised return series, so landing them there would have made that sentence false. `/attribution` is the backward-looking surface, the delta-vs-yesterday chips are exactly its claim, and the phase already opens with an answer card ("7 sessions -- 11 statistics are still withheld for want of history; var_95 unlocks at 30") that is about these tiles specifically, so they now sit directly beneath the card explaining their own absence. NO change to `RISK_SECTION_PHASE`: the grid moved INTO an existing section rather than becoming one, so the nav-section parity the split is tested on is untouched; the attribution lede gains a clause naming what the phase opens with. Two follow-ons the move forced: `RiskMetricsGrid` DROPS the `xl:grid-cols-1` step ADR-0181 added (it existed for a 318px column that no longer exists, and a class kept "in case" is a class nobody can delete later) with `mb-6` returning now that no parent stack owns its spacing; and `CapUtilisation` takes `open:h-full` BACK, owning the last column again so it is what meets the other two cards' bottom edge -- including the `[&::details-content]` rule ADR-0181 recorded but did not need, which is now what pins its closing note to the baseline. Measured at 1440: `/mandate`'s three cards run **474 -> 2062px**, one top and one bottom edge, page 2483 -> **2166px**; on `/attribution` the strip renders five across at 259px per tile, 175px tall, above the track record; collapsing the caps card still returns it to 58px; no overflow, no console errors, 975 frontend tests green, `tsc` clean. Costs: `/attribution` inherits duplication it does not have yet (`beta` also appears in `BenchmarkComparison` further down that page, VaR/CVaR in `VarMethods` on `/risk`) -- this removes one duplication and does not audit the rest; the provenance rendering MOVED rather than spread, so these tiles remain the only place `numeric_derivations` (method id, freshness, uncertainty band) reaches a reader for these five figures while the limit board reports the same numbers with only a source line, an asymmetry left standing; `/mandate` lost its only Sharpe, intended (a mandate is not a performance statement) but a figure a reader of that page could previously see; and the last column is a single disclosure again, stretched to 1588px against ~1131px of content. Rejected: leaving it (the duplication is on one screen and includes the same absence stated twice); retiring the grid entirely and rehoming only Sharpe (cheapest, and it would have deleted the only surface rendering derivation provenance for these five); moving it to `/risk` (contradicted by that page's own lede, which is a promise about every figure on it) | accepted | 2026-07-30 |

| [0183](0183-a-disclosure-that-buys-no-space.md) | A disclosure that buys no space -- the owner asked for `Cap utilisation` to stop being collapsible. It had been a `<details>` since it was written, correct while it was the fourth card down a long scrolling page where closing it moved everything below up by ~1000px; three changes on 2026-07-30 removed that property one step at a time -- ADR-0179 put it in a quarter column of a single row, ADR-0180 forced it `open` by default because a card whose whole content sits behind a summary bar was spending a quarter of that row on 58px, and ADR-0181/0182 made it the sole occupant of that column in a row whose height is set by the two cards BESIDE it. By the end, collapsing it returned nothing: the row is 1588px tall whether this card is 58px or 1131px. So the disclosure had become a control that hides content and frees no space -- and it needed a whole apparatus to avoid LOOKING broken while doing it: `open:h-full` so a collapsed card would not stretch into a 1600px empty box, `open:flex`/`open:flex-col`, and an `[&::details-content]` rule that existed only because Chrome wraps a `<details>`'s non-summary content in a UA box a flex column cannot reach through. Decision: it becomes a plain `<section className="card">` with a normal card header -- `<summary>` -> `<div className="card-header">`, `DisclosureChevron` and its import gone, the count line (`19 limits monitored - 0 breaches`) unmoved, the header now identical in form to the two cards beside it. Design goal 7 says to collapse optional detail rather than delete it and its own 2026-07-30 narrowing said "existing `<details>` are untouched", so this argues that goal down first, as that file requires -- narrowed AGAIN, as a TEST rather than an exception for one component: **a disclosure that buys no space is not a disclosure**; ask what closing it gives back, and where the answer is "a screenful" the goal is unchanged and every other `<details>` in the tree stays. Verified at 1440: the three cards run **474 -> 2062px**, `SECTION` all three, **zero `<details>` remain on /mandate**, and the closing note pins to 2047 (15px above the card edge, the card-body's own padding) with NO browser-specific rule involved, because a `<section>` flexes its own children; no overflow, no console errors, 975 frontend tests green, `tsc` clean. What this DELETES is the interesting part -- `open:h-full`, `open:flex`, `open:flex-col` and all three `[&::details-content]` variants, every one of which existed to make a collapsible card behave inside a stretched grid cell; removing the collapse removed the reason for the whole class of workaround, including the Chrome quirk ADR-0181 recorded, which no longer applies anywhere in this tree. Costs: 19 cap bars are now unconditionally on screen, ~1100px a reader cannot fold away -- free today since the row is as tall as the mandate panel regardless, but not if this card ever becomes the tallest, at which point the answer is to SHORTEN it rather than hide it; the rule is now conditional and conditional rules are harder to apply ("does closing it free space?" needs a judgement about layout that "collapse optional detail" did not), written into `docs/design-goals.md` as a question to ask rather than a list of exempt components so it applies to a card that does not exist yet; and a reader who wanted the board beside a quiet column loses that, which ADR-0180 argued for and which did work -- it just cost more than it returned once the card owned its column. Rejected: keeping the `<details>` and dropping only the `open` gating (that IS the state ADR-0180 shipped, and what produced the stretched-empty-box problem the `open:` variants paper over); keeping it collapsible and letting a collapsed card sit short at the top of a stretched cell (honest, and it breaks the one-baseline alignment asked for two messages earlier); making every card on the row collapsible for symmetry (three toggles that each free nothing) | accepted | 2026-07-30 |

| [0184](0184-a-column-with-no-ceiling-eventually-finds-one.md) | A column with no ceiling eventually finds one -- the narrative board's figures table was horizontally scrolling; measured live, its content was 347px against a 319px container, a 28px overflow. `table-layout: auto` gives a table no ceiling -- each column is min-content over its widest cell, and `w-full` is a target the table can still exceed. Per-column measurement found ONE string responsible: the Velocity column's `"not measurable"` (rendered for a null reading) alone set that column to 78px, nearly as wide as Status (`"established"`, 67.9px), against every OTHER value in it (`"+4.00"` etc) needing roughly a third of that. The component's own header comment had already documented this table's min-content width as deliberate and load-bearing, citing `"not measurable"` as one of the values setting it -- true when written, 28px wrong today, resolved by direct measurement rather than by trusting the comment. Decision: the cell reads `"n/a"` instead -- not new vocabulary, the `Emerging` block two sections down on the SAME card already uses `"n/a"` for the identical null-velocity state -- with the fuller reason moved to (not deleted from) a `title` attribute, satisfying ADR-0126's "never the only copy" by construction since the tooltip is additive detail on an already-meaningful visible value. Verified live: `scrollDiv.scrollWidth === scrollDiv.clientWidth` (319=319), scrollbar gone, `Found by` no longer clipped. 1 existing test updated in place (not net-new), 975 frontend green (unchanged count), `tsc` clean. Explicitly scoped as closing TODAY's overflow, not raising a ceiling: `table-layout: auto` still has none, and a narrative corroborated by both discovery methods renders `Found by` as `"frequency + lda + embedding"` (ADR-0133) at ~27 characters -- comfortably capable of reopening the scrollbar on a day that happens, at which point the fix to reach for is `table-layout: fixed` with explicit per-column proportions (deferred here, not rejected, since a single occurrence does not yet justify a change touching every column's visual behaviour). Costs: `"n/a"` is measurably less specific than "not measurable" for a reader who does not hover -- accepted because the identical tradeoff already existed in the Emerging list this now matches, not a new class of loss but a real one; whether the original ~308px estimate was ever accurate or the container narrowed since is left uninvestigated, since the live measurement is authoritative regardless of which. Rejected: `table-layout: fixed` now rather than deferred (bigger, riskier, six-column change for one measured 28px gap); shrinking `pr-2` gutters further (the component's own existing comment already reasoned through and rejected this); a bare em-dash with the reason only on `title` (weaker than "n/a" for a sighted non-hovering reader, and introduces a second symbol for a state "n/a" already names two sections down) | accepted | 2026-07-30 |

| [0185](0185-the-cap-the-page-listed-and-never-checked.md) | The cap the page listed and never checked -- the owner noticed white space at the bottom of `Cap utilisation` (1588px cell, ADR-0181's shared baseline, against ~1131px of content) and asked whether there was more RELEVANT information for it. The honest first step was an audit rather than a fill: `optimizer_result` carries eight fields this card does not render and SEVEN are already rendered on /book in `SizingProvenance` -- `binding_constraints`, `crowding` (via `describeCrowding`), `zeroed`, `warnings`, `unpriced_assets`, `ic`, `turnover*`. Pulling those across would be exactly the duplication ADR-0182 removed hours earlier, with less excuse: there it was accidental, here it would be deliberate and motivated by empty pixels. **`complex_sizing` is rendered nowhere**, and the gap it represents is worse than whitespace: `MandatePanel`, 660px to the left in the SAME ROW, lists "Correlation complex -- 20% -- `max_complex_weight`" under "Enforced -- the sizer cannot breach these", the limit board has no row for it and this card had no group, so the page published an enforced constraint and checked it nowhere. A second gap is derivable rather than persisted: sector rows come from `book_metrics.SECTOR_MAP` and geo rows from `GEO_MAP`, so a held ticker in NEITHER map lands in no group at all -- its weight is never counted toward any sector or geography cap and that cap silently cannot bind on it, while the bars look equally reassuring at 100% of the book or 60%. Decision: add the two things nothing else says, and leave the rest of the whitespace. (1) A `Correlation complex` block rendering `complex_sizing` -- how many complexes formed and their members, any groups deliberately skipped and why, and `risk_cap_multiple_of_single_name`, because ADR-0118's RISK budget can bind while the capital bars above still show headroom, so slack in the bars is not slack in the constraint; an absent column says so, which is a different claim from "no complex formed". (2) A coverage sentence in the closing note, from a new tested `capCoverage()` in `lib/risk/capBreach.ts`: what each grouping's rows add up to against `book_metrics.gross_exposure`, with `complete` asserted ONLY when every group reaches gross and one unmeasurable weight making that group `null` rather than `0` -- a sum that quietly skips a row is a wrong number, not a partial one. The whitespace is explicitly NOT the justification: both would be worth adding to a card with no space to spare, which is the test they had to pass. Live at 1440: content 1131 -> **1313px** in the 1588px cell (~275px of slack remains, and that is fine); the complex block reads "No complex formed on this run: no held names were grouped above the 0.70 correlation threshold, so this cap constrained nothing. It is listed because the mandate enforces it, not because it bound" plus "A complex's RISK budget is x1 one name's"; coverage reads "Single name / sector / geography account for 100% / 100% / 100% of the book's 78.74% gross -- every held dollar sits inside a mapped group", verified against the payload directly (all three groupings sum to 0.7874 against 0.7874 gross). 5 new `capCoverage` tests (all complete, one grouping seeing less, float dust, an unmeasurable weight, absent gross), 980 frontend green, `tsc` clean. Costs: the complex block will read "nothing formed" MOST days, and a block that usually says nothing trains a reader to skip it on the day it does not -- accepted because showing it only when non-empty makes a cap that appears and disappears, where absence is indistinguishable from the feature not existing; the 0.70 threshold is PROSE, not data (it comes from ADR-0116 and the payload does not carry it, so a change to the grouping threshold leaves this sentence wrong -- the same drift class `mandate-drift.test.ts` closes for limit VALUES, left open here); `capCoverage` is a browser-side check rather than a pipeline artefact, so it detects the EFFECT of a ticker missing from `SECTOR_MAP` -- a grouping that no longer reaches gross -- but names neither the ticker nor the map; and `ComplexSizing` now lives in `lib/book/sizingProvenance.ts` beside the rest of `OptimizerResult`, so a /mandate component imports from a `book/` module -- correct direction, but it crosses a folder boundary that had been one-way. Rejected: moving `binding_constraints` or the crowding summary here (both on /book; same-payload different-page duplication is still duplication, and ADR-0182's argument does not weaken because the destination has room); showing `cash` (21.3% undeployed -- already the /mandate answer row's headline four cards up the same page); stretching the bars or padding rows to fill the height (density that serves nothing, goal 7); leaving the card as it was (the complex cap would still be a published constraint with no measurement anywhere, which the whitespace question surfaced but did not create) | accepted | 2026-07-30 |

| [0186](0186-the-distance-to-a-complex-is-a-picture-and-two-tabs-that-scroll-nowhere.md) | The distance to a complex is a picture, and two tabs that scroll nowhere -- two things from one message. (1) ADR-0185's Correlation-complex block was PROSE in a card of nineteen `weight / cap (util%)` bars, so it read as a footnote to them rather than the same kind of claim, and it could not answer the question a reader has when told nothing formed: HOW CLOSE was it? (2) The `Mandate` / `Limits` tab strip was measured redundant, not judged so -- `#mandate` and `#limits` are the two COLUMNS of one row, both beginning at y=474 and ending at y=2062, so two tabs scroll to the same pixel; worse, `SectionNav` picks the first visible id in DOCUMENT order, so with both entering the viewport simultaneously "Limits" could never win the active state -- ADR-0179 recorded that as a cost and left it, but it is not a cost, it is a control that cannot be right. Decision: a `CorrelationMeter` draws the book's most correlated held pair as a filled bar on a 0-1 CORRELATION scale, the threshold at which names become one idea as a tick, and mean |rho| as a mark below the track -- every number read from `book_metrics.correlation_summary` (`max_abs_pair`, `flag_threshold`, `mean_abs_corr`, `pair_count`). **`flag_threshold` closes the drift ADR-0185 left open**: that ADR accepted "the 0.70 threshold is prose, not data" on the belief the payload did not carry it -- it does, and the component no longer contains the number. It is deliberately NOT a `CapBar` (the scale is a correlation, not a weight against a cap); when a complex DOES form, that one IS a `CapBar` -- sigma member weight against `complex_pct`, drawn exactly like the three groups above it, because that is precisely what a complex is. The surviving prose says what the picture cannot: live, the closest pair sits **0.011 below the line**, and "BABA + JD hold 30.00% between them, which a crossing would measure against 20.00% instead of two separate single-name caps" -- both derived from rows already on the card. And `PHASE_SECTION_NAV.mandate` becomes `[]`, with the exemption as a NAMED LIST (`PHASES_WITHOUT_SECTION_NAV`) rather than a special case inside a component, so a phase that grows back to two screens has to be taken off it deliberately; the section IDS stay, because /risk still hops `#mandate` and `#limits` here as fragments and the limit board's own copy links to `#mandate` -- the strip went, the anchors did not. `risk-sections.test.ts` GAINS two tests rather than losing one: an exempt phase must have an EMPTY nav (half a strip says the sections it omits are elsewhere), and must still render its sections with their ids gated in `RiskBody`. Live at 1440: the meter draws `BABA + JD 0.689 / 0.70` with the mean mark at 0.222 across 36 pairs, the strip is gone, the three cards run **409 -> 1997px** (one baseline, 65px higher up the page), page 2101px against the 3026px this sequence started from; no overflow, no console errors, 982 frontend tests green, `tsc` clean. Costs: the meter is ONE PAIR, not the distribution -- a book with five pairs at 0.68 and a book with one look identical here, and `mean_abs_corr` over 36 pairs is a weak hedge (the full matrix is on /risk, and reproducing it here is the duplication ADR-0182/0185 refused); the "what a crossing would cost" sentence is a HYPOTHETICAL that computes the cap which would apply, not what the sizer would do -- a real complex is a connected component that could pull in a third name and the book would be re-solved rather than clipped, so a reader could take it as a prediction of the outcome; the mean mark has no label at its position, only in the legend below, because at 0.222 it sits where a floating label would collide with the axis start; and a phase with no jump strip has NO in-page navigation at all -- correct at 1588px on one screen, wrong the moment something is added below the row, and nothing enforces the height except the list and the reason written beside it. Rejected: drawing the correlation matrix or pair list here (it is /risk's `CorrelationMatrix`, and a single summary measure against the mandate's own threshold is a different claim from the heatmap); rendering the complex block only when a complex exists (a cap that appears and disappears makes absence indistinguishable from the feature not existing -- ADR-0185's argument, unchanged); keeping one tab and dropping the other (a strip with a single destination is the same non-control with better odds); keeping both tabs and fixing `SectionNav` to tie-break by column (a nav that must know about grid columns to say where you are is solving the wrong problem -- nothing here is out of reach) | accepted | 2026-07-30 |

| [0187](0187-an-answer-row-and-its-evidence-share-one-ruler.md) | An answer row and its evidence share one ruler -- the owner reported that the first row of four answer cards does not line up with the cards below it. Measured at 1440: `AnswerRow` ran 76-403 / 415-742 / 754-1081 / 1093-1420 against a mandate row of 76-736 / 760-1078 / 1102-1420 -- only the page GUTTERS agreed, every internal boundary 6-9px out, because the two grids were built to different geometry despite sitting one above the other in the same container: **gap** `gap-3` (12px) against the evidence row's `gap-6` (24px), which alone lands four equal columns in different places, and **breakpoint** `wide:grid-cols-4` (1424) against `xl:grid-cols-4` (1280), so between those widths the page drew FOUR evidence columns under TWO answer columns -- not a misalignment so much as a different layout. `AnswerRow` is shared by /mandate, /risk and /attribution, so this was never mandate-specific; it is that until ADR-0179 nothing below an answer row had columns to disagree with. Decision: `AnswerRow` adopts the evidence grid's geometry, `xl:grid-cols-4 gap-6` -- both halves taken from the row BELOW rather than chosen here, because an answer row sits directly above the cards that justify it and a reader tracing "5/11 sourced" down to the limit board is following a column, which two rulers break. The four-up COUNT is unchanged and still justified in the component's own comment; `wide` was never a measured gate for this component the way it is in `tailwind.config.ts` for the two-pane book row -- the comment beside it justifies four CARDS, not the breakpoint. Verified exact at 1280 / 1440 / 1500: answer column 2 ends where the mandate card ends, column 3 spans the risk-limit board exactly, column 4 spans cap utilisation exactly; below `xl` both stack the same way; /risk and /attribution keep identical answer geometry; 982 frontend tests green, `tsc` clean. **It also surfaced a live clipping bug, the more useful half**: the answer card's source line is a bare `table.column` identifier with no spaces to break on and `.card` CLIPS rather than scrolls, so `research_recommendations.book_metrics.gross_exposure` measured **312px inside a 288px body** and its tail was cut with nothing on screen to say so -- a source a reader cannot finish reading is not a source (goal 1). It predates this change; narrower columns only made it wider of the mark. Fixed with `[overflow-wrap:anywhere]`, the same remedy `Ident` in `SectionGap.tsx` already carries for the same reason. Costs: answer cards are 9px narrower at 1440 and 316px narrower at 1280 (where they were two-up at 602px) -- four at 286px is tight, nothing overflows but the headline numbers now wrap to two lines there; the wrapped source line breaks MID-TOKEN (`...gross_expo / sure`), ugly and consistent with the established remedy, so consistency won over appearance; the alignment is NOT enforced -- two components must keep the same two Tailwind classes in step, with a comment in each and no test, since a drift test would have to parse both class strings; and /attribution still has two different rulers, correctly so, because its answer row is four-up and the risk-metric strip below it is five-up -- four answers and five statistics are not the same thing, so nothing there is traced down a column. Rejected: narrowing the evidence row to `gap-3` (`gap-6` is the card-grid gap everywhere else in this tree, so it would misalign the mandate row with every other paired grid to fix one row); moving the evidence row's four-up gate to `wide` (it would leave /mandate fully stacked from 1280 to 1423, a common laptop width, to fix an alignment that only exists above it); leaving it and accepting 6-9px (exactly the near-miss that reads as sloppiness rather than choice, on a page whose entire argument is that its numbers are careful) | accepted | 2026-07-30 |

| [0188](0188-the-risk-phase-measures-a-book-that-does-not-exist-yet.md) | The risk phase measured a book that did not exist yet -- the owner asked whether `Sizing` should come before `Risk` in the rail. The six-phase strip IS the site's navigation (ADR-0170) and teaches the process before a reader has read anything, so its order is a claim; it read `01 Mandate - 02 Alpha - 03 Risk - 04 Construction - 05 Execution - 06 Attribution`. **The pipeline settles it in its own words**: `q1_agent.finalise_book_analytics` -- "Recompute book metrics, correlations and stress scenarios against the FINAL SIZED book. `compute_book_metrics_node` and `run_scenario_analysis_node` run before reason_picks, over the equal-weighted candidate pool, because the LLM needs them as prompt context. That makes them a description of a portfolio nobody holds... This node runs last, on the real book, and writes machine-readable dicts that `_persist_to_supabase` stores and **the /risk page renders**." So the pre-selection scenario pass is 30 candidates at 3.3% each, exists as prompt context and is DISCARDED; every figure a reader sees on /risk -- `scenario_results`, `risk_decomposition`, `monte_carlo_var`, `var_forecast`, correlation, factor tilt -- is written AFTER `size_positions`, and the page's own lede says so ("a pure function of the recommended weights"). Phase 3 could not be produced without phase 4 having already happened. Where the drift came from: the order was RIGHT for the phase as originally written -- phase 3 was "Catalyst & scenario", a question about an IDEA, which a PM does ask before sizing. ADR-0172 renamed it "Risk & scenario" and gave it the `/risk` route, noting "the six-phase sequence is untouched". The sequence was untouched; the PHASE was not -- the rename moved it from idea-level to book-level and left it standing in the idea-level position. Decision: swap to `01 Mandate - 02 Alpha - 03 Construction - 04 Risk - 05 Execution - 06 Attribution`. Two entries in `lib/method/phases.ts` reorder and renumber and nothing else moves -- `TopBar`, `SideRail` and `ProcessMap` all render that array directly, so ADR-0170's "there is only one list" is what makes this a two-line change rather than a sweep; routes, ids, anchors and page CONTENT are untouched. `risk-sections.test.ts` now asserts THE PAIR (phase 4 is `risk` AND phase 3 is `construction`), because a swap that renumbered one and not the other would leave two phases claiming one position with nothing to catch it. Verified live: the rail reads Mandate - Alpha - Sizing - Risk - Execution - Outcome, /book marks `03 Construction` current and /risk marks `04 Risk`, and /method's process map renders 01-06 in the new order; 982 frontend tests green, `tsc` clean, no console errors. Costs: the numbering is USER-VISIBLE and now differs from three ADRs -- 0169, 0170 and 0172 all quote `03 Risk - 04 Construction`, they stay as written because they are the record of what was decided then, and anyone reading them cold will see a sequence the app no longer has; it moves away from the canonical PM workflow `task.md` Q1 is answered against, where scenario work precedes construction (the defence being that this system's phase 4 is not idea-level scenario work -- if a genuinely idea-level catalyst surface is ever built it wants its OWN phase before construction, not this one back); phase 1 is still "Mandate & risk architecture", so risk now appears at both ends of the sequence, correct but the word does two jobs in one strip; and /risk's per-position attribution sits in phase 4 while phase 6 is called Attribution -- pre-existing and documented in `phaseSections.ts` (one ex-ante, one ex-post), but the swap puts the two words two tabs apart instead of three. Rejected: keeping the order and restoring phase 3 to idea-level scope (it would mean moving VaR, the stress matrix and per-position attribution off /risk to somewhere after /book -- that content has to live somewhere and a page called Risk is where a reader looks for it); leaving it (the strip is the one piece of this site that teaches the process without being read, so an order the pipeline contradicts is the most expensive place to be wrong); renumbering without reordering the array (`TopBar` and `SideRail` render it in order, so the number and the position would disagree) | accepted | 2026-07-30 |

| [0189](0189-one-page-header-and-a-strip-that-stopped-repeating-the-answers.md) | One page header, and a strip that stopped repeating the answers -- the owner asked why the Sizing page's top row (a rule separating the header text from a band of stats) looks different from /mandate, /, /risk, and which should be standard. There were **five hand-rolled headers in two treatments**: /book had `sm:border-l` then `RUN DATE` as a 10px uppercase micro-label ABOVE its value, with a 14.5px `text-primary` lede plus a 12px tertiary second line; /mandate, /risk, /attribution and / had `RUN DATE 2026-07-30` inline -- label and value on one line at one size, no rule -- with a 13px `text-secondary` lede. Nobody decided this: / and `RiskBody` are near-identical markup written twice, so they did not differ from /book by choice, they differed because there were five copies of one thing. Decision: a shared `components/PageHeader.tsx` (title, lede, optional fine print, meta label/value pairs with the rule), on **/book's** treatment -- the inline form was the MAJORITY, four to one, and it is the one that went, because it is the only place on this site where a label and its figure share a line at the same size (reading as a sentence fragment) while every other labelled figure in the tree -- answer cards, this strip, the mandate panel's boxes -- is a micro-label above a `num` value; and the rule does work, separating when-the-data-is-from from the prose that reads it. `PageHeader` **renders no figure of its own and takes no data**: every value is passed in already formatted by the page that read it, so it can never become a second place a run date is derived, and /'s freshness label and status badge pass through as nodes rather than being special-cased. Separately, the /book stat strip had a written defence ("the AnswerCards line says what the figure MEANS, this strip says what the figure IS") but four of its six cells -- Gross, Net, Deployed, Worst scenario -- were the same figures the answer row states 200px below; owner's direction was to find OTHER stats rather than delete it, which is the better call since the instrument was right and pointed at the wrong figures. Those four become **Themes - Sectors - Geographies - Largest position** (Positions and Longs/Shorts stay), making the strip the book's SHAPE -- how many bets, which way, across how many ideas, spread over how many sectors and geographies, how big the largest single one is -- which the answer row describes nowhere; every cell derives from `picks` and `book_metrics`, both already on state, so no query moved. "Same number, different framing" turned out thinner than it read: a reader just told 78.7% gross WITH its consequence does not need 78.7% gross WITHOUT one 200px later. Live at 1440: all five headers render one component, the strip reads `Positions 9 - Longs/Shorts 4/5 - Themes 5 - Sectors 7 - Geographies 3 - Largest position 15.9% (JD)`, no cell restates an answer card; 982 frontend tests green, `tsc` clean, no console errors, no overflow. /'s lede grew with the component (owner's direction, same message): eleven words became a paragraph naming what a theme score is made of and what it does not claim -- "a high score means a theme is loud, not that it is right -- nothing on this page is a position" -- plus a fine line stating the narrative board sizes nothing; both facts existed only further down inside the answer cards, which is AFTER the board they qualify. Costs: four pages changed appearance without being asked to (/mandate, /risk, /attribution and / gained a rule, a larger lede and stacked meta labels -- what standardising on the minority treatment means); /'s h1 grew 19px -> 22px and its header block ~70px -> 148px; `Largest position` restates a bar on /mandate's caps card, a cross-page repeat of ONE figure accepted where four same-page repeats were not; `Themes`/`Sectors`/`Geographies` are counts of GROUPS not of risk, so seven sectors across nine names sounds diversified and is not necessarily -- `book_metrics.SECTOR_MAP` decides what a sector is and the HHI on /mandate is what measures concentration; and the header no longer carries a page's own hand, so a page wanting something structurally different must widen `PageHeader` or opt out, and opting out is how five copies happened the first time. Rejected: standardising on the inline form (cheapest, one page changes -- but it reads as prose where every other figure reads as a labelled figure); extracting a component but keeping the inline look (kills the drift risk without fixing what made /book look better, which is what prompted the question); deleting the strip (the owner asked for other stats, and the instrument was never the problem); keeping only the two non-duplicating cells (a two-cell strip is a caption) | accepted | 2026-07-30 |

| [0190](0190-credit-rates-exposures-shadow-on-arrival.md) | Credit & duration exposures (L2b), shadow on arrival | accepted | 2026-07-31 |
| [0191](0191-phrases-belong-to-the-board-themes-belong-to-the-funnel.md) | Phrases belong to the board, themes belong to the funnel -- applying ADR-0162 to the funnel card ("6 watched by nothing" was a count that named no phrase) produced two lists of phrases on one grid row: `SeriesTable`'s six columns over the 5 loudest, and a new three-column list over all 6 unwatched, sharing two identical rows with no stated relationship. Every number agreed -- both render the same `useNarrativeSeries` rows -- so nothing was wrong, and a reader still could not say why `global` appeared twice and `us` once. Design goal 7's narrowing decides it: check whether the content belongs where the duplicate already lives. The board's table was capped at five only because `top = topSeries(series, SERIES_COLORS.length)` is five for the TREND PLOT's palette and the table inherited the number by sharing the variable; the table has no palette and no width dependence on row count (columns are `min-content`, ADR-0184), so the cap bought nothing and cost it the rows the board exists to surface. Decision: the table renders `top` union every phrase with `covered_by === null` (nine rows today, all six columns), the funnel's phrase list is removed, and the funnel's committed half instead names its nine THEMES with `promotion_basis` -- nine pips that named none of them was the same ADR-0162 defect, and worse, since `themes` is rendered as a list nowhere else on the page. Counts derive from those rows so the pips and the list cannot disagree. The durable rule is the division: a card that counts a set may name it, but where the members are already tabulated it points rather than repeats, and two cards do not both own the same noun. Costs: the table's height is now data-driven and unbounded on a denser corpus (a cap would need arguing on its own terms rather than inheriting a palette's again); one cross-reference moved with the content. **Amended same day**: decision 1 was unbounded and the ADR said so -- the next nightly run took the corpus from 11 tracked / 6 unwatched to 55 / 42, the table rendered 47 rows at 1396px and the card went 750px -> 1758px. `UNWATCHED_SLOTS = 5`: `top` plus the five LOUDEST unwatched not already in it, at most ten rows whatever the corpus does (measured after: card 805px, page 5392px -> 4439px). Filled by share and not by velocity even though velocity is the alarm, because `emergingUncovered` below the table already IS "accelerating AND young AND unwatched" and a table selecting on velocity while sorting on share would be a second worse copy of it. Rejected: marking the overlap (a third encoding to explain a duplication instead of removing it); showing only the unwatched the table does not (membership becomes a rule about the UI, and the card's count 6 would disagree with its list of 4 -- a card that cannot name its own count is where this started); differentiating by column with `days_observed` (the alarm, velocity, disappears from the place that raises it); dropping the top-5 notion entirely (unbounded with no argument) | accepted | 2026-07-31 |
| [0192](0192-a-fallen-angel-transmits-through-measured-not-hand-set-shocks.md) | A fallen-angel scenario transmits through MEASURED credit betas, not hand-set shocks -- S, the deliberate opt-in ADR-0190 named in advance, and the PM critique's single highest-praised recommendation. Inserts a third tier into `_resolve_shock`, between `base_asset_shocks` and `sector_shocks`: `S7_fallen_angel` (IG issuer downgraded to HY, `d_ig +60bp` / `d_qual +140bp`, `mkt -5%` real but deliberately secondary) reads `marginal_beta_ig`/`marginal_beta_qual` off `credit_rates_exposures` -- never `total_beta_*`, which double-counts with the scenario's own `mkt` shock. Worked example pinned in a test: beta_ig=-6.8, beta_qual=-2.0 under S7's legs gives -6.88%. `_resolve_shock` stays pure (no Supabase call inside it) -- `credit_betas` is threaded in exactly as `factor_exposures` already is, fetched once by a new `q1_agent._load_credit_betas` and reused across node 5 and node 9 so the book cannot be stressed by one snapshot while the page explains another. Coverage is reported, not assumed (ADR-0097's doctrine): every held name is classified override/measured/sector/unresolved and the count leads `contribution_breakdown`, gated on the scenario declaring `credit_leg_shocks` so the six pre-existing scenarios get no new line. `credit_betas=None` (no measured rows for the run_date) is the required fallback -- S7 degrades to override-and-factor-path only, pinned bit-identical against a scenario with `credit_leg_shocks={}`. `base_asset_shocks` stays to one entry (SVXY) because the whole demonstration is that measured betas cover the book. 20 new/updated tests | accepted | 2026-07-31 |
| [0193](0193-a-beta-must-clear-its-own-standard-error-before-it-can-stress-the-book.md) | A measured credit beta must clear its own standard error before it can stress the book -- ADR-0192 wired S7_fallen_angel to transmit `marginal_beta_ig`/`marginal_beta_qual` unconditionally; residualising each leg against FF5+UMD (ADR-0190's own construction) leaves a small-variance regressor, so a noisy single-name fit routinely produces a large, imprecise coefficient. Measured by hand on the live 2026-07-30 rows: GEV t=-1.31, GLD t=+1.53, JD t=+1.08, SMH t=-1.73, UNG t=-0.48, SPY t=-0.91 -- not one clears |t|=2, and 23 of 62 assets carry |marginal_beta_ig| > 10. `marginal_r2` cannot gate this: it is the JOINT fit's r-squared, dominated by the six equity factors, and stays high even when the credit coefficients are noise. Decision: `_ols_core.ols_window` returns a namespaced `_se` (se = sqrt(diag(s2 * pinv(X'X)))), never leaking into `rolling_regression`'s bit-identical output; migration 061 adds `marginal_se_ig`/`marginal_se_qual` (the STANDARD ERROR is stored, not the derived t-statistic, so a reader can pick its own threshold or compute a confidence interval); `scenario_analysis.py` gates transmission on `|beta/se| >= CREDIT_BETA_T_THRESHOLD` (2.0, named), applied PER LEG, with a NULL/missing SE failing closed. An insignificant beta is never zeroed -- it falls through to the sector, then factor, tier exactly as an absent row would, because a 0.0 shock would assert "no credit exposure," the opposite of what an insignificant estimate establishes. Coverage now reports two counts (measured vs. believed) instead of one. The honest result on the live multi-asset book: 1 of 9 held names (GEV, via its quality-gap leg only, t=+2.33 -- its own IG leg does not clear, t=-1.31) clears the gate; the other 8 held names transmit through no leg and fall back. Across the full 62-asset universe, 16 clear on at least one leg, overwhelmingly literal bond/credit ETFs (LQD, HYG, JNK, ANGL, EMB, AGG, IEF, SHY, BIL) with real spread/duration sensitivity -- a finding, not a bug, and the threshold was not tuned to manufacture or suppress it. 22 new tests plus 2 updated in the ADR-0192 suite | accepted | 2026-07-31 |
| [0194](0194-a-credit-book-published-for-inspection-is-not-yet-a-track-record.md) | A credit book published for inspection is not yet a track record -- the PM critique's surviving claim was that the tradeable universe cannot express a credit book; ADR-0190/0193's measured, SE-gated credit betas show it can (CREDIT-lens universe: 12 tickers, 9 clear `|beta/se| >= 2` -- LQD 8.25, SHY 7.28, JNK 3.84, IEF 3.70, ANGL 3.61, AGG 3.15, HYG 3.15, EMB 2.55, BIL 2.06 -- vs. the published multi-asset book's 9 held names and 1 believable, GEV's quality leg only). ADR-0015's `lens` parameter has existed since 2026-07-21 and never run in production because `research_recommendations` had `UNIQUE (run_date)` and `book_holdings` had no `lens` column at all -- a second lens's run for the same date would violate the constraint or silently replace the published book. Migration 062: `book_holdings` gets `lens TEXT NOT NULL DEFAULT 'multi_asset'` + `UNIQUE (run_date, lens, asset)`; `research_recommendations`'s existing nullable `lens` (m022) becomes `NOT NULL` (one pre-lens-plumbing row backfilled) + `UNIQUE (run_date, lens)`. The critical boundary: `pick_outcomes` (ADR-0090), `book_signal` (ADR-0148), `book_holdings_performance` and `book_revisions` (ADR-0093) get **no** lens column and **no** schema change -- they are the multi-asset book's forward track record, and admitting a lens dimension would invite exactly the leak this ADR closes. Enforced in code at five points: `q1_agent._persist_to_supabase` gates `_record_book_revisions`/`_persist_signal` on `lens == "multi_asset"` (the one persist path both lenses share); `record_published_claims` checks `agent_result["lens"]`; `extend_held_book`'s four queries are scoped to `lens='multi_asset'` (an unscoped read could otherwise borrow or delete the coexisting credit book's rows once two rows per date became possible); `_fetch_previous_held_weights` filters its turnover-cap baseline by lens; and three offline scripts nobody asked about by name -- `resolve_outcomes.py`, `rebuild_held_book.py`, `check_data_integrity.py` -- are scoped too, found by tracing every existing reader of the two changed tables rather than trusting the task's own file list. `book_holdings` itself is NOT gated: `_persist_book_holdings` (new) writes a target-weight snapshot for EVERY lens, since a credit book published for inspection needs weights to show on `/book` even though it gets no P&L series. `run_credit_lens_book` (new, `scripts/daily_refresh.py`) runs L5 a second time under `lens="credit"` as its own `pipeline_runs` L5b stage, gated by `RUN_CREDIT_LENS` (default on, since this doubles the nightly LLM spend shared with `/ask`), wrapped so any failure -- a MiniMax 429 above all -- is logged and recorded without touching the already-persisted multi-asset book. Verified live for 2026-07-30: both books exist and are independently readable; the multi-asset book is byte-identical to the pre-run snapshot. Starting a credit track record is named explicitly as a deferred, deliberate decision, not an oversight. 9 new tests | accepted | 2026-07-31 |
| [0195](0195-an-axis-labelled-with-dates-must-be-spaced-in-days.md) | An axis labelled with dates must be spaced in days -- `TrendPlot` renders BOTH `Share over time` (ADR-0175) and `Theme trends`, both label their x-axis with two dates, and neither was spaced in dates: `x = xIndex.get(d) / (dates.length - 1)` gave every run an equal slice of the width regardless of when it happened. Defensible on a regular grid; neither series is one. Measured live 2026-07-31: `narrative_signals` (`archive`) had **21 runs across 31 calendar days** (holes at 07-01..04, 07-12/13, 07-15..17, 07-25) and `theme_signals_history` 9 across 10 (07-26 missing). So the five-day hole and the overnight 07-29 -> 07-30 step were drawn the same width -- a narrative that did not move for five UNMEASURED days and one that jumped overnight had identical slopes. This is ADR-0126's finding in the other axis, and worse than a missing axis because two date labels sat under it asserting otherwise. `ThemeTrends` already CAPTIONED "A missing day is a gap, never a zero" (ADR-0141): true of the data, false of the picture. Decision: x is calendar time, parsed as UTC midnight explicitly (`Date.parse("2026-07-30")` is UTC by spec, `new Date("...T00:00:00")` is local, and mixing them shifts a point by a day for any reader west of Greenwich); a rug of ticks along the baseline marks the days that have data, because calendar spacing puts a gap in the right PLACE and only the rug says a gap is there at all -- a line chart interpolates and has no vocabulary for `not sampled`. The rug takes a `runDates` prop rather than the plotted series, since the per-point circles are per SERIES and a phrase below the document floor on a day the corpus ran is missing a circle on a day that WAS measured: live, the plotted top-five's union was 14 days against the corpus's 20, so six measured days would have been marked missing under a caption saying a stretch without ticks is silence in the corpus. Caption and `aria-label` state both counts, since runs and days are now different numbers and that difference is the point. Costs: sampling density is now visible and unflattering (the archive's recent days are dense, its older ones are not); a long interpolated segment across a hole is still a straight line, deliberately -- breaking it needs to know WHY a point is absent, and `TrendSeries` distinguishes neither; one test sliced the `<TrendPlot>` call site with `+ 320` characters and failed when a prop pushed the last assertion out of the window, so it now slices to the element's own `/>`. Rejected: ordinal + label every point (still wrong about duration); a prop defaulting to ordinal so `ThemeTrends` keeps the old look (rejected on its data -- it runs Sat 07-25 and skips Sun 07-26, so it is not a weekday grid but "whenever the pipeline ran", exactly where ordinal lies, and defaulting to the old behaviour preserves the defect in the chart whose own caption denies it); breaking the polyline across gaps (overstates -- it would break for a below-floor phrase, which is a measurement); interpolating (ADR-0066/0141 forbid inventing a reading, and a chart is not an exception) | accepted | 2026-07-31 |
| [0196](0196-the-tape-is-six-markets-and-they-do-not-close-together.md) | The tape is six markets, and they do not close together -- the owner asked for Europe / Asia / FX / crypto / futures behind a toggle on the homepage tape, which was four US indices. Three things had to be settled first. WHERE THE TICKERS LIVE: the obvious move is to extend `YFINANCE_TICKERS`, which is also the map `upsert_latest_snapshot` walks into `macro_indicators` -- the L0 set `regime_classifier` reads, `/method` renders and the L5 prompt is built from -- so adding the Nikkei and Bitcoin for a decorative strip would make them macro indicators, inherited silently by every L0 consumer with no error anywhere. WHERE THE ORDER LIVES: the frontend held `DISPLAY_ORDER = ["^SPX","^NDX","^DJI","^RUT","^VIX"]` against the backend's `EQUITY_INDICES` of four -- two lists that had to agree, with nothing failing when they stopped, and they HAD stopped: `^VIX` was displayed and never fetched, so that cell had never once rendered; the sort was `indexOf`, so any ticker the backend added scored -1 and led the tape. WHETHER "TODAY" MEANS ONE THING: it does not -- measured on the backfill, Asia/FX/crypto carried 2026-07-31 while the US/Europe/futures carried 07-30, and the table's only time column was `updated_at`, the pipeline write time, identical for Tokyo and New York (ADR-0062 one level up). Decision: a separate `RIBBON_GROUPS` map as the single source of membership AND order, `HISTORY_TICKERS` = macro set union tape so tape tickers get price history and nothing more, `upsert_latest_snapshot` still walking `YFINANCE_TICKERS` alone; migration 063 adds `market_group` / `sort_order` / `as_of` so the frontend sorts on the row; `as_of` is the trading date of `current` (already in hand as `vals[0]["date"]` and being discarded), nullable per ADR-0066 since the four pre-existing rows have no recoverable session; freshness computed over the group ON SCREEN and the strip names the sessions; the strip says "Daily closes" because a tape is the most live-looking element on a page and this one is up to a day old. FX keeps 4dp keyed on magnitude -- `round(v, 2)` destroys EUR/USD 1.1512 into 1.15 and renders a 0.40% move as no move. Costs: `macro_daily_history` gains 17 series (additive, but ~2.7x wider per day, and `backfill_regime.py` reads it); Coinbase's COIN 50 has no Yahoo symbol (`COIN50-USD` -> "Quote not found") so the crypto group is BTC/ETH/SOL and claims no fourth; `market_assets` was missing from ARCHITECTURE.md entirely and is added here. Rejected: extending `YFINANCE_TICKERS` (the L0 leak, invisible); keeping the grouping in the frontend (recreates the two-list problem that already lost the VIX cell); one tape-wide freshness label (advertises Tokyo's age above New York's numbers); showing all six groups at once (23 tickers is four rows of tape above the page's own answer row); a `group_order` column (a column to order six strings that change once a year -- the array appends unknown groups rather than dropping them, which is the property that matters) | accepted | 2026-07-31 |

## When to write an ADR

- Any architectural choice you'd struggle to explain six months from now
- Any deviation from the active plan or spec
- Locking in a tooling choice with meaningful alternatives

## How to write one

1. Copy this index entry format and create a new `NNNN-short-kebab-case-title.md` file
2. Fill in every section of the im-Jarvis template (Context, Decision, Consequences, Alternatives considered)
3. Status starts `proposed`; flip to `accepted` after sign-off
4. Add entry to this index

## Q1 reasoning pipeline ADRs (group)

ADRs 0012–0014 form a coherent design group for the L5 Q1 reasoning agent. They should be read together:

- **[0012](0012-citation-guardrail-llm-defense.md)** — the *what*: every numeric claim in the LLM output must cite a key in the L0–L4 input snapshot; un-cited or unresolved claims → retry → fallback.
- **[0013](0013-deterministic-stochastic-split.md)** — the *where*: the LLM is invoked only at L5. Layers L0–L4 are pure functions; the LLM is a constrained synthesizer, not an agent.
- **[0014](0014-candidate-set-hard-filter.md)** — the *which*: the LLM can only pick names that survived `screen_candidates`. It cannot invent tickers or break theme-asset coherence.

Together: the L5 agent is auditable (L0–L4 are pure), constrained (candidate filter), and self-checking (citation guardrail). See [ARCHITECTURE.md](../../ARCHITECTURE.md) for the data flow and [spec §14](../superpowers/specs/2026-07-21-andromeda-market-theme-platform-design.md) for the full design.

### Naming note — L5 storage tables (read before grepping the ADRs)

ADRs **0011, 0012, 0013 and 0015** were written before migration **008** and name the L5 storage tables by their **original** names. Those tables were renamed in place and are the authoritative names everywhere in the live schema and code today:

| As written in the ADRs (pre-008) | Current table (post-008, live) |
|---|---|
| `q1_agent_runs` | `research_agent_runs` |
| `q1_recommendations` | `research_recommendations` |

The rename (migration `008_rename_q1_tables.sql`, [PROGRESS 2026-07-21](../../PROGRESS.md)) was label-only — it changed no schema, guardrail, or decision, so the ADR bodies are left intact as the historical record. When a doc, query, or grep needs the live name, use the right-hand column. Only the **tables** were renamed; the L5 module and its entry point kept their names — `backend/services/q1_agent.py::run_q1_agent`.
