# Andromeda baseline

Gate 0 read-only baseline. Remote Vercel/Supabase state was not verified; see `access.md`. Browser capture skipped because Playwright unavailable; see `frontend-routes.md`.

No tracked secrets. `git ls-files` matched only `.env.example`; `.env.vercel` is untracked.
