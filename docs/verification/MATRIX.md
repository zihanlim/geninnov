# Release Verification Matrix

This matrix is the release gate for investor-critical invariants. A row is **PASS** only when the linked automated evidence exists and the stated hand reconciliation and Playwright assertion have been executed successfully in the release candidate environment. `PENDING` means the evidence or execution is still outstanding; it is not a waiver.

| invariant | automated test(s) | hand reconciliation | Playwright assertion | pass/fail |
|---|---|---|---|---|
| Signed long/short return attribution | [`tests/backend/test_portfolio_math.py:13`](../../tests/backend/test_portfolio_math.py#L13), [`tests/backend/test_portfolio_math.py:28`](../../tests/backend/test_portfolio_math.py#L28), [`tests/backend/test_daily_refresh.py:549`](../../tests/backend/test_daily_refresh.py#L549), [`tests/backend/test_daily_refresh.py:617`](../../tests/backend/test_daily_refresh.py#L617) | For one long and one short, independently calculate `weight × asset_return`; confirm a positive short-asset return detracts and contribution signs sum to portfolio return. | On `/portfolio`, reconcile the displayed signed weights and return attribution against the same fixture; no dedicated browser assertion yet. | **PENDING** — browser reconciliation outstanding |
| Since-inception cumulative compounding | [`tests/backend/test_portfolio_math.py:37`](../../tests/backend/test_portfolio_math.py#L37), [`tests/backend/test_portfolio_math.py:45`](../../tests/backend/test_portfolio_math.py#L45), [`tests/backend/test_portfolio_math.py:49`](../../tests/backend/test_portfolio_math.py#L49) | Starting at 1.0, calculate `Π(1 + daily_return) - 1` for the stored series and compare with the latest cumulative value and inception date. | [`frontend/tests/e2e/portfolio.spec.ts:3`](../../frontend/tests/e2e/portfolio.spec.ts#L3) asserts cumulative return is rendered. | **PASS** — automated coverage linked; re-run at release gate |
| Missing prices raise (no silent zero) | [`tests/backend/test_portfolio_math.py:21`](../../tests/backend/test_portfolio_math.py#L21), [`tests/backend/test_daily_refresh.py:630`](../../tests/backend/test_daily_refresh.py#L630), [`tests/backend/test_daily_refresh.py:642`](../../tests/backend/test_daily_refresh.py#L642) | Remove one required price from a fixture/run and confirm the stage aborts without writing a zero return. | Not investor-rendered; assert `/portfolio` does not show a fabricated `0.00%` for an unavailable return. No dedicated browser assertion yet. | **PENDING** — browser assertion outstanding |
| Gross/net/leverage and sector/geography concentration | [`tests/backend/test_exposure.py:18`](../../tests/backend/test_exposure.py#L18), [`tests/backend/test_exposure.py:22`](../../tests/backend/test_exposure.py#L22), [`tests/backend/test_exposure.py:26`](../../tests/backend/test_exposure.py#L26), [`tests/backend/test_exposure.py:30`](../../tests/backend/test_exposure.py#L30), [`tests/backend/test_exposure.py:34`](../../tests/backend/test_exposure.py#L34) | From signed positions, independently sum absolute weights for gross, signed weights for net, gross/capital for leverage, and group absolute weights by sector/geography. | [`frontend/tests/e2e/portfolio.spec.ts:3`](../../frontend/tests/e2e/portfolio.spec.ts#L3) covers gross/net display; leverage and concentration still need explicit assertions. | **PENDING** — leverage/concentration browser coverage incomplete |
| VaR/CVaR/Sharpe/Beta/HHI are derivations, not raw numbers | [`tests/backend/test_risk_engine.py:170`](../../tests/backend/test_risk_engine.py#L170), [`tests/backend/test_risk_engine.py:184`](../../tests/backend/test_risk_engine.py#L184), [`tests/backend/test_risk_engine.py:203`](../../tests/backend/test_risk_engine.py#L203), [`tests/backend/test_risk_engine.py:211`](../../tests/backend/test_risk_engine.py#L211), [`tests/backend/test_risk_engine.py:225`](../../tests/backend/test_risk_engine.py#L225) | Recompute all five from the stored return/position inputs and confirm each persisted/displayed value carries method, status, freshness, and uncertainty provenance. | [`frontend/tests/e2e/portfolio.spec.ts:3`](../../frontend/tests/e2e/portfolio.spec.ts#L3) covers derivation-driven portfolio rendering; inspect all five cards for status/provenance. | **PASS** — automated coverage linked; re-run at release gate |
| L5 never marks a heuristic fallback as verified | [`tests/backend/test_q1_agent.py:338`](../../tests/backend/test_q1_agent.py#L338), [`tests/backend/test_q1_agent.py:456`](../../tests/backend/test_q1_agent.py#L456) | Force the no-LLM/fallback path and inspect the persisted advisory: `fallback_used=true` and `display_status != verified`. | [`tests/e2e/research.spec.ts:12`](../../tests/e2e/research.spec.ts#L12) asserts fallback is never presented as model-generated. | **PASS** — automated coverage linked; re-run at release gate |
| L5 thesis body is `null` when `display_status in {unverified, unavailable}` | [`tests/backend/test_q1_agent.py:456`](../../tests/backend/test_q1_agent.py#L456) (body-null assertions at [L515-L532](../../tests/backend/test_q1_agent.py#L515)) | Query an unverified and unavailable advisory; confirm persisted `body IS NULL` and no stale thesis prose is exposed. | [`tests/e2e/research.spec.ts:23`](../../tests/e2e/research.spec.ts#L23) asserts unavailable thesis body is not rendered; add an explicit unverified fixture/assertion. | **PENDING** — unverified browser case outstanding |
| HypeScore uses `abs(corr)` | [`tests/backend/test_hype_calculator.py:127`](../../tests/backend/test_hype_calculator.py#L127) | Feed equal-magnitude positive and negative correlations and confirm equal correlation contribution/HypeScore. | [`tests/e2e/derivation-drawer.spec.ts:4`](../../tests/e2e/derivation-drawer.spec.ts#L4) verifies the absolute-correlation presentation. | **PASS** — automated coverage linked; re-run at release gate |
| TradeScore momentum is normalized by `elapsed_days` | [`tests/backend/test_hype_calculator.py:146`](../../tests/backend/test_hype_calculator.py#L146), [`tests/backend/test_trade_generator.py:57`](../../tests/backend/test_trade_generator.py#L57), [`tests/backend/test_daily_refresh.py:252`](../../tests/backend/test_daily_refresh.py#L252) | Compare identical score changes over 1 day and N days; confirm momentum is `delta / elapsed_days` and the pipeline supplies real date differences. | Open a trade derivation on `/trades` and reconcile displayed momentum with two dated signal rows; no dedicated assertion yet. | **PENDING** — browser assertion outstanding |
| Asset taxonomy raises on unmapped tickers | [`tests/backend/test_trade_ranker.py:160`](../../tests/backend/test_trade_ranker.py#L160) | Inject an unknown ticker into ranking/book metrics and confirm a hard failure rather than an `Other` classification. | Not investor-rendered; confirm the failed run surfaces unavailable/stale UI state rather than an invented category. No dedicated assertion yet. | **PENDING** — browser failure-state assertion outstanding |
| Lens selector is consistent across `/`, `/trades`, `/research`, `/portfolio` | [`tests/e2e/lens.spec.ts:4`](../../tests/e2e/lens.spec.ts#L4) | Select `credit` on each route and confirm the URL/state and visible candidate set remain credit-filtered across navigation. | [`tests/e2e/lens.spec.ts:4`](../../tests/e2e/lens.spec.ts#L4) exercises all four routes. | **PASS** — automated coverage linked; re-run at release gate |
| ThemeDerivationDrawer shows `\|corr\|` and selection method | [`tests/e2e/derivation-drawer.spec.ts:4`](../../tests/e2e/derivation-drawer.spec.ts#L4) | Open a theme drawer and reconcile the displayed absolute correlation and selected asset/method against `theme_signals_history` and `theme_assets`. | [`tests/e2e/derivation-drawer.spec.ts:4`](../../tests/e2e/derivation-drawer.spec.ts#L4) asserts both labels are visible. | **PASS** — automated coverage linked; re-run at release gate |
| No critical accessibility violations | Planned test: [`tests/e2e/accessibility.spec.ts:1`](../../tests/e2e/accessibility.spec.ts#L1) (file not yet present; Task 23) | Keyboard-only walkthrough of all four routes: landmarks, focus visibility/order, drawer focus handling, labels, captions, and alt text. | Run axe on `/`, `/trades`, `/research`, `/portfolio`; assert `impact === "critical"` violations equal `[]`. | **FAIL** — required test file/evidence is missing |
| Migrations 010/011 deployed status matches T1 baseline | Baseline evidence: [`docs/baseline/schema.md:30`](../baseline/schema.md#L30) and [`docs/baseline/schema.md:31`](../baseline/schema.md#L31); no automated deployment test. | Use read-only Supabase schema inspection and REST probes for `market_assets` and `prediction_markets`; update baseline only from direct evidence. T1 records both as missing. T29 hand-recon (`docs/baseline/prod-recon-2026-07-21.md`) re-confirms both tables are absent. | [`frontend/tests/e2e/dashboard.spec.ts:3`](../../frontend/tests/e2e/dashboard.spec.ts#L3) covers market-bar rendering when available; prediction-market deployment assertion is absent. | **FAIL** — T1 confirms migrations 010/011 are not deployed; T29 reconfirms |

## Release rule

Release is blocked while any row is `FAIL` or `PENDING`. Task 26 must execute the full backend, build, type-check, and Playwright suites; successful historical commits or linked tests do not substitute for a fresh release-candidate run.

## T29 lineage reconciliation — run_date 2026-07-21

Hand-recon pass over `docs/lineage/MATRIX.md` for the chosen production anchor.
Full record: [`docs/baseline/prod-recon-2026-07-21.md`](../baseline/prod-recon-2026-07-21.md).
Counts below are over the 23 numeric/advisory fields in the matrix.

| result | count | fields |
|---|---|---|
| PASS | 11 | `theme.hype_score`, `theme.trade_score`, `regime.classification`, `macro.dgs10`, `macro.hy_oas`, `macro.vix`, `portfolio.daily_return`, `portfolio.gross_exposure`, `portfolio.net_exposure`, `portfolio.leverage`, `theme_signals_history` (raw) |
| FAIL | 7 | `theme.corr_to_market` (drawer label vs normalized column), `trade.thesis.body` (fallback LLM, verified=false), `trade.evidence` (zero citations), `risk.var_95`, `risk.cvar_95`, `risk.sharpe`, `risk.beta`, `risk.hhi` (raw floats; migration 013 `numeric_derivations` not applied) |
| CANNOT_RECONCILE | 5 | `prediction.top_outcome_price` (mig 011), `market.index_change_pct` (mig 010), `portfolio.cumulative_return` (mig 014), `portfolio.inception_date` (mig 014), `portfolio.sector_concentration`, `portfolio.geo_concentration` (never persisted) |

### T29 verdict

The release gate cannot move from its current state using this anchor: 12 of 23
lineage fields either fail the reconciliation or cannot be reconciled because
migrations 010/011/013/014 are not deployed. Two blocking findings from T29:

1. **L5 fallback thesis on production.** Every recent `research_agent_runs`
   row carries `verified=false, retries=5` with empty citations and the
   deterministic fallback prose in `raw_output`. The `/research` page is
   therefore surfacing fallback content as if it were Q1 output. This is a
   live failure of the "L5 never marks a heuristic fallback as verified"
   invariant above.
2. **Risk fields are raw floats, not derivations.** `portfolio_risk` exists
   with hardcoded numeric columns; `numeric_derivations` JSONB column from
   migration 013 is absent. Per-row method/freshness/uncertainty provenance is
   missing, so the "VaR/CVaR/Sharpe/Beta/HHI are derivations, not raw
   numbers" invariant is also failing on production today.
